---
description: 科研实验记录管理规则。每次运行实验、复现基线、消融实验时自动触发。当用户提到'跑实验'、'运行实验'、'复现'、'基线对比'、'消融实验'、'sanity check'、'实验结果'等关键词时触发。核心要求：实验执行前必须先创建实验文件夹和记录，执行后必须回填结果和结论。
globs:
alwaysApply: false
---

# 科研实验记录管理规则

## 核心原则

**一次实验 = 一个文件夹**。每个实验的配置、终端日志、结果、笔记全部在同一个文件夹内，互不干扰。

## 顶层目录结构

```
experiments/
├── sanity/                          # sanity check 类实验
│   ├── EXP-001_pacs_feddsa_sanity/  # 每个实验一个文件夹
│   └── EXP-002_digit5_feddsa_sanity/
│
├── baselines/                       # 基线复现
│   ├── EXP-003_pacs_fedavg/
│   ├── EXP-004_pacs_fedbn/
│   └── EXP-005_pacs_fedproto/
│
├── main/                            # 主实验
│   ├── EXP-010_pacs_feddsa_v1/
│   └── EXP-011_domainnet_feddsa_v1/
│
├── ablation/                        # 消融实验
│   ├── EXP-020_pacs_no_share/
│   ├── EXP-021_pacs_no_decouple/
│   └── EXP-022_pacs_orth_only/
│
├── diagnostic/                      # 诊断分析（解耦质量、t-SNE等）
│   └── EXP-030_pacs_decouple_probe/
│
├── search/                          # 参数搜索
│   └── EXP-040_pacs_lambda_sweep/
│
├── TRACKER.md                       # 全局追踪表（所有实验一览）
└── SUMMARY.md                       # 结果汇总（论文主表数据）
```

## 单个实验文件夹结构

每个实验文件夹内部：

```
EXP-{编号}_{数据集}_{方法}_{描述}/
├── config.yaml          # 完整实验参数（唯一来源）
├── NOTE.md              # 实验笔记（目的、差异、结论）
├── terminal.log         # 终端输出日志（nohup/screen的原始打印）
├── results/             # 实验产出
│   ├── metrics.json     # 关键指标（准确率、损失等）
│   ├── best_model.pt    # 最佳模型权重（可选，按需保留）
│   └── figures/         # 该实验生成的图表（t-SNE、曲线等）
└── checkpoints/         # 训练中间checkpoint（可选）
```

## NOTE.md 格式（实验笔记，最重要的文件）

```markdown
# EXP-{编号} | {简短标题}

## 基本信息
- **日期**：{YYYY-MM-DD}
- **类型**：sanity / 基线复现 / 主实验 / 消融 / 诊断 / 参数搜索
- **数据集**：
- **方法**：
- **状态**：🔄 运行中 / ✅ 完成 / ❌ 失败

## 目的
为什么要跑这个实验？要验证什么假设？

## 与默认配置的差异
只写这次改了什么（完整参数见 config.yaml）：
- {参数名}: {默认值} → {本次值}，原因：{为什么改}

## 运行
- **命令**：`完整命令行`
- **GPU**：
- **Seeds**：
- **开始时间**：
- **结束时间**：
- **耗时**：

## 结果

| 指标 | 值 |
|------|---|

## 结论（必填！写发现和判断，不是只贴数字）

## 问题与备注
```

### 失败实验额外填写

```markdown
## 失败分析
- **错误信息**：
- **原因**：
- **解决方案**：
- **教训**：
```

## config.yaml 格式

```yaml
experiment:
  id: "EXP-{编号}"
  name: "{描述}"
  date: "{YYYY-MM-DD}"
  purpose: "{一句话目的}"

dataset:
  name:
  num_clients:
  split:

model:
  backbone:
  pretrained:
  feat_dim:
  proj_dim:

training:
  global_rounds:
  local_epochs:
  batch_size:
  lr:
  optimizer:
  momentum:
  weight_decay:
  seeds: []

method:
  name:
  # 算法特有参数全部写在这里

notes: ""
```

**跑过的 config.yaml 永远不修改。要改参数就建新实验文件夹。**

## 全局追踪表（TRACKER.md）

所有实验的一览表，方便快速查找：

```markdown
# 实验追踪总表

| 编号 | 日期 | 类型 | 方法 | 数据集 | Seeds | 状态 | 核心结果 | 文件夹 |
|------|------|------|------|--------|-------|------|---------|--------|
| EXP-001 | 04-10 | sanity | FedDSA | PACS | 1 | ✅ | 跑通 | sanity/EXP-001_... |
```

## 结果汇总（SUMMARY.md）

用于直接生成论文表格，只收录已完成的主实验和消融实验结果：

```markdown
# 实验结果汇总

## 主表（Table 1）

| 方法 | Art | Cartoon | Photo | Sketch | Avg | Std | 来源 |
|------|-----|---------|-------|--------|-----|-----|------|
| FedAvg | | | | | | | EXP-003 |
| FedDSA | | | | | | | EXP-010 |

## 消融表（Table 2）

| 变体 | Avg | 来源 |
|------|-----|------|
```

`来源`列链接到具体实验编号，方便追溯。

## 编号规则

- 全局唯一递增：EXP-001, EXP-002, ...
- 确定编号时扫描 TRACKER.md 找最大编号 +1
- 文件夹名：`EXP-{编号}_{数据集}_{方法}_{描述}`

## 执行流程

```
用户说"跑实验"
    │
    ▼
1. 判断实验类型 → 确定放在哪个分类目录下
    │
    ▼
2. 读取 TRACKER.md 获取最新编号 → 分配新编号
    │
    ▼
3. 创建实验文件夹：{类型}/EXP-{编号}_{描述}/
    │
    ▼
4. 创建 config.yaml（完整参数）
    │
    ▼
5. 创建 NOTE.md（填写目的、差异、命令，状态🔄）
    │
    ▼
6. 在 TRACKER.md 追加一行
    │
    ▼
7. 执行实验，终端输出重定向到 terminal.log
    │
    ▼
8. 完成后回填 NOTE.md（结果、结论、耗时、状态）
    │
    ▼
9. 将关键指标写入 results/metrics.json
    │
    ▼
10. 更新 TRACKER.md 状态和核心结果
    │
    ▼
11. 如果是主表/消融实验 → 更新 SUMMARY.md
    │
    ▼
12. 如果失败 → 在 NOTE.md 填写失败分析
```

## 终端日志捕获

实验运行时自动将终端输出保存到实验文件夹内的 terminal.log：

```bash
# nohup方式
nohup python main.py [参数] > experiments/{类型}/EXP-{编号}_{描述}/terminal.log 2>&1 &

# screen方式
screen -S exp{编号}
python main.py [参数] | tee experiments/{类型}/EXP-{编号}_{描述}/terminal.log
```
