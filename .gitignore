# === 排除目录 ===
无关文件/

# 模型权重（太大）
Qwen3-VL-4B-mlc/
mlc-qwen3-vl/
mlc-qwen3-vl.zip

# PDF论文（大文件）
papers/

# 临时论文文本提取 / 服务器备份(大文件) — 都不入 Git
_pdf_texts/
_westb_backup/
_fedccrl_text.txt
_*.txt

# === 数据集原始数据（保留生成脚本 .py）===
# PFLlib 数据集：排除生成出来的数据，保留 generate_*.py
PFLlib/dataset/MNIST/
PFLlib/dataset/utils/LEAF/
# 未来可能下载的其他数据集目录
PFLlib/dataset/*/rawdata/
PFLlib/dataset/*/train/
PFLlib/dataset/*/test/

# RethinkFL 数据
RethinkFL/data/

# PARDON 风格统计缓存
PARDON-FedDG/style_stats/

# === 通用排除 ===
__pycache__/
*.pyc
*.pyo
*.egg-info/
.eggs/
dist/
build/
*.so
*.bin

# Office 临时文件
~$*

# OS 文件
.DS_Store
Thumbs.db
desktop.ini

# Python 环境
.env
.venv
env/
venv/

# Jupyter
.ipynb_checkpoints/

# IDE
.idea/
.vscode/
*.swp
*.swo

# 实验追踪（wandb太大，不入Git）
wandb/

# 大模型检查点文件（单个可达数百MB）
*.pth
*.pt
*.ckpt
# Log files with | in name (Windows incompatible)
FDSE_CVPR25/task/*/log/*|*

# === 临时脚本 / 调试输出 (不入 git) ===
_tmp_*
tmp_*
_novelty_*.txt
refine-tmp/

# 历史 conversation dumps
2026-04-*-*-*.txt

# Windows 二进制 / 崩溃文件
*.exe
*.stackdump

# Office 文档 / 无关文件
*.doc
*.docx
*.pptx
docx_output.txt

# 实验结果汇总 Excel (可由脚本重新生成)
experiment_master_table*.xlsx
experiment_results_*.xlsx
per_domain_analysis.txt

# 大数据压缩包 / 误分类可视化
*.tar.gz
misclass_pacs/
misclass_viz/
dn_subset/

# Claude Code 本地 state
.claude/scheduled_tasks.lock
.claude/scripts/
.claude/sessions/
.claude/worktrees/
.claude/settings.local.json
.planning/

# 外部工具仓库 (不应在 repo 里)
clash-for-linux-install/
clashctl/
rr/

# Obsidian 临时 / 缓存文件
obsidian_exprtiment_results/未命名*.base
obsidian_exprtiment_results/.obsidian/workspace.json
obsidian_exprtiment_results/.obsidian/page-preview.json


# F2DC vendored repo
F2DC/.git/
F2DC/__pycache__/
F2DC/**/__pycache__/
F2DC/checkpoint/
F2DC/rundata/
F2DC/data/
F2DC/_smoke_test/
F2DC/*.pyc


c1先跑着看看 但是要加一些诊断指标 如果效果不好是否真的如这个语言一般
m1，m2 要实际看一下我们现在复线的实验里面各个client分布如何再下定论
其他的没太多问题先不管
infonce加ablation试试
然后改一下实验的设计方案
# === cold path 诊断 dump (大文件, ~1.5GB, 服务器有备份) ===
experiments/cold_path_analysis/diag_office/
experiments/cold_path_analysis/diag_pacs/
experiments/cold_path_analysis/figs/*.png

# === EXP-13x 实验 cold-path diagnostic dump (太大, 用 rsync 同步, 不入 git) ===
experiments/ablation/EXP-135_diag_full/diag_*/
experiments/ablation/EXP-137_pg_rerun_after_fix/diag_*/
