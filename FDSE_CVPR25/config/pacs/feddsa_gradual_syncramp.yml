# EXP-075: Gradual training + shallow aug + synchronized ramps (t_mid_aug = t_mid_align = 30)
# Ablation: does decoupling aug/align schedules matter?
algo_para:
- 1.0    # lo
- 0.0    # lh
- 1.0    # ls
- 0.2    # tau
- 5      # sdn
- 128    # pd
- 30     # tma: same as align
- 10     # twa
- 30     # tml: same as aug
- 10     # twl
- 1      # al: shallow
- 10     # gli
learning_rate: 0.1
batch_size: 50
weight_decay: 1e-3
lr_scheduler: 0
learning_rate_decay: 0.9998
num_rounds: 200
num_epochs: 5
clip_grad: 10
proportion: 1.0
train_holdout: 0.2
local_test: True
no_log_console: True
log_file: True
