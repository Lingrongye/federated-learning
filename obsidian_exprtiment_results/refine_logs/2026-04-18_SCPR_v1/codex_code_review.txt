OpenAI Codex v0.117.0 (research preview)
--------
workdir: D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-18_SCPR_v1
model: gpt-5.4
provider: fox
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR, C:\Users\31279\.codex\memories]
reasoning effort: xhigh
reasoning summaries: none
session id: 019da082-0845-70d0-99b3-c3fad2fa8e34
--------
user
You are a senior ML engineer reviewing a PyTorch/Federated-Learning diff that adds a new mechanism called SCPR (Self-Masked Style-Weighted Multi-Positive InfoNCE) to an existing FL codebase.

CONTEXT:
- The method has been refined through 5 rounds of GPT-5.4 review and reached READY 9.1/10. Design spec at: obsidian_exprtiment_results/refine_logs/2026-04-18_SCPR_v1/FINAL_PROPOSAL.md
- The codebase is flgo-based. Server collects per-client class prototypes and style vectors, Client does FedBN + orth decouple + InfoNCE variants.
- SCPR is added as scpr (0/1/2) + scpr_tau algo_para to feddsa_scheduled.py.
  - scpr=0: off (equivalent to existing Plan A / orth_only)
  - scpr=1: uniform multi-positive over other clients (M3 lower bound; = SCPR tau -> infinity)
  - scpr=2: style-weighted multi-positive (SCPR, OURS)
  - Always self-masked (j != k by construction)
- Mathematical property: scpr=1 should strictly reduce to M3 multi-positive SupCon; high scpr_tau under scpr=2 should approach this.

YOUR JOB:
Review the diff below strictly. Focus on:
(1) CORRECTNESS of the math (self-mask, per-class renormalize, detach, softmax numerical stability)
(2) CORRECTNESS of the flgo integration (new key 'scpr_payload' in svr_pkg, backward compat when scpr=0)
(3) EDGE CASES (empty bank, missing class, 2-client vs 4-client, first-round bank empty, K=1 after self-mask, has_pos=0 samples)
(4) MEMORY / PERFORMANCE issues (proto_matrix rebuilt every batch — is that OK? any gradient leak?)
(5) INTERACTION with existing sas/sm paths (can scpr coexist with sas=1 without conflict?)

Find any bugs, including subtle ones. Rank each finding:
- CRITICAL: breaks correctness or math
- IMPORTANT: breaks in edge case / performance regression / hidden gradient leak
- MINOR: style, docstring, micro-optimization

Output format:
1. Summary verdict: READY / MINOR_FIXES / MUST_FIX
2. Findings list ranked by priority
3. For each CRITICAL/IMPORTANT, give the exact line-level fix

Do NOT rewrite the diff. Give structured feedback only.

=== DIFF ===
diff --git a/FDSE_CVPR25/algorithm/feddsa_scheduled.py b/FDSE_CVPR25/algorithm/feddsa_scheduled.py
index c1d5219..70b49e5 100644
--- a/FDSE_CVPR25/algorithm/feddsa_scheduled.py
+++ b/FDSE_CVPR25/algorithm/feddsa_scheduled.py
@@ -131,6 +131,16 @@ class Server(flgo.algorithm.fedbase.BasicServer):
                           #   3 = sem_head sas + classifier uniform-avg (C2 counterfactual)
                           #   4 = sem_head sas + classifier fully local (C1 counterfactual)
             'sas_tau': 0.3,  # softmax temperature for style similarity
+            # -------- SCPR (Self-Masked Style-Weighted Multi-Positive InfoNCE) --------
+            # NEW (2026-04-19): refined via GPT-5.4 x5-round refine session
+            # (obsidian_exprtiment_results/refine_logs/2026-04-18_SCPR_v1/)
+            'scpr': 0,    # SCPR mode:
+                          #   0 = off (fall back to sm schedule / Plan A, no SCPR loss)
+                          #   1 = uniform multi-positive (= M3 / SCPR tau->inf lower bound)
+                          #   2 = style-weighted multi-positive (SCPR, OURS)
+                          # Always self-masked (j != k) when mode > 0.
+                          # Uses domain-indexed class prototypes; NO new trainable params.
+            'scpr_tau': 0.3,  # SCPR style attention temperature (inherited from SAS optimal)
         })
         # Readable aliases
         self.lambda_orth = float(self.lo)
@@ -149,6 +159,9 @@ class Server(flgo.algorithm.fedbase.BasicServer):
         self.save_errors = int(self.se)
         self.style_aware_sem = int(self.sas)
         self.style_aware_tau = float(self.sas_tau)
+        # SCPR aliases
+        self.scpr_mode = int(self.scpr)
+        self.scpr_tau_val = float(self.scpr_tau)
         self.sample_option = 'full'
 
         # 方案 A：per-client semantic_head states (最近一轮上传的)
@@ -160,6 +173,9 @@ class Server(flgo.algorithm.fedbase.BasicServer):
         self.style_bank = {}
         # Global semantic prototypes
         self.global_semantic_protos = {}
+        # SCPR: per-(client, class) prototypes bank (domain-indexed)
+        #   key: client_id -> value: dict {class_idx: proto_tensor (cpu)}
+        self.client_class_protos = {}
         # Gradient conflict log
         self.grad_conflict_log = {}
         # Best checkpoint tracking (for save_errors)
@@ -186,6 +202,9 @@ class Server(flgo.algorithm.fedbase.BasicServer):
             c.grad_log_interval = self.grad_log_interval
             c.lambda_mse = self.lambda_mse
             c.alpha_sparsity = self.alpha_sparsity
+            # SCPR params to client
+            c.scpr_mode = self.scpr_mode
+            c.scpr_tau_val = self.scpr_tau_val
 
     def _init_agg_keys(self):
         """Classify model keys into: shared (FedAvg), private (style_head+BN)."""
@@ -271,11 +290,18 @@ class Server(flgo.algorithm.fedbase.BasicServer):
         if overridden:
             model_to_send.load_state_dict(model_state)
 
+        # SCPR: compute self-masked style-weighted (or uniform) prototype payload
+        # Returns None when scpr=0 OR bank insufficient; client falls back to non-SCPR path.
+        scpr_payload = None
+        if self.scpr_mode > 0 and len(self.client_class_protos) >= 2:
+            scpr_payload = self._compute_scpr_payload(client_id)
+
         return {
             'model': model_to_send,
             'global_protos': copy.deepcopy(self.global_semantic_protos),
             'style_bank': dispatched_styles,
             'current_round': self.current_round,
+            'scpr_payload': scpr_payload,
         }
 
     @staticmethod
@@ -358,6 +384,70 @@ class Server(flgo.algorithm.fedbase.BasicServer):
                     accumulated[k] += st[k] / n
         return accumulated
 
+    def _compute_scpr_payload(self, target_cid):
+        """Compute SCPR payload for target client: self-masked style-weighted
+        (or uniform) prototype dispatch.
+
+        scpr_mode=1 (uniform multi-positive, = M3 lower bound):
+            w_{k->j}^{uniform} = 1 / (K-1) for all j != k that have proto(s)
+        scpr_mode=2 (style-weighted, OURS):
+            w_{k->j} = softmax_j( cos(s_k, s_j) / tau_SCPR ) * 1[j != k]
+
+        Returns:
+            None if insufficient bank or target style missing.
+            Otherwise dict with two keys:
+              'weights': {source_cid: float}  (self-masked, sums to 1 over source_cids)
+              'protos':  {source_cid: {class_idx: proto_tensor_cpu}}  (raw per-(class, client) bank,
+                         client will re-normalize per-class based on availability)
+        """
+        import torch as _t
+        target_style = self.style_bank.get(target_cid)
+        if target_style is None:
+            return None
+        target_vec = self._extract_style_vec(target_style)
+
+        # Collect candidates (self-masked: j != target_cid)
+        source_cids = []
+        source_vecs = []
+        source_protos = {}
+        for cid, protos in self.client_class_protos.items():
+            if cid == target_cid:  # SELF-MASK (R1 reviewer fix)
+                continue
+            src_style = self.style_bank.get(cid)
+            if src_style is None:
+                continue
+            src_vec = self._extract_style_vec(src_style)
+            if src_vec.shape != target_vec.shape:
+                continue
+            if protos is None or len(protos) == 0:
+                continue
+            source_cids.append(cid)
+            source_vecs.append(src_vec)
+            source_protos[cid] = protos
+
+        if len(source_cids) == 0:
+            return None
+
+        # Attention weights over source clients (self-mask already applied above)
+        if self.scpr_mode == 1:
+            # Uniform (mathematically equivalent to SCPR tau -> infinity; = M3 lower bound)
+            uniform_w = 1.0 / len(source_cids)
+            weights = {cid: uniform_w for cid in source_cids}
+        elif self.scpr_mode == 2:
+            # Style-weighted softmax (SCPR main method)
+            sims = []
+            for src_vec in source_vecs:
+                dot = (src_vec * target_vec).sum()
+                norm = src_vec.norm() * target_vec.norm() + 1e-8
+                sims.append((dot / norm).item())
+            sims_t = _t.tensor(sims, dtype=_t.float32) / max(self.scpr_tau_val, 1e-3)
+            w_vec = _t.softmax(sims_t, dim=0).tolist()
+            weights = {cid: float(w) for cid, w in zip(source_cids, w_vec)}
+        else:
+            return None
+
+        return {'weights': weights, 'protos': source_protos}
+
     def iterate(self):
         self.selected_clients = self.sample()
         res = self.communicate(self.selected_clients)
@@ -390,6 +480,14 @@ class Server(flgo.algorithm.fedbase.BasicServer):
             if style is not None:
                 self.style_bank[cid] = style
 
+        # SCPR: update per-(client, class) prototype bank (domain-indexed)
+        if self.scpr_mode > 0:
+            for cid, protos in zip(self.received_clients, protos_list):
+                if protos is not None and len(protos) > 0:
+                    self.client_class_protos[cid] = {
+                        c: p.detach().clone().cpu() for c, p in protos.items()
+                    }
+
         # Aggregate global protos (weighted mean)
         self._aggregate_protos(protos_list, proto_counts_list)
 
@@ -550,12 +648,18 @@ class Client(flgo.algorithm.fedbase.BasicClient):
         self.local_style_bank = None
         self.current_round = 0
         self._grad_conflict_log = None
+        # SCPR state (populated by Server.initialize via `c.scpr_mode=...`)
+        self.scpr_mode = 0
+        self.scpr_tau_val = 0.3
+        self.scpr_payload = None
 
     def reply(self, svr_pkg):
         model, global_protos, style_bank, current_round = self.unpack(svr_pkg)
         self.current_round = current_round
         self.global_protos = global_protos
         self.local_style_bank = style_bank
+        # SCPR: extract per-round payload (self-masked weights + domain protos)
+        self.scpr_payload = svr_pkg.get('scpr_payload', None)
         self.train(model)
         return self.pack()
 
@@ -725,6 +829,13 @@ class Client(flgo.algorithm.fedbase.BasicClient):
                 + w_aux * self.lambda_mse * loss_mse_anchor
             )
 
+            # SCPR loss: self-masked (style-)weighted multi-positive InfoNCE.
+            # Independent from sm/w_aux schedule (works even when Plan A sm=0).
+            # scpr_mode=0 → skip entirely; =1 → uniform multi-pos (M3 bound); =2 → style-weighted.
+            if self.scpr_mode > 0 and self.scpr_payload is not None:
+                loss_scpr = self._scpr_loss(z_sem, y)
+                loss = loss + self.lambda_sem * loss_scpr
+
             # Gradient conflict logging (last batch of last epoch)
             is_last_batch = (step == num_steps - 1)
             if should_log_grad and is_last_batch and loss_sem.item() > 0:
@@ -935,6 +1046,77 @@ class Client(flgo.algorithm.fedbase.BasicClient):
         valid_t = torch.tensor(valid, device=z_sem.device)
         return F.mse_loss(z_sem[valid_t], target_matrix)
 
+    # ----------------------------------------------------------------
+    # SCPR: Self-Masked Style-Weighted Multi-Positive InfoNCE
+    # ----------------------------------------------------------------
+
+    def _scpr_loss(self, z_sem, y):
+        """Self-masked (style-)weighted multi-positive InfoNCE.
+
+        For client k (self-masked by Server: j != k), given domain-indexed
+        protos {p_c^j} and client-pair weights w_{k->j}, and target sample
+        (z_i, y_i=c), the loss (per-class renormalized) is:
+            L = - sum_{j in A_i^c} (w_{k->j} / Z_i^c) * log [
+                  exp(sim(z_i, p_c^j)/tau_nce) /
+                  ( exp(sim(z_i, p_c^j)/tau_nce)
+                    + sum_{c' != c, l in A_i^{c'}} exp(sim(z_i, p_{c'}^l)/tau_nce) )
+                ]
+        where A_i^c = {j != k : p_c^j exists} and Z_i^c = sum of weights over A_i^c.
+
+        Under scpr_mode=1 (uniform weights) this reduces to M3 domain-aware
+        multi-positive SupCon (PACS EXP-072 +5.09% lower bound). Under
+        scpr_mode=2 (style-weighted softmax) it is the full SCPR method.
+
+        All prototypes are detached (soft anchors, no grad back to bank).
+        """
+        if self.scpr_payload is None:
+            return torch.tensor(0.0, device=z_sem.device)
+        weights = self.scpr_payload.get('weights', {})
+        domain_protos = self.scpr_payload.get('protos', {})
+        if not weights or not domain_protos:
+            return torch.tensor(0.0, device=z_sem.device)
+
+        device = z_sem.device
+
+        # Flatten into per-entry arrays: each entry = (class, source_cid, proto, weight)
+        entry_protos = []
+        entry_classes = []
+        entry_weights = []
+        for cid, protos in domain_protos.items():
+            w = float(weights.get(cid, 0.0))
+            for c, proto in protos.items():
+                entry_protos.append(proto)
+                entry_classes.append(int(c))
+                entry_weights.append(w)
+
+        if len(entry_protos) < 2:
+            return torch.tensor(0.0, device=device)
+
+        proto_matrix = torch.stack([p.to(device) for p in entry_protos]).detach()  # [N, D]
+        entry_classes_t = torch.tensor(entry_classes, device=device, dtype=torch.long)  # [N]
+        entry_weights_t = torch.tensor(entry_weights, device=device, dtype=torch.float32)  # [N]
+
+        # Cosine-similarity logits with InfoNCE temperature
+        z_n = F.normalize(z_sem, dim=1)
+        p_n = F.normalize(proto_matrix, dim=1)
+        logits = (z_n @ p_n.T) / self.tau  # [B, N]
+        log_prob = logits - torch.logsumexp(logits, dim=1, keepdim=True)  # [B, N]
+
+        # Positive mask: entry.class == y[i]  -> [B, N]
+        y_col = y.unsqueeze(1)  # [B, 1]
+        pos_mask = (entry_classes_t.unsqueeze(0) == y_col).float()  # [B, N]
+
+        # Per-sample renormalize positive weights over A_i^c (clients that have class c)
+        pos_w = pos_mask * entry_weights_t.unsqueeze(0)  # [B, N]
+        pos_w_sum = pos_w.sum(dim=1, keepdim=True)  # [B, 1]
+        pos_w_norm = pos_w / pos_w_sum.clamp(min=1e-8)  # [B, N]
+
+        # Weighted NLL; skip samples with no positive (A_i^c empty)
+        loss_per_sample = -(pos_w_norm * log_prob).sum(dim=1)  # [B]
+        has_pos = (pos_w_sum.squeeze(1) > 1e-8).float()  # [B]
+        denom = has_pos.sum().clamp(min=1.0)
+        return (loss_per_sample * has_pos).sum() / denom
+
 
 # ============================================================
 # Model initialization

=== END DIFF ===

codex
Reviewing the diff against the surrounding implementation first. I’m checking the actual `feddsa_scheduled.py` context and the review skill guidance so the findings stay line-accurate and focused on math, integration, edge cases, and interaction paths.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'FDSE_CVPR25\\algorithm\\feddsa_scheduled.py' | Select-Object -First 1200" in D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-18_SCPR_v1
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'C:\\Users\\31279\\.agents\\skills\\code-review-excellence\\SKILL.md'" in D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-18_SCPR_v1
 succeeded in 871ms:
---
name: code-review-excellence
description: Master effective code review practices to provide constructive feedback, catch bugs early, and foster knowledge sharing while maintaining team morale. Use when reviewing pull requests, establishing review standards, or mentoring developers.
---

# Code Review Excellence

Transform code reviews from gatekeeping to knowledge sharing through constructive feedback, systematic analysis, and collaborative improvement.

## When to Use This Skill

- Reviewing pull requests and code changes
- Establishing code review standards for teams
- Mentoring junior developers through reviews
- Conducting architecture reviews
- Creating review checklists and guidelines
- Improving team collaboration
- Reducing code review cycle time
- Maintaining code quality standards

## Core Principles

### 1. The Review Mindset

**Goals of Code Review:**

- Catch bugs and edge cases
- Ensure code maintainability
- Share knowledge across team
- Enforce coding standards
- Improve design and architecture
- Build team culture

**Not the Goals:**

- Show off knowledge
- Nitpick formatting (use linters)
- Block progress unnecessarily
- Rewrite to your preference

### 2. Effective Feedback

**Good Feedback is:**

- Specific and actionable
- Educational, not judgmental
- Focused on the code, not the person
- Balanced (praise good work too)
- Prioritized (critical vs nice-to-have)

```markdown
❌ Bad: "This is wrong."
✅ Good: "This could cause a race condition when multiple users
access simultaneously. Consider using a mutex here."

❌ Bad: "Why didn't you use X pattern?"
✅ Good: "Have you considered the Repository pattern? It would
make this easier to test. Here's an example: [link]"

❌ Bad: "Rename this variable."
✅ Good: "[nit] Consider `userCount` instead of `uc` for
clarity. Not blocking if you prefer to keep it."
```

### 3. Review Scope

**What to Review:**

- Logic correctness and edge cases
- Security vulnerabilities
- Performance implications
- Test coverage and quality
- Error handling
- Documentation and comments
- API design and naming
- Architectural fit

**What Not to Review Manually:**

- Code formatting (use Prettier, Black, etc.)
- Import organization
- Linting violations
- Simple typos

## Review Process

### Phase 1: Context Gathering (2-3 minutes)

```markdown
Before diving into code, understand:

1. Read PR description and linked issue
2. Check PR size (>400 lines? Ask to split)
3. Review CI/CD status (tests passing?)
4. Understand the business requirement
5. Note any relevant architectural decisions
```

### Phase 2: High-Level Review (5-10 minutes)

```markdown
1. **Architecture & Design**
   - Does the solution fit the problem?
   - Are there simpler approaches?
   - Is it consistent with existing patterns?
   - Will it scale?

2. **File Organization**
   - Are new files in the right places?
   - Is code grouped logically?
   - Are there duplicate files?

3. **Testing Strategy**
   - Are there tests?
   - Do tests cover edge cases?
   - Are tests readable?
```

### Phase 3: Line-by-Line Review (10-20 minutes)

```markdown
For each file:

1. **Logic & Correctness**
   - Edge cases handled?
   - Off-by-one errors?
   - Null/undefined checks?
   - Race conditions?

2. **Security**
   - Input validation?
   - SQL injection risks?
   - XSS vulnerabilities?
   - Sensitive data exposure?

3. **Performance**
   - N+1 queries?
   - Unnecessary loops?
   - Memory leaks?
   - Blocking operations?

4. **Maintainability**
   - Clear variable names?
   - Functions doing one thing?
   - Complex code commented?
   - Magic numbers extracted?
```

### Phase 4: Summary & Decision (2-3 minutes)

```markdown
1. Summarize key concerns
2. Highlight what you liked
3. Make clear decision:
   - ✅ Approve
   - 💬 Comment (minor suggestions)
   - 🔄 Request Changes (must address)
4. Offer to pair if complex
```

## Review Techniques

### Technique 1: The Checklist Method

```markdown
## Security Checklist

- [ ] User input validated and sanitized
- [ ] SQL queries use parameterization
- [ ] Authentication/authorization checked
- [ ] Secrets not hardcoded
- [ ] Error messages don't leak info

## Performance Checklist

- [ ] No N+1 queries
- [ ] Database queries indexed
- [ ] Large lists paginated
- [ ] Expensive operations cached
- [ ] No blocking I/O in hot paths

## Testing Checklist

- [ ] Happy path tested
- [ ] Edge cases covered
- [ ] Error cases tested
- [ ] Test names are descriptive
- [ ] Tests are deterministic
```

### Technique 2: The Question Approach

Instead of stating problems, ask questions to encourage thinking:

```markdown
❌ "This will fail if the list is empty."
✅ "What happens if `items` is an empty array?"

❌ "You need error handling here."
✅ "How should this behave if the API call fails?"

❌ "This is inefficient."
✅ "I see this loops through all users. Have we considered
the performance impact with 100k users?"
```

### Technique 3: Suggest, Don't Command

````markdown
## Use Collaborative Language

❌ "You must change this to use async/await"
✅ "Suggestion: async/await might make this more readable:
`typescript
    async function fetchUser(id: string) {
        const user = await db.query('SELECT * FROM users WHERE id = ?', id);
        return user;
    }
    `
What do you think?"

❌ "Extract this into a function"
✅ "This logic appears in 3 places. Would it make sense to
extract it into a shared utility function?"
````

### Technique 4: Differentiate Severity

```markdown
Use labels to indicate priority:

🔴 [blocking] - Must fix before merge
🟡 [important] - Should fix, discuss if disagree
🟢 [nit] - Nice to have, not blocking
💡 [suggestion] - Alternative approach to consider
📚 [learning] - Educational comment, no action needed
🎉 [praise] - Good work, keep it up!

Example:
"🔴 [blocking] This SQL query is vulnerable to injection.
Please use parameterized queries."

"🟢 [nit] Consider renaming `data` to `userData` for clarity."

"🎉 [praise] Excellent test coverage! This will catch edge cases."
```

## Language-Specific Patterns

### Python Code Review

```python
# Check for Python-specific issues

# ❌ Mutable default arguments
def add_item(item, items=[]):  # Bug! Shared across calls
    items.append(item)
    return items

# ✅ Use None as default
def add_item(item, items=None):
    if items is None:
        items = []
    items.append(item)
    return items

# ❌ Catching too broad
try:
    result = risky_operation()
except:  # Catches everything, even KeyboardInterrupt!
    pass

# ✅ Catch specific exceptions
try:
    result = risky_operation()
except ValueError as e:
    logger.error(f"Invalid value: {e}")
    raise

# ❌ Using mutable class attributes
class User:
    permissions = []  # Shared across all instances!

# ✅ Initialize in __init__
class User:
    def __init__(self):
        self.permissions = []
```

### TypeScript/JavaScript Code Review

```typescript
// Check for TypeScript-specific issues

// ❌ Using any defeats type safety
function processData(data: any) {  // Avoid any
    return data.value;
}

// ✅ Use proper types
interface DataPayload {
    value: string;
}
function processData(data: DataPayload) {
    return data.value;
}

// ❌ Not handling async errors
async function fetchUser(id: string) {
    const response = await fetch(`/api/users/${id}`);
    return response.json();  // What if network fails?
}

// ✅ Handle errors properly
async function fetchUser(id: string): Promise<User> {
    try {
        const response = await fetch(`/api/users/${id}`);
        if (!response.ok) {
            throw new Error(`HTTP ${response.status}`);
        }
        return await response.json();
    } catch (error) {
        console.error('Failed to fetch user:', error);
        throw error;
    }
}

// ❌ Mutation of props
function UserProfile({ user }: Props) {
    user.lastViewed = new Date();  // Mutating prop!
    return <div>{user.name}</div>;
}

// ✅ Don't mutate props
function UserProfile({ user, onView }: Props) {
    useEffect(() => {
        onView(user.id);  // Notify parent to update
    }, [user.id]);
    return <div>{user.name}</div>;
}
```

## Advanced Review Patterns

### Pattern 1: Architectural Review

```markdown
When reviewing significant changes:

1. **Design Document First**
   - For large features, request design doc before code
   - Review design with team before implementation
   - Agree on approach to avoid rework

2. **Review in Stages**
   - First PR: Core abstractions and interfaces
   - Second PR: Implementation
   - Third PR: Integration and tests
   - Easier to review, faster to iterate

3. **Consider Alternatives**
   - "Have we considered using [pattern/library]?"
   - "What's the tradeoff vs. the simpler approach?"
   - "How will this evolve as requirements change?"
```

### Pattern 2: Test Quality Review

```typescript
// ❌ Poor test: Implementation detail testing
test('increments counter variable', () => {
    const component = render(<Counter />);
    const button = component.getByRole('button');
    fireEvent.click(button);
    expect(component.state.counter).toBe(1);  // Testing internal state
});

// ✅ Good test: Behavior testing
test('displays incremented count when clicked', () => {
    render(<Counter />);
    const button = screen.getByRole('button', { name: /increment/i });
    fireEvent.click(button);
    expect(screen.getByText('Count: 1')).toBeInTheDocument();
});

// Review questions for tests:
// - Do tests describe behavior, not implementation?
// - Are test names clear and descriptive?
// - Do tests cover edge cases?
// - Are tests independent (no shared state)?
// - Can tests run in any order?
```

### Pattern 3: Security Review

```markdown
## Security Review Checklist

### Authentication & Authorization

- [ ] Is authentication required where needed?
- [ ] Are authorization checks before every action?
- [ ] Is JWT validation proper (signature, expiry)?
- [ ] Are API keys/secrets properly secured?

### Input Validation

- [ ] All user inputs validated?
- [ ] File uploads restricted (size, type)?
- [ ] SQL queries parameterized?
- [ ] XSS protection (escape output)?

### Data Protection

- [ ] Passwords hashed (bcrypt/argon2)?
- [ ] Sensitive data encrypted at rest?
- [ ] HTTPS enforced for sensitive data?
- [ ] PII handled according to regulations?

### Common Vulnerabilities

- [ ] No eval() or similar dynamic execution?
- [ ] No hardcoded secrets?
- [ ] CSRF protection for state-changing operations?
- [ ] Rate limiting on public endpoints?
```

## Giving Difficult Feedback

### Pattern: The Sandwich Method (Modified)

```markdown
Traditional: Praise + Criticism + Praise (feels fake)

Better: Context + Specific Issue + Helpful Solution

Example:
"I noticed the payment processing logic is inline in the
controller. This makes it harder to test and reuse.

[Specific Issue]
The calculateTotal() function mixes tax calculation,
discount logic, and database queries, making it difficult
to unit test and reason about.

[Helpful Solution]
Could we extract this into a PaymentService class? That
would make it testable and reusable. I can pair with you
on this if helpful."
```

### Handling Disagreements

```markdown
When author disagrees with your feedback:

1. **Seek to Understand**
   "Help me understand your approach. What led you to
   choose this pattern?"

2. **Acknowledge Valid Points**
   "That's a good point about X. I hadn't considered that."

3. **Provide Data**
   "I'm concerned about performance. Can we add a benchmark
   to validate the approach?"

4. **Escalate if Needed**
   "Let's get [architect/senior dev] to weigh in on this."

5. **Know When to Let Go**
   If it's working and not a critical issue, approve it.
   Perfection is the enemy of progress.
```

## Best Practices

1. **Review Promptly**: Within 24 hours, ideally same day
2. **Limit PR Size**: 200-400 lines max for effective review
3. **Review in Time Blocks**: 60 minutes max, take breaks
4. **Use Review Tools**: GitHub, GitLab, or dedicated tools
5. **Automate What You Can**: Linters, formatters, security scans
6. **Build Rapport**: Emoji, praise, and empathy matter
7. **Be Available**: Offer to pair on complex issues
8. **Learn from Others**: Review others' review comments

## Common Pitfalls

- **Perfectionism**: Blocking PRs for minor style preferences
- **Scope Creep**: "While you're at it, can you also..."
- **Inconsistency**: Different standards for different people
- **Delayed Reviews**: Letting PRs sit for days
- **Ghosting**: Requesting changes then disappearing
- **Rubber Stamping**: Approving without actually reviewing
- **Bike Shedding**: Debating trivial details extensively

## Templates

### PR Review Comment Template

```markdown
## Summary

[Brief overview of what was reviewed]

## Strengths

- [What was done well]
- [Good patterns or approaches]

## Required Changes

🔴 [Blocking issue 1]
🔴 [Blocking issue 2]

## Suggestions

💡 [Improvement 1]
💡 [Improvement 2]

## Questions

❓ [Clarification needed on X]
❓ [Alternative approach consideration]

## Verdict

✅ Approve after addressing required changes
```

 exited 1 in 1076ms:
[31;1mGet-Content: [0m
[31;1m[36;1mLine |[0m
[31;1m[36;1m[36;1m   2 | [0m [36;1mGet-Content -Path 'FDSE_CVPR25\algorithm\feddsa_scheduled.py'[0m | Selec …[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m     | [31;1m ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m[31;1m[31;1m[36;1m     | [31;1mCannot find path 'D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-18_SCPR_v1\FDSE_CVPR25\algorithm\feddsa_scheduled.py' because it does not exist.[0m

2026-04-18T12:13:13.160247Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 1.1 seconds
Output:
[31;1mGet-Content: [0m
[31;1m[36;1mLine |[0m
[31;1m[36;1m[36;1m   2 | [0m [36;1mGet-Content -Path 'FDSE_CVPR25\algorithm\feddsa_scheduled.py'[0m | Selec …[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m     | [31;1m ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m[31;1m[31;1m[36;1m     | [31;1mCannot find path 'D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-18_SCPR_v1\FDSE_CVPR25\algorithm\feddsa_scheduled.py' because it does not exist.[0m

ERROR: Reconnecting... 1/5
codex
The review guidance is loaded. The code file path in the pasted diff is not rooted at the current directory, so I’m locating the actual repo copy and then I’ll trace the surrounding server/client paths that SCPR plugs into.
ERROR: Reconnecting... 2/5
codex
The file path in the pasted diff isn’t relative to the current folder, so I’m locating the real checkout and then reading the surrounding server/client code before I judge the SCPR math or integration.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-ChildItem -Recurse -Filter 'feddsa_scheduled.py' | Select-Object -ExpandProperty FullName" in D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-18_SCPR_v1
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-ChildItem -Recurse -Filter 'FINAL_PROPOSAL.md' | Select-Object -ExpandProperty FullName" in D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-18_SCPR_v1
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-ChildItem -Force' in D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-18_SCPR_v1
 succeeded in 829ms:

    Directory: D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-18_SCPR_v1

[32;1mMode   [0m[32;1m              LastWriteTime[0m[32;1m         Length[0m[32;1m Name[0m
[32;1m----   [0m [32;1m             -------------[0m [32;1m        ------[0m [32;1m----[0m
-a---           2026/4/18     5:12              0 codex_code_review.txt
-a---           2026/4/18     4:46          21570 EXPERIMENT_PLAN.md
-a---           2026/4/18     4:47           3972 EXPERIMENT_TRACKER.md
-a---           2026/4/18     4:17          17352 FINAL_PROPOSAL.md
-a---           2026/4/18     4:14            201 REFINE_STATE.json
-a---           2026/4/18     4:19          12055 REFINEMENT_REPORT.md
-a---           2026/4/18     3:21          15868 review_prompt_r1.txt
-a---           2026/4/18     3:32          10249 review_prompt_r2.txt
-a---           2026/4/18     3:39          10932 review_prompt_r3.txt
-a---           2026/4/18     3:45           7880 review_prompt_r4.txt
-a---           2026/4/18     4:12           8911 review_prompt_r5.txt
-a---           2026/4/18     4:26           6737 REVIEW_SUMMARY.md
-a---           2026/4/18     3:19          19637 round-0-initial-proposal.md
-a---           2026/4/18     3:30          16795 round-1-refinement.md
-a---           2026/4/18     3:26          34098 round-1-review-raw.txt
-a---           2026/4/18     3:27           5846 round-1-review.md
-a---           2026/4/18     3:38          16138 round-2-refinement.md
-a---           2026/4/18     3:34          20924 round-2-review-raw.txt
-a---           2026/4/18     3:35           4395 round-2-review.md
-a---           2026/4/18     3:44          12162 round-3-refinement.md
-a---           2026/4/18     3:41          17455 round-3-review-raw.txt
-a---           2026/4/18     3:42           4209 round-3-review.md
-a---           2026/4/18     4:11           9469 round-4-refinement.md
-a---           2026/4/18     3:46          15890 round-4-review-raw.txt
-a---           2026/4/18     3:48           3796 round-4-review.md
-a---           2026/4/18     4:13          18524 round-5-review-raw.txt
-a---           2026/4/18     4:14           4319 round-5-review.md
-a---           2026/4/18     4:14           1281 score-history.md


 succeeded in 903ms:
 succeeded in 900ms:
D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-18_SCPR_v1\FINAL_PROPOSAL.md

codex
The review artifacts are here, but not the repo checkout itself. I’m searching upward for the actual `FDSE_CVPR25` tree so I can verify the surrounding code instead of judging the patch in isolation.
ERROR: Reconnecting... 1/5
codex
The SCPR proposal files are here, but the code checkout isn’t in this folder. I’m searching the broader workspace for `FDSE_CVPR25/algorithm/feddsa_scheduled.py` so I can validate the diff against the live surrounding code instead of reviewing the patch in isolation.
ERROR: Reconnecting... 2/5
codex
The current folder only has the design artifacts, not the repo. I’m expanding the search to parent directories so I can line up the pasted diff with the real implementation, especially the existing `sm` and `sas` paths.
ERROR: Reconnecting... 3/5
codex
The current workspace only contains the review artifacts, not the repo checkout. I’m searching upward for the real `FDSE_CVPR25` tree so I can validate the diff against the surrounding implementation instead of reviewing it in isolation.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-ChildItem -Path 'D:\\桌面文件\\联邦学习' -Recurse -Directory -Filter 'FDSE_CVPR25' -ErrorAction SilentlyContinue | Select-Object -ExpandProperty FullName" in D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-18_SCPR_v1
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-ChildItem -Path 'D:\\桌面文件\\联邦学习' -Recurse -Filter 'feddsa_scheduled.py' -ErrorAction SilentlyContinue | Select-Object -ExpandProperty FullName" in D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-18_SCPR_v1
 succeeded in 1117ms:
D:\桌面文件\联邦学习\.claude\worktrees\dreamy-mcnulty-8d2c06\FDSE_CVPR25\algorithm\feddsa_scheduled.py
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py

 succeeded in 1237ms:
D:\桌面文件\联邦学习\FDSE_CVPR25
D:\桌面文件\联邦学习\.claude\worktrees\dreamy-mcnulty-8d2c06\FDSE_CVPR25

codex
I found the real repo checkout at `D:\桌面文件\联邦学习\FDSE_CVPR25`. Next I’m reading the current server/client implementation around dispatch, reply, loss construction, and the existing `sm`/`sas` logic to check whether this diff fits the codebase correctly.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'D:\\桌面文件\\联邦学习\\FDSE_CVPR25\\algorithm\\feddsa_scheduled.py' | Select-Object -Skip 260 -First 260" in D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-18_SCPR_v1
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'D:\\桌面文件\\联邦学习\\FDSE_CVPR25\\algorithm\\feddsa_scheduled.py' | Select-Object -Skip 520 -First 360" in D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-18_SCPR_v1
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'D:\\桌面文件\\联邦学习\\FDSE_CVPR25\\algorithm\\feddsa_scheduled.py' | Select-Object -First 260" in D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-18_SCPR_v1
 succeeded in 1982ms:
"""
FedDSA-Scheduled: Controlled auxiliary loss scheduling for FedDSA.

Based on feddsa.py with key changes:
  1. L_orth always at full weight from R0 (no ramp) — properly tests decoupling
  2. Configurable schedule for InfoNCE + style augmentation:
     - mode 0: orth_only (no aug, no InfoNCE — pure decouple test)
     - mode 1: bell-curve (InfoNCE peaks at bell_peak then decays)
     - mode 2: cutoff (InfoNCE active until cutoff_round, then off)
     - mode 3: always-on (original behavior but with orth from R0)
  3. Gradient conflict diagnostic logging (every grad_log_interval rounds)

algo_para order:
  lo=lambda_orth, lh=lambda_hsic, ls=lambda_sem, tau,
  sdn=style_dispatch_num, pd=proj_dim,
  sm=schedule_mode, bp=bell_peak, bw=bell_width, cr=cutoff_round,
  gli=grad_log_interval
"""
import os
import copy
import math
import collections
import numpy as np
import torch
import torch.nn as nn
import torch.nn.functional as F
import flgo.algorithm.fedbase
import flgo.utils.fmodule as fuf
from collections import OrderedDict


# ============================================================
# Model: AlexNet backbone + dual-head (same as feddsa.py)
# ============================================================

class AlexNetEncoder(nn.Module):
    """Same AlexNet as config.py but without the final classifier fc3."""
    def __init__(self):
        super().__init__()
        self.features = nn.Sequential(OrderedDict([
            ('conv1', nn.Conv2d(3, 64, kernel_size=11, stride=4, padding=2)),
            ('bn1', nn.BatchNorm2d(64)),
            ('relu1', nn.ReLU(inplace=True)),
            ('maxpool1', nn.MaxPool2d(kernel_size=3, stride=2)),
            ('conv2', nn.Conv2d(64, 192, kernel_size=5, padding=2)),
            ('bn2', nn.BatchNorm2d(192)),
            ('relu2', nn.ReLU(inplace=True)),
            ('maxpool2', nn.MaxPool2d(kernel_size=3, stride=2)),
            ('conv3', nn.Conv2d(192, 384, kernel_size=3, padding=1)),
            ('bn3', nn.BatchNorm2d(384)),
            ('relu3', nn.ReLU(inplace=True)),
            ('conv4', nn.Conv2d(384, 256, kernel_size=3, padding=1)),
            ('bn4', nn.BatchNorm2d(256)),
            ('relu4', nn.ReLU(inplace=True)),
            ('conv5', nn.Conv2d(256, 256, kernel_size=3, padding=1)),
            ('bn5', nn.BatchNorm2d(256)),
            ('relu5', nn.ReLU(inplace=True)),
            ('maxpool5', nn.MaxPool2d(kernel_size=3, stride=2)),
        ]))
        self.avgpool = nn.AdaptiveAvgPool2d((6, 6))
        self.fc1 = nn.Linear(256 * 6 * 6, 1024)
        self.bn6 = nn.BatchNorm1d(1024)
        self.relu = nn.ReLU()
        self.fc2 = nn.Linear(1024, 1024)
        self.bn7 = nn.BatchNorm1d(1024)

    def forward(self, x):
        x = self.features(x)
        x = self.avgpool(x)
        x = torch.flatten(x, 1)
        x = self.bn6(self.fc1(x))
        x = self.relu(x)
        x = self.bn7(self.fc2(x))
        x = self.relu(x)
        return x  # [B, 1024]


class FedDSAModel(fuf.FModule):
    def __init__(self, num_classes=7, feat_dim=1024, proj_dim=128):
        super().__init__()
        self.encoder = AlexNetEncoder()
        self.semantic_head = nn.Sequential(
            nn.Linear(feat_dim, proj_dim), nn.ReLU(), nn.Linear(proj_dim, proj_dim)
        )
        self.style_head = nn.Sequential(
            nn.Linear(feat_dim, proj_dim), nn.ReLU(), nn.Linear(proj_dim, proj_dim)
        )
        self.head = nn.Linear(proj_dim, num_classes)

    def forward(self, x):
        h = self.encoder(x)
        z_sem = self.semantic_head(h)
        return self.head(z_sem)

    def encode(self, x):
        return self.encoder(x)

    def get_semantic(self, h):
        return self.semantic_head(h)

    def get_style(self, h):
        return self.style_head(h)


# ============================================================
# Server
# ============================================================

class Server(flgo.algorithm.fedbase.BasicServer):
    def initialize(self):
        self.init_algo_para({
            'lo': 1.0,    # lambda_orth
            'lh': 0.0,    # lambda_hsic
            'ls': 1.0,    # lambda_sem (base weight, multiplied by schedule)
            'tau': 0.2,   # InfoNCE temperature
            'sdn': 5,     # style_dispatch_num
            'pd': 128,    # proj_dim
            'sm': 0,      # schedule_mode: 0=orth_only, 1=bell, 2=cutoff, 3=always_on,
                          #   4=mse_anchor, 5=alpha_sparsity, 6=mse+alpha, 7=detach_aug
            'bp': 60,     # bell_peak (round where bell peaks)
            'bw': 30,     # bell_width (gaussian sigma)
            'cr': 80,     # cutoff_round (for mode 2)
            'gli': 10,    # grad_log_interval (0=off)
            'lm': 1.0,    # lambda_mse (MSE anchor weight, modes 4/6)
            'al': 0.25,   # alpha_sparsity (power for cos_sim, modes 5/6)
            'se': 0,      # save_errors: 0=off, 1=save client checkpoints on last round
            'sas': 0,     # style-aware aggregation scope:
                          #   0 = off (baseline B0)
                          #   1 = sem_head only, classifier FedAvg (Plan A / B1 / EXP-084)
                          #   2 = sem_head + classifier, both style-conditioned (A2 / sas-FH, OURS)
                          #   3 = sem_head sas + classifier uniform-avg (C2 counterfactual)
                          #   4 = sem_head sas + classifier fully local (C1 counterfactual)
            'sas_tau': 0.3,  # softmax temperature for style similarity
            # -------- SCPR (Self-Masked Style-Weighted Multi-Positive InfoNCE) --------
            # NEW (2026-04-19): refined via GPT-5.4 x5-round refine session
            # (obsidian_exprtiment_results/refine_logs/2026-04-18_SCPR_v1/)
            'scpr': 0,    # SCPR mode:
                          #   0 = off (fall back to sm schedule / Plan A, no SCPR loss)
                          #   1 = uniform multi-positive (= M3 / SCPR tau->inf lower bound)
                          #   2 = style-weighted multi-positive (SCPR, OURS)
                          # Always self-masked (j != k) when mode > 0.
                          # Uses domain-indexed class prototypes; NO new trainable params.
            'scpr_tau': 0.3,  # SCPR style attention temperature (inherited from SAS optimal)
        })
        # Readable aliases
        self.lambda_orth = float(self.lo)
        self.lambda_hsic = float(self.lh)
        self.lambda_sem = float(self.ls)
        self.tau = float(self.tau)
        self.style_dispatch_num = int(self.sdn)
        self.proj_dim = int(self.pd)
        self.schedule_mode = int(self.sm)
        self.bell_peak = float(self.bp)
        self.bell_width = float(self.bw)
        self.cutoff_round = int(self.cr)
        self.grad_log_interval = int(self.gli)
        self.lambda_mse = float(self.lm)
        self.alpha_sparsity = float(self.al)
        self.save_errors = int(self.se)
        self.style_aware_sem = int(self.sas)
        self.style_aware_tau = float(self.sas_tau)
        # SCPR aliases
        self.scpr_mode = int(self.scpr)
        self.scpr_tau_val = float(self.scpr_tau)
        self.sample_option = 'full'

        # 方案 A：per-client semantic_head states (最近一轮上传的)
        self.client_sem_states = {}
        # sas-FH (A2/C1/C2)：per-client classifier head states
        self.client_head_states = {}

        # Style bank
        self.style_bank = {}
        # Global semantic prototypes
        self.global_semantic_protos = {}
        # SCPR: per-(client, class) prototypes bank (domain-indexed)
        #   key: client_id -> value: dict {class_idx: proto_tensor (cpu)}
        self.client_class_protos = {}
        # Gradient conflict log
        self.grad_conflict_log = {}
        # Best checkpoint tracking (for save_errors)
        self._best_avg_acc = -1.0
        self._best_round = 0
        self._best_global_state = None
        self._best_client_states = None
        # Same-round FedAvg head snapshot at best round (for Claim 2 swap diagnostic)
        self._best_fedavg_head_state = None

        self._init_agg_keys()

        # Pass config to clients
        for c in self.clients:
            c.lambda_orth = self.lambda_orth
            c.lambda_hsic = self.lambda_hsic
            c.lambda_sem = self.lambda_sem
            c.tau = self.tau
            c.proj_dim = self.proj_dim
            c.schedule_mode = self.schedule_mode
            c.bell_peak = self.bell_peak
            c.bell_width = self.bell_width
            c.cutoff_round = self.cutoff_round
            c.grad_log_interval = self.grad_log_interval
            c.lambda_mse = self.lambda_mse
            c.alpha_sparsity = self.alpha_sparsity
            # SCPR params to client
            c.scpr_mode = self.scpr_mode
            c.scpr_tau_val = self.scpr_tau_val

    def _init_agg_keys(self):
        """Classify model keys into: shared (FedAvg), private (style_head+BN)."""
        all_keys = list(self.model.state_dict().keys())
        self.private_keys = set()
        for k in all_keys:
            if 'style_head' in k:
                self.private_keys.add(k)
            elif 'bn' in k.lower() and ('running_' in k or 'num_batches_tracked' in k):
                self.private_keys.add(k)
        self.shared_keys = [k for k in all_keys if k not in self.private_keys]

    def pack(self, client_id, mtype=0):
        """Send global model + protos + styles.

        sas 聚合策略（分两个部分分别处理）：
          sem_head:
            - sas in {1,2,3,4}: style-conditioned 个性化 (同 Plan A)
            - sas == 0: FedAvg (不做个性化)
          classifier head:
            - sas == 2 (A2): style-conditioned 个性化（OURS, sas-FH）
            - sas == 3 (C2): uniform-avg 聚合（均匀加权，不用 style）
            - sas == 4 (C1): local-only（用 client 自己最近一轮上传的 head）
            - sas in {0,1}: FedAvg (Plan A 行为)
        """
        dispatched_styles = None
        if self.schedule_mode != 0 and len(self.style_bank) > 0:
            available = {cid: s for cid, s in self.style_bank.items() if cid != client_id}
            if len(available) == 0:
                available = self.style_bank
            n = min(self.style_dispatch_num, len(available))
            if n > 0:
                keys = list(available.keys())
                chosen = np.random.choice(keys, n, replace=False)
                dispatched_styles = [available[k] for k in chosen]

        model_to_send = copy.deepcopy(self.model)
        model_state = model_to_send.state_dict()
        overridden = False

        # --- 1) sem_head 部分 ---
        if self.style_aware_sem in (1, 2, 3, 4) \
                and len(self.client_sem_states) >= 2 \
                and client_id in self.style_bank:
            personalized_sem = self._compute_style_weighted(
                self.client_sem_states, client_id
            )
            if personalized_sem is not None:
                for k, v in personalized_sem.items():
                    if k in model_state:
                        model_state[k] = v.to(model_state[k].device)
                overridden = True


 succeeded in 1978ms:
        # --- 2) classifier head 部分 ---
        if self.style_aware_sem == 2 \
                and len(self.client_head_states) >= 2 \
                and client_id in self.style_bank:
            # A2 (sas-FH): style-conditioned personalized head
            personalized_head = self._compute_style_weighted(
                self.client_head_states, client_id
            )
            if personalized_head is not None:
                for k, v in personalized_head.items():
                    if k in model_state:
                        model_state[k] = v.to(model_state[k].device)
                overridden = True
        elif self.style_aware_sem == 3 and len(self.client_head_states) >= 2:
            # C2 counterfactual: uniform-avg head (每个 client 收到同一份均值 head)
            uniform_head = self._compute_uniform_avg(self.client_head_states)
            if uniform_head is not None:
                for k, v in uniform_head.items():
                    if k in model_state:
                        model_state[k] = v.to(model_state[k].device)
                overridden = True
        elif self.style_aware_sem == 4 and client_id in self.client_head_states:
            # C1 counterfactual: local-only head (client 拿自己上轮上传的 head)
            local_head = self.client_head_states[client_id]
            for k, v in local_head.items():
                if k in model_state:
                    model_state[k] = v.to(model_state[k].device)
            overridden = True

        if overridden:
            model_to_send.load_state_dict(model_state)

        # SCPR: compute self-masked style-weighted (or uniform) prototype payload
        # Returns None when scpr=0 OR bank insufficient; client falls back to non-SCPR path.
        scpr_payload = None
        if self.scpr_mode > 0 and len(self.client_class_protos) >= 2:
            scpr_payload = self._compute_scpr_payload(client_id)

        return {
            'model': model_to_send,
            'global_protos': copy.deepcopy(self.global_semantic_protos),
            'style_bank': dispatched_styles,
            'current_round': self.current_round,
            'scpr_payload': scpr_payload,
        }

    @staticmethod
    def _extract_style_vec(style):
        """从 style_bank 的 entry (tuple/dict/tensor) 提取展平的 style 向量。"""
        if isinstance(style, (tuple, list)):
            return style[0].flatten().cpu()
        elif isinstance(style, dict):
            v = style.get('mu', next(iter(style.values())))
            return v.flatten().cpu()
        else:
            return style.flatten().cpu()

    def _compute_style_weighted(self, client_states, target_cid):
        """通用版本：对 target client 计算其他 client 的 style-similarity 加权 state。

        Args:
            client_states: dict[cid -> state_dict]，比如 self.client_sem_states 或
                           self.client_head_states。所有 value 必须有相同的 key 集合。
            target_cid: 目标 client id。
        Returns:
            aggregated state dict (同 client_states[cid] 的结构)，或 None（无法聚合时）。
        """
        import torch as _t
        target_style = self.style_bank.get(target_cid)
        if target_style is None:
            return None
        target_vec = self._extract_style_vec(target_style)

        sims = []
        cids = []
        for cid in client_states:
            src_style = self.style_bank.get(cid)
            if src_style is None:
                continue
            src_vec = self._extract_style_vec(src_style)
            if src_vec.shape != target_vec.shape:
                continue
            dot = (src_vec * target_vec).sum()
            norm = src_vec.norm() * target_vec.norm() + 1e-8
            sims.append((dot / norm).item())
            cids.append(cid)

        if len(sims) == 0:
            return None

        sims_t = _t.tensor(sims, dtype=_t.float32) / max(self.style_aware_tau, 1e-3)
        weights = _t.softmax(sims_t, dim=0)

        accumulated = None
        for w, cid in zip(weights.tolist(), cids):
            st = client_states[cid]
            if accumulated is None:
                accumulated = {k: v.clone() * w for k, v in st.items()}
            else:
                for k in accumulated:
                    accumulated[k] += st[k] * w
        return accumulated

    def _compute_uniform_avg(self, client_states):
        """C2 counterfactual: 对所有 client state 做均匀平均（uniform mean），
        不使用 style 信息。每个 target client 都会收到同一份结果。

        Args:
            client_states: dict[cid -> state_dict]
        Returns:
            averaged state dict，或 None（空输入时）。
        """
        if len(client_states) == 0:
            return None
        cids = list(client_states.keys())
        n = float(len(cids))
        accumulated = None
        for cid in cids:
            st = client_states[cid]
            if accumulated is None:
                accumulated = {k: v.clone() / n for k, v in st.items()}
            else:
                for k in accumulated:
                    accumulated[k] += st[k] / n
        return accumulated

    def _compute_scpr_payload(self, target_cid):
        """Compute SCPR payload for target client: self-masked style-weighted
        (or uniform) prototype dispatch.

        scpr_mode=1 (uniform multi-positive, = M3 lower bound):
            w_{k->j}^{uniform} = 1 / (K-1) for all j != k that have proto(s)
        scpr_mode=2 (style-weighted, OURS):
            w_{k->j} = softmax_j( cos(s_k, s_j) / tau_SCPR ) * 1[j != k]

        Returns:
            None if insufficient bank or target style missing.
            Otherwise dict with two keys:
              'weights': {source_cid: float}  (self-masked, sums to 1 over source_cids)
              'protos':  {source_cid: {class_idx: proto_tensor_cpu}}  (raw per-(class, client) bank,
                         client will re-normalize per-class based on availability)
        """
        import torch as _t
        target_style = self.style_bank.get(target_cid)
        if target_style is None:
            return None
        target_vec = self._extract_style_vec(target_style)

        # Collect candidates (self-masked: j != target_cid)
        source_cids = []
        source_vecs = []
        source_protos = {}
        for cid, protos in self.client_class_protos.items():
            if cid == target_cid:  # SELF-MASK (R1 reviewer fix)
                continue
            src_style = self.style_bank.get(cid)
            if src_style is None:
                continue
            src_vec = self._extract_style_vec(src_style)
            if src_vec.shape != target_vec.shape:
                continue
            if protos is None or len(protos) == 0:
                continue
            source_cids.append(cid)
            source_vecs.append(src_vec)
            source_protos[cid] = protos

        if len(source_cids) == 0:
            return None

        # Attention weights over source clients (self-mask already applied above)
        if self.scpr_mode == 1:
            # Uniform (mathematically equivalent to SCPR tau -> infinity; = M3 lower bound)
            uniform_w = 1.0 / len(source_cids)
            weights = {cid: uniform_w for cid in source_cids}
        elif self.scpr_mode == 2:
            # Style-weighted softmax (SCPR main method)
            sims = []
            for src_vec in source_vecs:
                dot = (src_vec * target_vec).sum()
                norm = src_vec.norm() * target_vec.norm() + 1e-8
                sims.append((dot / norm).item())
            sims_t = _t.tensor(sims, dtype=_t.float32) / max(self.scpr_tau_val, 1e-3)
            w_vec = _t.softmax(sims_t, dim=0).tolist()
            weights = {cid: float(w) for cid, w in zip(source_cids, w_vec)}
        else:
            return None

        return {'weights': weights, 'protos': source_protos}

    def iterate(self):
        self.selected_clients = self.sample()
        res = self.communicate(self.selected_clients)

        models = res['model']
        protos_list = res['protos']
        proto_counts_list = res['proto_counts']
        style_stats_list = res['style_stats']
        grad_conflict_list = res['grad_conflict']

        # Aggregate shared parameters (FedAvg)
        self._aggregate_shared(models)

        # sas：记录每个 client 上传的 sem_head / head state（供 pack 时做聚合）
        if self.style_aware_sem in (1, 2, 3, 4):
            for cid, m in zip(self.received_clients, models):
                m_state = m.state_dict()
                sem_state = {k: v.clone().cpu() for k, v in m_state.items()
                             if 'semantic_head' in k}
                self.client_sem_states[cid] = sem_state
                # sas-FH (A2/C1/C2) 需要额外记录 classifier head
                if self.style_aware_sem in (2, 3, 4):
                    head_state = {k: v.clone().cpu() for k, v in m_state.items()
                                  if k.startswith('head.')}
                    if head_state:
                        self.client_head_states[cid] = head_state

        # Collect style bank
        for cid, style in zip(self.received_clients, style_stats_list):
            if style is not None:
                self.style_bank[cid] = style

        # SCPR: update per-(client, class) prototype bank (domain-indexed)
        if self.scpr_mode > 0:
            for cid, protos in zip(self.received_clients, protos_list):
                if protos is not None and len(protos) > 0:
                    self.client_class_protos[cid] = {
                        c: p.detach().clone().cpu() for c, p in protos.items()
                    }

        # Aggregate global protos (weighted mean)
        self._aggregate_protos(protos_list, proto_counts_list)

        # Log gradient conflict
        for cid, gc in zip(self.received_clients, grad_conflict_list):
            if gc is not None:
                if self.current_round not in self.grad_conflict_log:
                    self.grad_conflict_log[self.current_round] = {}
                self.grad_conflict_log[self.current_round][cid] = gc
        if self.current_round in self.grad_conflict_log:
            entries = self.grad_conflict_log[self.current_round]
            vals = list(entries.values())
            mean_cos = sum(vals) / len(vals)
            self.gv.logger.info(
                f"[GradConflict] round={self.current_round} "
                f"mean_cos={mean_cos:.4f} per_client={entries}"
            )

        # 追踪 best（每轮）+ 最后一轮保存 best checkpoint
        if int(getattr(self, 'save_errors', 0)) == 1:
            self._track_best()
            if self.current_round >= self.num_rounds:
                self._save_best_checkpoints()

    def _track_best(self):
        """每轮聚合后缓存 best AVG acc 对应的模型快照。
        读取 flgo logger.output 拿最新 test metrics（上一轮的结果）。"""
        try:
            output = getattr(self.gv.logger, 'output', None)
            if not output or 'mean_local_test_accuracy' not in output:

 succeeded in 2052ms:
                return
            history = output['mean_local_test_accuracy']
            if not history:
                return
            current = float(history[-1]) * 100
            if current > self._best_avg_acc:
                import copy
                self._best_avg_acc = current
                self._best_round = len(history)  # round index (1-based)
                self._best_global_state = copy.deepcopy(self.model.state_dict())
                self._best_client_states = [
                    copy.deepcopy(c.model.state_dict()) if getattr(c, 'model', None) is not None else None
                    for c in self.clients
                ]
                # sas-FH Claim 2：保存同轮 FedAvg head 快照（供 swap diagnostic 用）
                # 仅 A2 模式下有意义：此时 server.model 里 head 已是 FedAvg 版本，
                # 但下发给 client 的 head 是 style-conditioned personalized，
                # 两者有差异 → 需要保留这份 "same-round global_head"。
                self._best_fedavg_head_state = {
                    k: v.clone().cpu() for k, v in self.model.state_dict().items()
                    if k.startswith('head.')
                }
        except Exception as e:
            try:
                self.gv.logger.info(f"[TrackBest] skip: {e}")
            except Exception:
                pass

    def _save_best_checkpoints(self):
        """训练结束时把 best round 的模型快照写到 ~/fl_checkpoints/"""
        import os, torch, time, json
        if self._best_global_state is None:
            # 兜底：best 未追踪到（比如 test 没跑过），就存最后一轮
            self._best_global_state = self.model.state_dict()
            self._best_client_states = [
                c.model.state_dict() if getattr(c, 'model', None) is not None else None
                for c in self.clients
            ]
            self._best_round = self.current_round
            # 同时保一份最后一轮的 FedAvg head 作为 fallback snapshot
            if self._best_fedavg_head_state is None:
                self._best_fedavg_head_state = {
                    k: v.clone().cpu() for k, v in self.model.state_dict().items()
                    if k.startswith('head.')
                }
            fallback = True
        else:
            fallback = False
        seed = getattr(self, 'option', {}).get('seed', 'unknown')
        tag = f"feddsa_s{seed}_R{self.num_rounds}_best{self._best_round}_{int(time.time())}"
        save_dir = os.path.expanduser(f'~/fl_checkpoints/{tag}')
        os.makedirs(save_dir, exist_ok=True)
        torch.save(self._best_global_state, os.path.join(save_dir, 'global_model.pt'))
        saved = 0
        for cid, state in enumerate(self._best_client_states):
            if state is not None:
                torch.save(state, os.path.join(save_dir, f'client_{cid}.pt'))
                saved += 1
        # 同轮 FedAvg head 快照（Claim 2 swap diagnostic）
        if self._best_fedavg_head_state is not None:
            torch.save(self._best_fedavg_head_state,
                       os.path.join(save_dir, 'fedavg_head.pt'))
        meta = {
            'best_round': self._best_round,
            'best_avg_acc': self._best_avg_acc,
            'num_rounds': self.num_rounds,
            'num_clients': len(self.clients),
            'schedule_mode': int(self.schedule_mode),
            'lambda_orth': float(self.lambda_orth),
            'tau': float(self.tau),
            'sas': int(self.style_aware_sem),
            'sas_tau': float(self.style_aware_tau),
            'seed': seed,
            'has_fedavg_head': self._best_fedavg_head_state is not None,
            'is_last_round_fallback': fallback,
        }
        with open(os.path.join(save_dir, 'meta.json'), 'w') as f:
            json.dump(meta, f, indent=2)
        self.gv.logger.info(
            f"[SaveBest] best@R{self._best_round} avg={self._best_avg_acc:.4f} "
            f"global+{saved} clients -> {save_dir} (fallback={fallback})"
        )

    def _aggregate_shared(self, models):
        if len(models) == 0:
            return
        weights = np.array(
            [len(self.clients[cid].train_data) for cid in self.received_clients],
            dtype=float,
        )
        weights /= weights.sum()

        global_dict = self.model.state_dict()
        model_dicts = [m.state_dict() for m in models]

        for key in self.shared_keys:
            global_dict[key] = sum(w * md[key] for w, md in zip(weights, model_dicts))
        self.model.load_state_dict(global_dict)

    def _aggregate_protos(self, protos_list, proto_counts_list):
        agg_sum = {}
        agg_cnt = {}
        for protos, counts in zip(protos_list, proto_counts_list):
            if protos is None:
                continue
            for c, p in protos.items():
                n = counts.get(c, 1)
                if c not in agg_sum:
                    agg_sum[c] = p * n
                    agg_cnt[c] = n
                else:
                    agg_sum[c] += p * n
                    agg_cnt[c] += n
        self.global_semantic_protos = {
            c: agg_sum[c] / agg_cnt[c] for c in agg_sum
        }


# ============================================================
# Client
# ============================================================

class Client(flgo.algorithm.fedbase.BasicClient):
    def initialize(self):
        self.model = None
        self.loss_fn = nn.CrossEntropyLoss()
        self.global_protos = None
        self.local_style_bank = None
        self.current_round = 0
        self._grad_conflict_log = None
        # SCPR state (populated by Server.initialize via `c.scpr_mode=...`)
        self.scpr_mode = 0
        self.scpr_tau_val = 0.3
        self.scpr_payload = None

    def reply(self, svr_pkg):
        model, global_protos, style_bank, current_round = self.unpack(svr_pkg)
        self.current_round = current_round
        self.global_protos = global_protos
        self.local_style_bank = style_bank
        # SCPR: extract per-round payload (self-masked weights + domain protos)
        self.scpr_payload = svr_pkg.get('scpr_payload', None)
        self.train(model)
        return self.pack()

    def unpack(self, svr_pkg):
        global_model = svr_pkg['model']
        if self.model is None:
            self.model = global_model
        else:
            local_dict = self.model.state_dict()
            global_dict = global_model.state_dict()
            for key in global_dict:
                if 'style_head' not in key and not (
                    'bn' in key.lower() and ('running_' in key or 'num_batches_tracked' in key)
                ):
                    local_dict[key] = global_dict[key]
            self.model.load_state_dict(local_dict)
        return (
            self.model,
            svr_pkg['global_protos'],
            svr_pkg['style_bank'],
            svr_pkg['current_round'],
        )

    def pack(self):
        return {
            'model': copy.deepcopy(self.model.to('cpu')),
            'protos': self._local_protos,
            'proto_counts': self._local_proto_counts,
            'style_stats': self._local_style_stats,
            'grad_conflict': self._grad_conflict_log,
        }

    # ----------------------------------------------------------------
    # Schedule functions
    # ----------------------------------------------------------------

    @staticmethod
    def _bell_weight(t, t_peak, width):
        """Gaussian bell: peaks at t_peak, decays both sides."""
        if width <= 0:
            return 1.0 if t == t_peak else 0.0
        return math.exp(-0.5 * ((t - t_peak) / width) ** 2)

    def _get_aux_weight(self):
        """Compute the auxiliary loss weight based on schedule_mode.

        Returns:
            w_aux: weight for InfoNCE and style augmentation (0 to 1).
                   L_orth is ALWAYS full weight (not affected by this).
        """
        t = self.current_round
        mode = self.schedule_mode

        if mode == 0:
            # orth_only: no InfoNCE, no augmentation
            return 0.0
        elif mode == 1:
            # bell-curve: peaks at bell_peak, decays with bell_width
            return self._bell_weight(t, self.bell_peak, self.bell_width)
        elif mode == 2:
            # cutoff: full weight until cutoff_round, then off
            return 1.0 if t <= self.cutoff_round else 0.0
        elif mode in (3, 4, 5, 6, 7):
            # always-on variants: full aux weight, loss type varies in train()
            return 1.0
        else:
            return 0.0

    # ----------------------------------------------------------------
    # Training
    # ----------------------------------------------------------------

    @fuf.with_multi_gpus
    def train(self, model, *args, **kwargs):
        model.train()
        optimizer = self.calculator.get_optimizer(
            model, lr=self.learning_rate,
            weight_decay=self.weight_decay, momentum=self.momentum
        )

        # Compute schedule weight for this round
        w_aux = self._get_aux_weight()

        # Gradient conflict logging
        should_log_grad = (
            self.grad_log_interval > 0
            and self.current_round % self.grad_log_interval == 0
        )
        self._grad_conflict_log = None

        # Online accumulators
        proto_sum = {}
        proto_count = {}
        style_sum = None
        style_sq_sum = None
        style_n = 0

        num_steps = self.num_steps
        steps_per_epoch = max(1, len(self.train_data) // self.batch_size)

        for step in range(num_steps):
            batch_data = self.get_batch_data()
            batch_data = self.calculator.to_device(batch_data)
            x, y = batch_data[0], batch_data[-1]

            model.zero_grad()

            # Forward
            h = model.encode(x)
            z_sem = model.get_semantic(h)
            z_sty = model.get_style(h)

            # Loss 1: Task CE (always active)
            output = model.head(z_sem)
            loss_task = self.loss_fn(output, y)

            # Loss 2: Style-augmented CE (controlled by w_aux)
            # Mode 7: augmented features go to contrastive ONLY, not CE (PARDON insight)
            loss_aug = torch.tensor(0.0, device=x.device)
            z_sem_aug = None
            has_aug = (w_aux > 1e-4
                       and self.local_style_bank is not None
                       and len(self.local_style_bank) > 0)
            if has_aug:
                h_aug = self._style_augment(h)
                z_sem_aug = model.get_semantic(h_aug)
                if self.schedule_mode != 7:
                    # Normal modes: augmented features go through CE
                    output_aug = model.head(z_sem_aug)
                    loss_aug = self.loss_fn(output_aug, y)
                # Mode 7: z_sem_aug computed but NOT sent through CE

            # Loss 3: Decoupling — ALWAYS full weight from R0
            loss_orth, loss_hsic = self._decouple_loss(z_sem, z_sty)

            # Loss 4: Alignment (controlled by w_aux, type depends on mode)
            loss_sem = torch.tensor(0.0, device=x.device)
            loss_mse_anchor = torch.tensor(0.0, device=x.device)
            use_alpha = self.schedule_mode in (5, 6, 7)
            use_mse = self.schedule_mode in (4, 6, 7)
            if w_aux > 1e-4 and self.global_protos and len(self.global_protos) >= 2:
                # Choose InfoNCE variant
                # For mode 7: use z_sem + z_sem_aug concatenated for richer contrastive
                z_for_contrast = z_sem
                y_for_contrast = y
                if self.schedule_mode == 7 and z_sem_aug is not None:
                    z_for_contrast = torch.cat([z_sem, z_sem_aug], dim=0)
                    y_for_contrast = torch.cat([y, y], dim=0)

                if use_alpha:
                    loss_sem = self._infonce_alpha_loss(z_for_contrast, y_for_contrast)
                else:
                    loss_sem = self._infonce_loss(z_for_contrast, y_for_contrast)

                if use_mse:
                    loss_mse_anchor = self._mse_anchor_loss(z_sem, y)

            # Total loss:
            # L_orth: ALWAYS full weight (key change from original)
            # L_aug + L_InfoNCE + L_MSE: controlled by w_aux schedule
            loss = (
                loss_task
                + w_aux * loss_aug
                + self.lambda_orth * loss_orth
                + self.lambda_hsic * loss_hsic
                + w_aux * self.lambda_sem * loss_sem
                + w_aux * self.lambda_mse * loss_mse_anchor
            )

            # SCPR loss: self-masked (style-)weighted multi-positive InfoNCE.
            # Independent from sm/w_aux schedule (works even when Plan A sm=0).
            # scpr_mode=0 → skip entirely; =1 → uniform multi-pos (M3 bound); =2 → style-weighted.
            if self.scpr_mode > 0 and self.scpr_payload is not None:
                loss_scpr = self._scpr_loss(z_sem, y)
                loss = loss + self.lambda_sem * loss_scpr

            # Gradient conflict logging (last batch of last epoch)
            is_last_batch = (step == num_steps - 1)
            if should_log_grad and is_last_batch and loss_sem.item() > 0:
                self._log_grad_conflict(
                    model, loss_task, w_aux * self.lambda_sem * loss_sem
                )

            loss.backward()
            if self.clip_grad > 0:
                torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=self.clip_grad)
            optimizer.step()

            # Accumulate prototypes + style stats (last epoch only)
            if step >= num_steps - steps_per_epoch - 1:
                with torch.no_grad():
                    z_det = z_sem.detach().cpu()
                    h_det = h.detach()

                    for i, label in enumerate(y):
                        c = label.item()
                        if c not in proto_sum:
                            proto_sum[c] = z_det[i].clone()
                            proto_count[c] = 1
                        else:
                            proto_sum[c] += z_det[i]
                            proto_count[c] += 1

                    b = h_det.size(0)
                    batch_mu = h_det.mean(dim=0).cpu()
                    batch_sq = (h_det ** 2).mean(dim=0).cpu()
                    if style_sum is None:
                        style_sum = batch_mu * b
                        style_sq_sum = batch_sq * b
                        style_n = b
                    else:
                        style_sum += batch_mu * b
                        style_sq_sum += batch_sq * b
                        style_n += b

        # Store for pack()
        self._local_protos = {c: proto_sum[c] / proto_count[c] for c in proto_sum}
        self._local_proto_counts = proto_count

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'D:\\桌面文件\\联邦学习\\FDSE_CVPR25\\algorithm\\feddsa_scheduled.py' | Select-Object -Skip 880 -First 220" in D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-18_SCPR_v1
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$i=1; Get-Content -Path '"'D:\\桌面文件\\联邦学习\\FDSE_CVPR25\\algorithm\\feddsa_scheduled.py' | ForEach-Object { '{0,5}: {1}' -f "'$i, $_; $i++ } | Select-Object -Skip 900 -First 220' in D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-18_SCPR_v1
 succeeded in 860ms:

        if style_n > 1:
            mu = style_sum / style_n
            var = style_sq_sum / style_n - mu ** 2
            self._local_style_stats = (mu, var.clamp(min=1e-6).sqrt())
        else:
            self._local_style_stats = None

    # ----------------------------------------------------------------
    # Gradient Conflict Logger
    # ----------------------------------------------------------------

    def _log_grad_conflict(self, model, loss_task, loss_align_weighted):
        """Compute cosine similarity between CE and InfoNCE gradients on encoder."""
        encoder_params = [p for p in model.encoder.parameters() if p.requires_grad]
        if not encoder_params:
            return
        try:
            grad_ce = torch.autograd.grad(
                loss_task, encoder_params,
                retain_graph=True, allow_unused=True, create_graph=False
            )
            grad_align = torch.autograd.grad(
                loss_align_weighted, encoder_params,
                retain_graph=True, allow_unused=True, create_graph=False
            )
        except RuntimeError:
            return

        flat_ce = []
        flat_align = []
        for g_ce, g_al in zip(grad_ce, grad_align):
            if g_ce is not None and g_al is not None:
                flat_ce.append(g_ce.flatten())
                flat_align.append(g_al.flatten())
        if not flat_ce:
            return

        flat_ce = torch.cat(flat_ce)
        flat_align = torch.cat(flat_align)
        cos_sim = F.cosine_similarity(flat_ce.unsqueeze(0), flat_align.unsqueeze(0)).item()
        self._grad_conflict_log = cos_sim

    # ----------------------------------------------------------------
    # Helper functions (same as feddsa.py)
    # ----------------------------------------------------------------

    def _style_augment(self, h):
        """AdaIN-style augmentation using external style."""
        idx = np.random.randint(0, len(self.local_style_bank))
        mu_ext, sigma_ext = self.local_style_bank[idx]
        mu_ext = mu_ext.to(h.device)
        sigma_ext = sigma_ext.to(h.device)

        mu_local = h.mean(dim=0)
        sigma_local = h.std(dim=0, unbiased=False).clamp(min=1e-6)

        alpha = np.random.beta(0.1, 0.1)
        mu_mix = alpha * mu_local + (1 - alpha) * mu_ext
        sigma_mix = alpha * sigma_local + (1 - alpha) * sigma_ext

        h_norm = (h - mu_local) / sigma_local
        return h_norm * sigma_mix + mu_mix

    def _decouple_loss(self, z_sem, z_sty):
        """Orthogonal + HSIC dual constraint."""
        z_sem_n = F.normalize(z_sem, dim=1)
        z_sty_n = F.normalize(z_sty, dim=1)
        cos = (z_sem_n * z_sty_n).sum(dim=1)
        loss_orth = (cos ** 2).mean()

        loss_hsic = self._hsic(z_sem, z_sty)
        return loss_orth, loss_hsic

    def _hsic(self, x, y):
        n = x.size(0)
        if n < 4:
            return torch.tensor(0.0, device=x.device)
        Kx = self._gaussian_kernel(x)
        Ky = self._gaussian_kernel(y)
        H = torch.eye(n, device=x.device) - torch.ones(n, n, device=x.device) / n
        return torch.trace(Kx @ H @ Ky @ H) / (n * n)

    def _gaussian_kernel(self, x):
        n = x.size(0)
        dist = torch.cdist(x, x, p=2) ** 2
        nonzero = dist[dist > 0]
        if nonzero.numel() == 0:
            return torch.ones(n, n, device=x.device)
        median = torch.median(nonzero)
        bw = median / (2.0 * np.log(n + 1) + 1e-6)
        return torch.exp(-dist / (2.0 * bw.clamp(min=1e-6)))

    def _infonce_loss(self, z_sem, y):
        """InfoNCE: pull toward same-class global proto, push away others."""
        available = sorted([c for c, p in self.global_protos.items() if p is not None])
        if len(available) < 2:
            return torch.tensor(0.0, device=z_sem.device)

        proto_matrix = torch.stack([self.global_protos[c].to(z_sem.device) for c in available])
        class_to_idx = {c: i for i, c in enumerate(available)}

        z_n = F.normalize(z_sem, dim=1)
        p_n = F.normalize(proto_matrix, dim=1)
        logits = (z_n @ p_n.T) / self.tau

        targets = []
        valid = []
        for i in range(y.size(0)):
            label = y[i].item()
            if label in class_to_idx:
                targets.append(class_to_idx[label])
                valid.append(i)

        if len(valid) == 0:
            return torch.tensor(0.0, device=z_sem.device)

        valid_t = torch.tensor(valid, device=z_sem.device)
        targets_t = torch.tensor(targets, device=z_sem.device, dtype=torch.long)
        return F.cross_entropy(logits[valid_t], targets_t)

    def _infonce_alpha_loss(self, z_sem, y):
        """Alpha-sparsity InfoNCE (FedPLVM-inspired).
        cos_sim^alpha weakens positive-pair gradients, focuses on hard negatives.
        Requires cos_sim >= 0, so we clamp after normalization."""
        available = sorted([c for c, p in self.global_protos.items() if p is not None])
        if len(available) < 2:
            return torch.tensor(0.0, device=z_sem.device)

        proto_matrix = torch.stack([self.global_protos[c].to(z_sem.device) for c in available])
        class_to_idx = {c: i for i, c in enumerate(available)}

        z_n = F.normalize(z_sem, dim=1)
        p_n = F.normalize(proto_matrix, dim=1)
        cos_sim = (z_n @ p_n.T).clamp(min=0)  # clamp negatives to 0 for pow safety
        logits = cos_sim.pow(self.alpha_sparsity) / self.tau

        targets = []
        valid = []
        for i in range(y.size(0)):
            label = y[i].item()
            if label in class_to_idx:
                targets.append(class_to_idx[label])
                valid.append(i)

        if len(valid) == 0:
            return torch.tensor(0.0, device=z_sem.device)

        valid_t = torch.tensor(valid, device=z_sem.device)
        targets_t = torch.tensor(targets, device=z_sem.device, dtype=torch.long)
        return F.cross_entropy(logits[valid_t], targets_t)

    def _mse_anchor_loss(self, z_sem, y):
        """MSE anchor loss (FPL-inspired): pull z_sem toward own-class global proto.
        Acts as a stable 'gravitational center' preventing feature drift."""
        targets = []
        valid = []
        for i in range(y.size(0)):
            label = y[i].item()
            if label in self.global_protos and self.global_protos[label] is not None:
                targets.append(self.global_protos[label])
                valid.append(i)
        if not valid:
            return torch.tensor(0.0, device=z_sem.device)
        target_matrix = torch.stack(targets).to(z_sem.device).detach()
        valid_t = torch.tensor(valid, device=z_sem.device)
        return F.mse_loss(z_sem[valid_t], target_matrix)

    # ----------------------------------------------------------------
    # SCPR: Self-Masked Style-Weighted Multi-Positive InfoNCE
    # ----------------------------------------------------------------

    def _scpr_loss(self, z_sem, y):
        """Self-masked (style-)weighted multi-positive InfoNCE.

        For client k (self-masked by Server: j != k), given domain-indexed
        protos {p_c^j} and client-pair weights w_{k->j}, and target sample
        (z_i, y_i=c), the loss (per-class renormalized) is:
            L = - sum_{j in A_i^c} (w_{k->j} / Z_i^c) * log [
                  exp(sim(z_i, p_c^j)/tau_nce) /
                  ( exp(sim(z_i, p_c^j)/tau_nce)
                    + sum_{c' != c, l in A_i^{c'}} exp(sim(z_i, p_{c'}^l)/tau_nce) )
                ]
        where A_i^c = {j != k : p_c^j exists} and Z_i^c = sum of weights over A_i^c.

        Under scpr_mode=1 (uniform weights) this reduces to M3 domain-aware
        multi-positive SupCon (PACS EXP-072 +5.09% lower bound). Under
        scpr_mode=2 (style-weighted softmax) it is the full SCPR method.

        All prototypes are detached (soft anchors, no grad back to bank).
        """
        if self.scpr_payload is None:
            return torch.tensor(0.0, device=z_sem.device)
        weights = self.scpr_payload.get('weights', {})
        domain_protos = self.scpr_payload.get('protos', {})
        if not weights or not domain_protos:
            return torch.tensor(0.0, device=z_sem.device)

        device = z_sem.device

        # Flatten into per-entry arrays: each entry = (class, source_cid, proto, weight)
        entry_protos = []
        entry_classes = []
        entry_weights = []
        for cid, protos in domain_protos.items():
            w = float(weights.get(cid, 0.0))
            for c, proto in protos.items():
                entry_protos.append(proto)
                entry_classes.append(int(c))
                entry_weights.append(w)

        if len(entry_protos) < 2:
            return torch.tensor(0.0, device=device)

        proto_matrix = torch.stack([p.to(device) for p in entry_protos]).detach()  # [N, D]
        entry_classes_t = torch.tensor(entry_classes, device=device, dtype=torch.long)  # [N]
        entry_weights_t = torch.tensor(entry_weights, device=device, dtype=torch.float32)  # [N]

        # Cosine-similarity logits with InfoNCE temperature
        z_n = F.normalize(z_sem, dim=1)

 succeeded in 851ms:
  901:                 retain_graph=True, allow_unused=True, create_graph=False
  902:             )
  903:             grad_align = torch.autograd.grad(
  904:                 loss_align_weighted, encoder_params,
  905:                 retain_graph=True, allow_unused=True, create_graph=False
  906:             )
  907:         except RuntimeError:
  908:             return
  909: 
  910:         flat_ce = []
  911:         flat_align = []
  912:         for g_ce, g_al in zip(grad_ce, grad_align):
  913:             if g_ce is not None and g_al is not None:
  914:                 flat_ce.append(g_ce.flatten())
  915:                 flat_align.append(g_al.flatten())
  916:         if not flat_ce:
  917:             return
  918: 
  919:         flat_ce = torch.cat(flat_ce)
  920:         flat_align = torch.cat(flat_align)
  921:         cos_sim = F.cosine_similarity(flat_ce.unsqueeze(0), flat_align.unsqueeze(0)).item()
  922:         self._grad_conflict_log = cos_sim
  923: 
  924:     # ----------------------------------------------------------------
  925:     # Helper functions (same as feddsa.py)
  926:     # ----------------------------------------------------------------
  927: 
  928:     def _style_augment(self, h):
  929:         """AdaIN-style augmentation using external style."""
  930:         idx = np.random.randint(0, len(self.local_style_bank))
  931:         mu_ext, sigma_ext = self.local_style_bank[idx]
  932:         mu_ext = mu_ext.to(h.device)
  933:         sigma_ext = sigma_ext.to(h.device)
  934: 
  935:         mu_local = h.mean(dim=0)
  936:         sigma_local = h.std(dim=0, unbiased=False).clamp(min=1e-6)
  937: 
  938:         alpha = np.random.beta(0.1, 0.1)
  939:         mu_mix = alpha * mu_local + (1 - alpha) * mu_ext
  940:         sigma_mix = alpha * sigma_local + (1 - alpha) * sigma_ext
  941: 
  942:         h_norm = (h - mu_local) / sigma_local
  943:         return h_norm * sigma_mix + mu_mix
  944: 
  945:     def _decouple_loss(self, z_sem, z_sty):
  946:         """Orthogonal + HSIC dual constraint."""
  947:         z_sem_n = F.normalize(z_sem, dim=1)
  948:         z_sty_n = F.normalize(z_sty, dim=1)
  949:         cos = (z_sem_n * z_sty_n).sum(dim=1)
  950:         loss_orth = (cos ** 2).mean()
  951: 
  952:         loss_hsic = self._hsic(z_sem, z_sty)
  953:         return loss_orth, loss_hsic
  954: 
  955:     def _hsic(self, x, y):
  956:         n = x.size(0)
  957:         if n < 4:
  958:             return torch.tensor(0.0, device=x.device)
  959:         Kx = self._gaussian_kernel(x)
  960:         Ky = self._gaussian_kernel(y)
  961:         H = torch.eye(n, device=x.device) - torch.ones(n, n, device=x.device) / n
  962:         return torch.trace(Kx @ H @ Ky @ H) / (n * n)
  963: 
  964:     def _gaussian_kernel(self, x):
  965:         n = x.size(0)
  966:         dist = torch.cdist(x, x, p=2) ** 2
  967:         nonzero = dist[dist > 0]
  968:         if nonzero.numel() == 0:
  969:             return torch.ones(n, n, device=x.device)
  970:         median = torch.median(nonzero)
  971:         bw = median / (2.0 * np.log(n + 1) + 1e-6)
  972:         return torch.exp(-dist / (2.0 * bw.clamp(min=1e-6)))
  973: 
  974:     def _infonce_loss(self, z_sem, y):
  975:         """InfoNCE: pull toward same-class global proto, push away others."""
  976:         available = sorted([c for c, p in self.global_protos.items() if p is not None])
  977:         if len(available) < 2:
  978:             return torch.tensor(0.0, device=z_sem.device)
  979: 
  980:         proto_matrix = torch.stack([self.global_protos[c].to(z_sem.device) for c in available])
  981:         class_to_idx = {c: i for i, c in enumerate(available)}
  982: 
  983:         z_n = F.normalize(z_sem, dim=1)
  984:         p_n = F.normalize(proto_matrix, dim=1)
  985:         logits = (z_n @ p_n.T) / self.tau
  986: 
  987:         targets = []
  988:         valid = []
  989:         for i in range(y.size(0)):
  990:             label = y[i].item()
  991:             if label in class_to_idx:
  992:                 targets.append(class_to_idx[label])
  993:                 valid.append(i)
  994: 
  995:         if len(valid) == 0:
  996:             return torch.tensor(0.0, device=z_sem.device)
  997: 
  998:         valid_t = torch.tensor(valid, device=z_sem.device)
  999:         targets_t = torch.tensor(targets, device=z_sem.device, dtype=torch.long)
 1000:         return F.cross_entropy(logits[valid_t], targets_t)
 1001: 
 1002:     def _infonce_alpha_loss(self, z_sem, y):
 1003:         """Alpha-sparsity InfoNCE (FedPLVM-inspired).
 1004:         cos_sim^alpha weakens positive-pair gradients, focuses on hard negatives.
 1005:         Requires cos_sim >= 0, so we clamp after normalization."""
 1006:         available = sorted([c for c, p in self.global_protos.items() if p is not None])
 1007:         if len(available) < 2:
 1008:             return torch.tensor(0.0, device=z_sem.device)
 1009: 
 1010:         proto_matrix = torch.stack([self.global_protos[c].to(z_sem.device) for c in available])
 1011:         class_to_idx = {c: i for i, c in enumerate(available)}
 1012: 
 1013:         z_n = F.normalize(z_sem, dim=1)
 1014:         p_n = F.normalize(proto_matrix, dim=1)
 1015:         cos_sim = (z_n @ p_n.T).clamp(min=0)  # clamp negatives to 0 for pow safety
 1016:         logits = cos_sim.pow(self.alpha_sparsity) / self.tau
 1017: 
 1018:         targets = []
 1019:         valid = []
 1020:         for i in range(y.size(0)):
 1021:             label = y[i].item()
 1022:             if label in class_to_idx:
 1023:                 targets.append(class_to_idx[label])
 1024:                 valid.append(i)
 1025: 
 1026:         if len(valid) == 0:
 1027:             return torch.tensor(0.0, device=z_sem.device)
 1028: 
 1029:         valid_t = torch.tensor(valid, device=z_sem.device)
 1030:         targets_t = torch.tensor(targets, device=z_sem.device, dtype=torch.long)
 1031:         return F.cross_entropy(logits[valid_t], targets_t)
 1032: 
 1033:     def _mse_anchor_loss(self, z_sem, y):
 1034:         """MSE anchor loss (FPL-inspired): pull z_sem toward own-class global proto.
 1035:         Acts as a stable 'gravitational center' preventing feature drift."""
 1036:         targets = []
 1037:         valid = []
 1038:         for i in range(y.size(0)):
 1039:             label = y[i].item()
 1040:             if label in self.global_protos and self.global_protos[label] is not None:
 1041:                 targets.append(self.global_protos[label])
 1042:                 valid.append(i)
 1043:         if not valid:
 1044:             return torch.tensor(0.0, device=z_sem.device)
 1045:         target_matrix = torch.stack(targets).to(z_sem.device).detach()
 1046:         valid_t = torch.tensor(valid, device=z_sem.device)
 1047:         return F.mse_loss(z_sem[valid_t], target_matrix)
 1048: 
 1049:     # ----------------------------------------------------------------
 1050:     # SCPR: Self-Masked Style-Weighted Multi-Positive InfoNCE
 1051:     # ----------------------------------------------------------------
 1052: 
 1053:     def _scpr_loss(self, z_sem, y):
 1054:         """Self-masked (style-)weighted multi-positive InfoNCE.
 1055: 
 1056:         For client k (self-masked by Server: j != k), given domain-indexed
 1057:         protos {p_c^j} and client-pair weights w_{k->j}, and target sample
 1058:         (z_i, y_i=c), the loss (per-class renormalized) is:
 1059:             L = - sum_{j in A_i^c} (w_{k->j} / Z_i^c) * log [
 1060:                   exp(sim(z_i, p_c^j)/tau_nce) /
 1061:                   ( exp(sim(z_i, p_c^j)/tau_nce)
 1062:                     + sum_{c' != c, l in A_i^{c'}} exp(sim(z_i, p_{c'}^l)/tau_nce) )
 1063:                 ]
 1064:         where A_i^c = {j != k : p_c^j exists} and Z_i^c = sum of weights over A_i^c.
 1065: 
 1066:         Under scpr_mode=1 (uniform weights) this reduces to M3 domain-aware
 1067:         multi-positive SupCon (PACS EXP-072 +5.09% lower bound). Under
 1068:         scpr_mode=2 (style-weighted softmax) it is the full SCPR method.
 1069: 
 1070:         All prototypes are detached (soft anchors, no grad back to bank).
 1071:         """
 1072:         if self.scpr_payload is None:
 1073:             return torch.tensor(0.0, device=z_sem.device)
 1074:         weights = self.scpr_payload.get('weights', {})
 1075:         domain_protos = self.scpr_payload.get('protos', {})
 1076:         if not weights or not domain_protos:
 1077:             return torch.tensor(0.0, device=z_sem.device)
 1078: 
 1079:         device = z_sem.device
 1080: 
 1081:         # Flatten into per-entry arrays: each entry = (class, source_cid, proto, weight)
 1082:         entry_protos = []
 1083:         entry_classes = []
 1084:         entry_weights = []
 1085:         for cid, protos in domain_protos.items():
 1086:             w = float(weights.get(cid, 0.0))
 1087:             for c, proto in protos.items():
 1088:                 entry_protos.append(proto)
 1089:                 entry_classes.append(int(c))
 1090:                 entry_weights.append(w)
 1091: 
 1092:         if len(entry_protos) < 2:
 1093:             return torch.tensor(0.0, device=device)
 1094: 
 1095:         proto_matrix = torch.stack([p.to(device) for p in entry_protos]).detach()  # [N, D]
 1096:         entry_classes_t = torch.tensor(entry_classes, device=device, dtype=torch.long)  # [N]
 1097:         entry_weights_t = torch.tensor(entry_weights, device=device, dtype=torch.float32)  # [N]
 1098: 
 1099:         # Cosine-similarity logits with InfoNCE temperature
 1100:         z_n = F.normalize(z_sem, dim=1)
 1101:         p_n = F.normalize(proto_matrix, dim=1)
 1102:         logits = (z_n @ p_n.T) / self.tau  # [B, N]
 1103:         log_prob = logits - torch.logsumexp(logits, dim=1, keepdim=True)  # [B, N]
 1104: 
 1105:         # Positive mask: entry.class == y[i]  -> [B, N]
 1106:         y_col = y.unsqueeze(1)  # [B, 1]
 1107:         pos_mask = (entry_classes_t.unsqueeze(0) == y_col).float()  # [B, N]
 1108: 
 1109:         # Per-sample renormalize positive weights over A_i^c (clients that have class c)
 1110:         pos_w = pos_mask * entry_weights_t.unsqueeze(0)  # [B, N]
 1111:         pos_w_sum = pos_w.sum(dim=1, keepdim=True)  # [B, 1]
 1112:         pos_w_norm = pos_w / pos_w_sum.clamp(min=1e-8)  # [B, N]
 1113: 
 1114:         # Weighted NLL; skip samples with no positive (A_i^c empty)
 1115:         loss_per_sample = -(pos_w_norm * log_prob).sum(dim=1)  # [B]
 1116:         has_pos = (pos_w_sum.squeeze(1) > 1e-8).float()  # [B]
 1117:         denom = has_pos.sum().clamp(min=1.0)
 1118:         return (loss_per_sample * has_pos).sum() / denom
 1119: 
 1120: 

codex
The surrounding implementation is loaded now. I’m cross-checking the SCPR doc/math against the live `train()`, `_infonce_loss()`, and server packing paths to see if the new branch truly matches the stated M3/SCPR behavior and doesn’t collide with existing scheduling or personalization.
ERROR: Reconnecting... 1/5
codex
I have the live implementation now. Next pass is narrower: I’m checking the new server-side bank/payload lifecycle and then the exact SCPR loss against the stated math, especially the multi-positive denominator and the `scpr=1` reduction claim.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'D:\\桌面文件\\联邦学习\\FDSE_CVPR25\\algorithm\\feddsa_scheduled.py' | Select-Object -Skip 1040 -First 90" in D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-18_SCPR_v1
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$i=1; Get-Content -Path '"'D:\\桌面文件\\联邦学习\\FDSE_CVPR25\\algorithm\\feddsa_scheduled.py' | ForEach-Object { '{0,5}: {1}' -f "'$i, $_; $i++ } | Select-Object -Skip 120 -First 420' in D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-18_SCPR_v1
 succeeded in 808ms:
                targets.append(self.global_protos[label])
                valid.append(i)
        if not valid:
            return torch.tensor(0.0, device=z_sem.device)
        target_matrix = torch.stack(targets).to(z_sem.device).detach()
        valid_t = torch.tensor(valid, device=z_sem.device)
        return F.mse_loss(z_sem[valid_t], target_matrix)

    # ----------------------------------------------------------------
    # SCPR: Self-Masked Style-Weighted Multi-Positive InfoNCE
    # ----------------------------------------------------------------

    def _scpr_loss(self, z_sem, y):
        """Self-masked (style-)weighted multi-positive InfoNCE.

        For client k (self-masked by Server: j != k), given domain-indexed
        protos {p_c^j} and client-pair weights w_{k->j}, and target sample
        (z_i, y_i=c), the loss (per-class renormalized) is:
            L = - sum_{j in A_i^c} (w_{k->j} / Z_i^c) * log [
                  exp(sim(z_i, p_c^j)/tau_nce) /
                  ( exp(sim(z_i, p_c^j)/tau_nce)
                    + sum_{c' != c, l in A_i^{c'}} exp(sim(z_i, p_{c'}^l)/tau_nce) )
                ]
        where A_i^c = {j != k : p_c^j exists} and Z_i^c = sum of weights over A_i^c.

        Under scpr_mode=1 (uniform weights) this reduces to M3 domain-aware
        multi-positive SupCon (PACS EXP-072 +5.09% lower bound). Under
        scpr_mode=2 (style-weighted softmax) it is the full SCPR method.

        All prototypes are detached (soft anchors, no grad back to bank).
        """
        if self.scpr_payload is None:
            return torch.tensor(0.0, device=z_sem.device)
        weights = self.scpr_payload.get('weights', {})
        domain_protos = self.scpr_payload.get('protos', {})
        if not weights or not domain_protos:
            return torch.tensor(0.0, device=z_sem.device)

        device = z_sem.device

        # Flatten into per-entry arrays: each entry = (class, source_cid, proto, weight)
        entry_protos = []
        entry_classes = []
        entry_weights = []
        for cid, protos in domain_protos.items():
            w = float(weights.get(cid, 0.0))
            for c, proto in protos.items():
                entry_protos.append(proto)
                entry_classes.append(int(c))
                entry_weights.append(w)

        if len(entry_protos) < 2:
            return torch.tensor(0.0, device=device)

        proto_matrix = torch.stack([p.to(device) for p in entry_protos]).detach()  # [N, D]
        entry_classes_t = torch.tensor(entry_classes, device=device, dtype=torch.long)  # [N]
        entry_weights_t = torch.tensor(entry_weights, device=device, dtype=torch.float32)  # [N]

        # Cosine-similarity logits with InfoNCE temperature
        z_n = F.normalize(z_sem, dim=1)
        p_n = F.normalize(proto_matrix, dim=1)
        logits = (z_n @ p_n.T) / self.tau  # [B, N]
        log_prob = logits - torch.logsumexp(logits, dim=1, keepdim=True)  # [B, N]

        # Positive mask: entry.class == y[i]  -> [B, N]
        y_col = y.unsqueeze(1)  # [B, 1]
        pos_mask = (entry_classes_t.unsqueeze(0) == y_col).float()  # [B, N]

        # Per-sample renormalize positive weights over A_i^c (clients that have class c)
        pos_w = pos_mask * entry_weights_t.unsqueeze(0)  # [B, N]
        pos_w_sum = pos_w.sum(dim=1, keepdim=True)  # [B, 1]
        pos_w_norm = pos_w / pos_w_sum.clamp(min=1e-8)  # [B, N]

        # Weighted NLL; skip samples with no positive (A_i^c empty)
        loss_per_sample = -(pos_w_norm * log_prob).sum(dim=1)  # [B]
        has_pos = (pos_w_sum.squeeze(1) > 1e-8).float()  # [B]
        denom = has_pos.sum().clamp(min=1.0)
        return (loss_per_sample * has_pos).sum() / denom


# ============================================================
# Model initialization
# ============================================================

model_map = {
    'PACS': lambda: FedDSAModel(num_classes=7, feat_dim=1024, proj_dim=128),
    'office': lambda: FedDSAModel(num_classes=10, feat_dim=1024, proj_dim=128),
    'domainnet': lambda: FedDSAModel(num_classes=10, feat_dim=1024, proj_dim=128),
}


 succeeded in 862ms:
  121:             'bw': 30,     # bell_width (gaussian sigma)
  122:             'cr': 80,     # cutoff_round (for mode 2)
  123:             'gli': 10,    # grad_log_interval (0=off)
  124:             'lm': 1.0,    # lambda_mse (MSE anchor weight, modes 4/6)
  125:             'al': 0.25,   # alpha_sparsity (power for cos_sim, modes 5/6)
  126:             'se': 0,      # save_errors: 0=off, 1=save client checkpoints on last round
  127:             'sas': 0,     # style-aware aggregation scope:
  128:                           #   0 = off (baseline B0)
  129:                           #   1 = sem_head only, classifier FedAvg (Plan A / B1 / EXP-084)
  130:                           #   2 = sem_head + classifier, both style-conditioned (A2 / sas-FH, OURS)
  131:                           #   3 = sem_head sas + classifier uniform-avg (C2 counterfactual)
  132:                           #   4 = sem_head sas + classifier fully local (C1 counterfactual)
  133:             'sas_tau': 0.3,  # softmax temperature for style similarity
  134:             # -------- SCPR (Self-Masked Style-Weighted Multi-Positive InfoNCE) --------
  135:             # NEW (2026-04-19): refined via GPT-5.4 x5-round refine session
  136:             # (obsidian_exprtiment_results/refine_logs/2026-04-18_SCPR_v1/)
  137:             'scpr': 0,    # SCPR mode:
  138:                           #   0 = off (fall back to sm schedule / Plan A, no SCPR loss)
  139:                           #   1 = uniform multi-positive (= M3 / SCPR tau->inf lower bound)
  140:                           #   2 = style-weighted multi-positive (SCPR, OURS)
  141:                           # Always self-masked (j != k) when mode > 0.
  142:                           # Uses domain-indexed class prototypes; NO new trainable params.
  143:             'scpr_tau': 0.3,  # SCPR style attention temperature (inherited from SAS optimal)
  144:         })
  145:         # Readable aliases
  146:         self.lambda_orth = float(self.lo)
  147:         self.lambda_hsic = float(self.lh)
  148:         self.lambda_sem = float(self.ls)
  149:         self.tau = float(self.tau)
  150:         self.style_dispatch_num = int(self.sdn)
  151:         self.proj_dim = int(self.pd)
  152:         self.schedule_mode = int(self.sm)
  153:         self.bell_peak = float(self.bp)
  154:         self.bell_width = float(self.bw)
  155:         self.cutoff_round = int(self.cr)
  156:         self.grad_log_interval = int(self.gli)
  157:         self.lambda_mse = float(self.lm)
  158:         self.alpha_sparsity = float(self.al)
  159:         self.save_errors = int(self.se)
  160:         self.style_aware_sem = int(self.sas)
  161:         self.style_aware_tau = float(self.sas_tau)
  162:         # SCPR aliases
  163:         self.scpr_mode = int(self.scpr)
  164:         self.scpr_tau_val = float(self.scpr_tau)
  165:         self.sample_option = 'full'
  166: 
  167:         # 方案 A：per-client semantic_head states (最近一轮上传的)
  168:         self.client_sem_states = {}
  169:         # sas-FH (A2/C1/C2)：per-client classifier head states
  170:         self.client_head_states = {}
  171: 
  172:         # Style bank
  173:         self.style_bank = {}
  174:         # Global semantic prototypes
  175:         self.global_semantic_protos = {}
  176:         # SCPR: per-(client, class) prototypes bank (domain-indexed)
  177:         #   key: client_id -> value: dict {class_idx: proto_tensor (cpu)}
  178:         self.client_class_protos = {}
  179:         # Gradient conflict log
  180:         self.grad_conflict_log = {}
  181:         # Best checkpoint tracking (for save_errors)
  182:         self._best_avg_acc = -1.0
  183:         self._best_round = 0
  184:         self._best_global_state = None
  185:         self._best_client_states = None
  186:         # Same-round FedAvg head snapshot at best round (for Claim 2 swap diagnostic)
  187:         self._best_fedavg_head_state = None
  188: 
  189:         self._init_agg_keys()
  190: 
  191:         # Pass config to clients
  192:         for c in self.clients:
  193:             c.lambda_orth = self.lambda_orth
  194:             c.lambda_hsic = self.lambda_hsic
  195:             c.lambda_sem = self.lambda_sem
  196:             c.tau = self.tau
  197:             c.proj_dim = self.proj_dim
  198:             c.schedule_mode = self.schedule_mode
  199:             c.bell_peak = self.bell_peak
  200:             c.bell_width = self.bell_width
  201:             c.cutoff_round = self.cutoff_round
  202:             c.grad_log_interval = self.grad_log_interval
  203:             c.lambda_mse = self.lambda_mse
  204:             c.alpha_sparsity = self.alpha_sparsity
  205:             # SCPR params to client
  206:             c.scpr_mode = self.scpr_mode
  207:             c.scpr_tau_val = self.scpr_tau_val
  208: 
  209:     def _init_agg_keys(self):
  210:         """Classify model keys into: shared (FedAvg), private (style_head+BN)."""
  211:         all_keys = list(self.model.state_dict().keys())
  212:         self.private_keys = set()
  213:         for k in all_keys:
  214:             if 'style_head' in k:
  215:                 self.private_keys.add(k)
  216:             elif 'bn' in k.lower() and ('running_' in k or 'num_batches_tracked' in k):
  217:                 self.private_keys.add(k)
  218:         self.shared_keys = [k for k in all_keys if k not in self.private_keys]
  219: 
  220:     def pack(self, client_id, mtype=0):
  221:         """Send global model + protos + styles.
  222: 
  223:         sas 聚合策略（分两个部分分别处理）：
  224:           sem_head:
  225:             - sas in {1,2,3,4}: style-conditioned 个性化 (同 Plan A)
  226:             - sas == 0: FedAvg (不做个性化)
  227:           classifier head:
  228:             - sas == 2 (A2): style-conditioned 个性化（OURS, sas-FH）
  229:             - sas == 3 (C2): uniform-avg 聚合（均匀加权，不用 style）
  230:             - sas == 4 (C1): local-only（用 client 自己最近一轮上传的 head）
  231:             - sas in {0,1}: FedAvg (Plan A 行为)
  232:         """
  233:         dispatched_styles = None
  234:         if self.schedule_mode != 0 and len(self.style_bank) > 0:
  235:             available = {cid: s for cid, s in self.style_bank.items() if cid != client_id}
  236:             if len(available) == 0:
  237:                 available = self.style_bank
  238:             n = min(self.style_dispatch_num, len(available))
  239:             if n > 0:
  240:                 keys = list(available.keys())
  241:                 chosen = np.random.choice(keys, n, replace=False)
  242:                 dispatched_styles = [available[k] for k in chosen]
  243: 
  244:         model_to_send = copy.deepcopy(self.model)
  245:         model_state = model_to_send.state_dict()
  246:         overridden = False
  247: 
  248:         # --- 1) sem_head 部分 ---
  249:         if self.style_aware_sem in (1, 2, 3, 4) \
  250:                 and len(self.client_sem_states) >= 2 \
  251:                 and client_id in self.style_bank:
  252:             personalized_sem = self._compute_style_weighted(
  253:                 self.client_sem_states, client_id
  254:             )
  255:             if personalized_sem is not None:
  256:                 for k, v in personalized_sem.items():
  257:                     if k in model_state:
  258:                         model_state[k] = v.to(model_state[k].device)
  259:                 overridden = True
  260: 
  261:         # --- 2) classifier head 部分 ---
  262:         if self.style_aware_sem == 2 \
  263:                 and len(self.client_head_states) >= 2 \
  264:                 and client_id in self.style_bank:
  265:             # A2 (sas-FH): style-conditioned personalized head
  266:             personalized_head = self._compute_style_weighted(
  267:                 self.client_head_states, client_id
  268:             )
  269:             if personalized_head is not None:
  270:                 for k, v in personalized_head.items():
  271:                     if k in model_state:
  272:                         model_state[k] = v.to(model_state[k].device)
  273:                 overridden = True
  274:         elif self.style_aware_sem == 3 and len(self.client_head_states) >= 2:
  275:             # C2 counterfactual: uniform-avg head (每个 client 收到同一份均值 head)
  276:             uniform_head = self._compute_uniform_avg(self.client_head_states)
  277:             if uniform_head is not None:
  278:                 for k, v in uniform_head.items():
  279:                     if k in model_state:
  280:                         model_state[k] = v.to(model_state[k].device)
  281:                 overridden = True
  282:         elif self.style_aware_sem == 4 and client_id in self.client_head_states:
  283:             # C1 counterfactual: local-only head (client 拿自己上轮上传的 head)
  284:             local_head = self.client_head_states[client_id]
  285:             for k, v in local_head.items():
  286:                 if k in model_state:
  287:                     model_state[k] = v.to(model_state[k].device)
  288:             overridden = True
  289: 
  290:         if overridden:
  291:             model_to_send.load_state_dict(model_state)
  292: 
  293:         # SCPR: compute self-masked style-weighted (or uniform) prototype payload
  294:         # Returns None when scpr=0 OR bank insufficient; client falls back to non-SCPR path.
  295:         scpr_payload = None
  296:         if self.scpr_mode > 0 and len(self.client_class_protos) >= 2:
  297:             scpr_payload = self._compute_scpr_payload(client_id)
  298: 
  299:         return {
  300:             'model': model_to_send,
  301:             'global_protos': copy.deepcopy(self.global_semantic_protos),
  302:             'style_bank': dispatched_styles,
  303:             'current_round': self.current_round,
  304:             'scpr_payload': scpr_payload,
  305:         }
  306: 
  307:     @staticmethod
  308:     def _extract_style_vec(style):
  309:         """从 style_bank 的 entry (tuple/dict/tensor) 提取展平的 style 向量。"""
  310:         if isinstance(style, (tuple, list)):
  311:             return style[0].flatten().cpu()
  312:         elif isinstance(style, dict):
  313:             v = style.get('mu', next(iter(style.values())))
  314:             return v.flatten().cpu()
  315:         else:
  316:             return style.flatten().cpu()
  317: 
  318:     def _compute_style_weighted(self, client_states, target_cid):
  319:         """通用版本：对 target client 计算其他 client 的 style-similarity 加权 state。
  320: 
  321:         Args:
  322:             client_states: dict[cid -> state_dict]，比如 self.client_sem_states 或
  323:                            self.client_head_states。所有 value 必须有相同的 key 集合。
  324:             target_cid: 目标 client id。
  325:         Returns:
  326:             aggregated state dict (同 client_states[cid] 的结构)，或 None（无法聚合时）。
  327:         """
  328:         import torch as _t
  329:         target_style = self.style_bank.get(target_cid)
  330:         if target_style is None:
  331:             return None
  332:         target_vec = self._extract_style_vec(target_style)
  333: 
  334:         sims = []
  335:         cids = []
  336:         for cid in client_states:
  337:             src_style = self.style_bank.get(cid)
  338:             if src_style is None:
  339:                 continue
  340:             src_vec = self._extract_style_vec(src_style)
  341:             if src_vec.shape != target_vec.shape:
  342:                 continue
  343:             dot = (src_vec * target_vec).sum()
  344:             norm = src_vec.norm() * target_vec.norm() + 1e-8
  345:             sims.append((dot / norm).item())
  346:             cids.append(cid)
  347: 
  348:         if len(sims) == 0:
  349:             return None
  350: 
  351:         sims_t = _t.tensor(sims, dtype=_t.float32) / max(self.style_aware_tau, 1e-3)
  352:         weights = _t.softmax(sims_t, dim=0)
  353: 
  354:         accumulated = None
  355:         for w, cid in zip(weights.tolist(), cids):
  356:             st = client_states[cid]
  357:             if accumulated is None:
  358:                 accumulated = {k: v.clone() * w for k, v in st.items()}
  359:             else:
  360:                 for k in accumulated:
  361:                     accumulated[k] += st[k] * w
  362:         return accumulated
  363: 
  364:     def _compute_uniform_avg(self, client_states):
  365:         """C2 counterfactual: 对所有 client state 做均匀平均（uniform mean），
  366:         不使用 style 信息。每个 target client 都会收到同一份结果。
  367: 
  368:         Args:
  369:             client_states: dict[cid -> state_dict]
  370:         Returns:
  371:             averaged state dict，或 None（空输入时）。
  372:         """
  373:         if len(client_states) == 0:
  374:             return None
  375:         cids = list(client_states.keys())
  376:         n = float(len(cids))
  377:         accumulated = None
  378:         for cid in cids:
  379:             st = client_states[cid]
  380:             if accumulated is None:
  381:                 accumulated = {k: v.clone() / n for k, v in st.items()}
  382:             else:
  383:                 for k in accumulated:
  384:                     accumulated[k] += st[k] / n
  385:         return accumulated
  386: 
  387:     def _compute_scpr_payload(self, target_cid):
  388:         """Compute SCPR payload for target client: self-masked style-weighted
  389:         (or uniform) prototype dispatch.
  390: 
  391:         scpr_mode=1 (uniform multi-positive, = M3 lower bound):
  392:             w_{k->j}^{uniform} = 1 / (K-1) for all j != k that have proto(s)
  393:         scpr_mode=2 (style-weighted, OURS):
  394:             w_{k->j} = softmax_j( cos(s_k, s_j) / tau_SCPR ) * 1[j != k]
  395: 
  396:         Returns:
  397:             None if insufficient bank or target style missing.
  398:             Otherwise dict with two keys:
  399:               'weights': {source_cid: float}  (self-masked, sums to 1 over source_cids)
  400:               'protos':  {source_cid: {class_idx: proto_tensor_cpu}}  (raw per-(class, client) bank,
  401:                          client will re-normalize per-class based on availability)
  402:         """
  403:         import torch as _t
  404:         target_style = self.style_bank.get(target_cid)
  405:         if target_style is None:
  406:             return None
  407:         target_vec = self._extract_style_vec(target_style)
  408: 
  409:         # Collect candidates (self-masked: j != target_cid)
  410:         source_cids = []
  411:         source_vecs = []
  412:         source_protos = {}
  413:         for cid, protos in self.client_class_protos.items():
  414:             if cid == target_cid:  # SELF-MASK (R1 reviewer fix)
  415:                 continue
  416:             src_style = self.style_bank.get(cid)
  417:             if src_style is None:
  418:                 continue
  419:             src_vec = self._extract_style_vec(src_style)
  420:             if src_vec.shape != target_vec.shape:
  421:                 continue
  422:             if protos is None or len(protos) == 0:
  423:                 continue
  424:             source_cids.append(cid)
  425:             source_vecs.append(src_vec)
  426:             source_protos[cid] = protos
  427: 
  428:         if len(source_cids) == 0:
  429:             return None
  430: 
  431:         # Attention weights over source clients (self-mask already applied above)
  432:         if self.scpr_mode == 1:
  433:             # Uniform (mathematically equivalent to SCPR tau -> infinity; = M3 lower bound)
  434:             uniform_w = 1.0 / len(source_cids)
  435:             weights = {cid: uniform_w for cid in source_cids}
  436:         elif self.scpr_mode == 2:
  437:             # Style-weighted softmax (SCPR main method)
  438:             sims = []
  439:             for src_vec in source_vecs:
  440:                 dot = (src_vec * target_vec).sum()
  441:                 norm = src_vec.norm() * target_vec.norm() + 1e-8
  442:                 sims.append((dot / norm).item())
  443:             sims_t = _t.tensor(sims, dtype=_t.float32) / max(self.scpr_tau_val, 1e-3)
  444:             w_vec = _t.softmax(sims_t, dim=0).tolist()
  445:             weights = {cid: float(w) for cid, w in zip(source_cids, w_vec)}
  446:         else:
  447:             return None
  448: 
  449:         return {'weights': weights, 'protos': source_protos}
  450: 
  451:     def iterate(self):
  452:         self.selected_clients = self.sample()
  453:         res = self.communicate(self.selected_clients)
  454: 
  455:         models = res['model']
  456:         protos_list = res['protos']
  457:         proto_counts_list = res['proto_counts']
  458:         style_stats_list = res['style_stats']
  459:         grad_conflict_list = res['grad_conflict']
  460: 
  461:         # Aggregate shared parameters (FedAvg)
  462:         self._aggregate_shared(models)
  463: 
  464:         # sas：记录每个 client 上传的 sem_head / head state（供 pack 时做聚合）
  465:         if self.style_aware_sem in (1, 2, 3, 4):
  466:             for cid, m in zip(self.received_clients, models):
  467:                 m_state = m.state_dict()
  468:                 sem_state = {k: v.clone().cpu() for k, v in m_state.items()
  469:                              if 'semantic_head' in k}
  470:                 self.client_sem_states[cid] = sem_state
  471:                 # sas-FH (A2/C1/C2) 需要额外记录 classifier head
  472:                 if self.style_aware_sem in (2, 3, 4):
  473:                     head_state = {k: v.clone().cpu() for k, v in m_state.items()
  474:                                   if k.startswith('head.')}
  475:                     if head_state:
  476:                         self.client_head_states[cid] = head_state
  477: 
  478:         # Collect style bank
  479:         for cid, style in zip(self.received_clients, style_stats_list):
  480:             if style is not None:
  481:                 self.style_bank[cid] = style
  482: 
  483:         # SCPR: update per-(client, class) prototype bank (domain-indexed)
  484:         if self.scpr_mode > 0:
  485:             for cid, protos in zip(self.received_clients, protos_list):
  486:                 if protos is not None and len(protos) > 0:
  487:                     self.client_class_protos[cid] = {
  488:                         c: p.detach().clone().cpu() for c, p in protos.items()
  489:                     }
  490: 
  491:         # Aggregate global protos (weighted mean)
  492:         self._aggregate_protos(protos_list, proto_counts_list)
  493: 
  494:         # Log gradient conflict
  495:         for cid, gc in zip(self.received_clients, grad_conflict_list):
  496:             if gc is not None:
  497:                 if self.current_round not in self.grad_conflict_log:
  498:                     self.grad_conflict_log[self.current_round] = {}
  499:                 self.grad_conflict_log[self.current_round][cid] = gc
  500:         if self.current_round in self.grad_conflict_log:
  501:             entries = self.grad_conflict_log[self.current_round]
  502:             vals = list(entries.values())
  503:             mean_cos = sum(vals) / len(vals)
  504:             self.gv.logger.info(
  505:                 f"[GradConflict] round={self.current_round} "
  506:                 f"mean_cos={mean_cos:.4f} per_client={entries}"
  507:             )
  508: 
  509:         # 追踪 best（每轮）+ 最后一轮保存 best checkpoint
  510:         if int(getattr(self, 'save_errors', 0)) == 1:
  511:             self._track_best()
  512:             if self.current_round >= self.num_rounds:
  513:                 self._save_best_checkpoints()
  514: 
  515:     def _track_best(self):
  516:         """每轮聚合后缓存 best AVG acc 对应的模型快照。
  517:         读取 flgo logger.output 拿最新 test metrics（上一轮的结果）。"""
  518:         try:
  519:             output = getattr(self.gv.logger, 'output', None)
  520:             if not output or 'mean_local_test_accuracy' not in output:
  521:                 return
  522:             history = output['mean_local_test_accuracy']
  523:             if not history:
  524:                 return
  525:             current = float(history[-1]) * 100
  526:             if current > self._best_avg_acc:
  527:                 import copy
  528:                 self._best_avg_acc = current
  529:                 self._best_round = len(history)  # round index (1-based)
  530:                 self._best_global_state = copy.deepcopy(self.model.state_dict())
  531:                 self._best_client_states = [
  532:                     copy.deepcopy(c.model.state_dict()) if getattr(c, 'model', None) is not None else None
  533:                     for c in self.clients
  534:                 ]
  535:                 # sas-FH Claim 2：保存同轮 FedAvg head 快照（供 swap diagnostic 用）
  536:                 # 仅 A2 模式下有意义：此时 server.model 里 head 已是 FedAvg 版本，
  537:                 # 但下发给 client 的 head 是 style-conditioned personalized，
  538:                 # 两者有差异 → 需要保留这份 "same-round global_head"。
  539:                 self._best_fedavg_head_state = {
  540:                     k: v.clone().cpu() for k, v in self.model.state_dict().items()

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Path 'FINAL_PROPOSAL.md' | Select-Object -First 240" in D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-18_SCPR_v1
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Select-String -Path 'FINAL_PROPOSAL.md' -Pattern 'SCPR|multi-positive|M3|self-mask|renormal' -Context 3,6" in D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-18_SCPR_v1
 succeeded in 766ms:
# Research Proposal:Style-Conditioned Prototype Retrieval (SCPR)

## Self-Masked Style-Weighted Multi-Positive InfoNCE for Cross-Domain Federated Learning

> 基础框架:FedDSA(Decouple-Share-Align)
> 目标会议:CVPR / ICCV / NeurIPS
> Refine 最终评分:**9.1/10 READY**(5 轮 GPT-5.4 xhigh 审核)

---

## Problem Anchor

- **Bottom-line problem**:跨域联邦学习(FL)中,客户端数据来自不同视觉风格域(照片/素描/油画/线稿等)。现有原型对齐方法(FedProto / FPL / FedPLVM)把所有客户端的同类原型求全局均值后广播,使得风格-outlier 客户端(Caltech、Sketch)在对齐时被严重稀释,性能显著落后于 FedBN / FDSE 等域适应方法。

- **Must-solve bottleneck**:
  1. **Global-mean prototype 揉糊 style**,outlier 域在对齐时丢失自身结构
  2. **SAS 参数空间个性化** 在 Office 单 outlier 场景 +1.21%,但 PACS 全 outlier 场景 −0.65%(EXP-086 诊断:参数软聚合退化为 FedBN-like 本地化)
  3. **FedDSA "Share" 章节从未成功**:EXP-059 z_sem AdaIN 致 PACS −2.54%,EXP-078d h 空间 AdaIN→InfoNCE 致 NaN

- **Non-goals**(严格排除,85 次实验证伪):
  1. 不做"风格作为训练数据增强"
  2. 不做分类器个性化(EXP-093 证伪)
  3. 不加辅助损失(HSIC / PCGrad / Triplet / CKA / Uncertainty 全败)
  4. 不做架构改动(多层注入 / VAE head / 非对称 heads 全败)
  5. 不做训练调度复杂化
  6. 不引入新的可训练组件

- **Constraints**:
  - Backbone:ResNet-18(Office)/ AlexNet-from-scratch(PACS)
  - FedBN 原则(BN 参数本地)
  - R=200,3-seed {2, 15, 333},单卡 24GB
  - 保留正交解耦(cos² + HSIC)、orth_only Plan A(LR=0.05, warmup=50, λ_orth=1.0)
  - 保留 InfoNCE 对齐(**只改 target 权重**,不改损失形式)

- **Success condition**:
  1. PACS 3-seed mean AVG Best ≥ **81.5%**(vs orth_only 80.41%,严格优于 M3 孤立值 81.91%)
  2. Office 3-seed mean AVG Best ≥ **90.5%**(vs SAS 89.82%,逼近 FDSE 90.58%)
  3. R200 drop(Best − Last)≤ 2%
  4. **新增代码 < 100 行、0 新 trainable 组件**
  5. FedDSA Share 章节首次有正向实证

---

## Technical Gap

**无现有方法把"客户端风格 s_k"作为跨客户端 attention key、"域索引类原型 {p_c^j}"作为 value**,在**解耦后的表征空间**执行软对齐。所有已发表路径归为四派:

| 流派 | 代表 | 对风格的态度 | 失败根因(对我们) |
|------|------|-------------|----------------|
| 擦除派 | FDSE / FedDP / FedSeProto | 视为噪声,压缩或丢弃 | 风格信息全部丢失,下游看不见 |
| 私有派 | FedSTAR / FedBN / FedSDAF | 严格本地 | 无跨域互助 |
| 共享不解耦派 | FISC / PARDON / StyleDDG / FedCCRL | 在**混合空间**做 AdaIN | 破坏语义特征(EXP-059 证伪) |
| 参数路由派 | SAS(我们,未发表) | 参数空间软聚合 | PACS 全 outlier 退化(EXP-086) |

**2×2 差异化矩阵**:

|   | 风格不共享 | 风格共享 |
|---|-----------|---------|
| 不解耦 | FedBN / FedAvg / FedProto | FISC / PARDON / StyleDDG / FedCCRL |
| 解耦 | FedSTAR / FedSeProto / FDSE | **SCPR(首次占据)** |

**最小充分干预**:只改 InfoNCE alignment target 的**权重构造**,其余(特征、sem_head、classifier、BN、正交解耦、FedAvg/FedBN 规则)全部冻结。

---

## Method Thesis

> **唯一 thesis**:把 M3 domain-aware multi-positive InfoNCE 的"等权 positives"替换为"**按客户端风格相似度加权的 positives**",并**自掩码**(自己不参与 attention)。
>
> **机制动机**:在 imperfect decouple 假设和线性噪声近似下,从 entropy-regularized noise-minimization 目标函数**推导**出来的 **unique Boltzmann 最优解**(见 Formal Derivation)。softmax-over-cosine 是**推导结果**,不是设计选择。

- **最小充分干预**:修改 M3 的 positive 权重计算(~30 行),无新组件
- **数学自洽**:uniform weight 严格退化为 M3(已验证 +5.09% PACS)
- **安全性**:self-mask 保证 Share 永不退化为 FedBN-like;最差情况 = M3

---

## Contribution Focus

- **唯一主贡献**:**Self-Masked Style-Weighted Multi-Positive InfoNCE(SCPR)**
  > 在 FedDSA 解耦架构上,首次把"客户端风格"作为跨客户端 attention key,对 M3 的等权多正例进行风格加权;自掩码阻断 local-only 退化;softmax-over-cosine 的**确切形式**是从 imperfect-decouple 下 entropy-regularized noise minimization 的**唯一 Boltzmann 解**推导而得。

- **附录 composability**(非主 claim):SCPR 与 SAS 的叠加正交性

- **非贡献**:不做风格作训练数据、不做参数路由、不做新损失项、不加新可训练网络

---

## Proposed Method

### Notation(统一符号)

| 符号 | 含义 |
|------|------|
| `k, j ∈ {1..K}` | 客户端索引;`C` 类数 |
| `h = backbone(x)` | 骨干 pooled feature |
| `z_sem = sem_head(h)`,`z_sty = style_head(h)` | 解耦后的语义/风格特征 |
| `s_k := normalize(E_{x ∈ D_k}[z_sty(x)])` | 客户端 k 的 **L2-normalized 风格锚点** |
| `p_c^j := E_{(x, y=c) ∈ D_j}[z_sem(x)]` | 客户端 j 类 c 的**原型** |
| `τ_nce = 0.3` | InfoNCE 温度(继承 Plan A) |
| `τ_SCPR = 0.3` | SCPR attention 温度(继承 SAS) |

### Complexity Budget

| 组件 | 状态 |
|------|------|
| 骨干 / sem_head / style_head / classifier / BN | **冻结复用**(FedAvg/FedBN 规则不变) |
| 正交解耦(cos² + HSIC) | **复用 Plan A** |
| Style bank `{s_j}_j` | **复用 SAS/EXP-084 实现** |
| Class-proto bank `{p_c^j}_{c,j}` | **复用 M3/EXP-072 实现** |
| **SCPR attention + self-mask + renorm(~30 行)** | **新增,无参数** |

- **0** 新 trainable 组件
- **0** 新 loss 项
- **1** 新超参(`τ_SCPR`)
- **~30** 行新代码,远低于 100 LOC 预算

### System Overview

```
┌────────────── 客户端 k ─────────────────┐
│ x → backbone → h                          │
│   ├→ style_head → z_sty → s_k (上传)     │
│   ├→ sem_head  → z_sem                    │
│   │   ├→ classifier → ŷ → L_CE            │
│   │   └→ L_SCPR(z_sem, {p_c^j, w_{k→j}^c})│
│   └→ per-class z_sem mean → p_c^k (上传)  │
└───────────────────────────────────────────┘
                        │
                        ▼
┌────────── Server (SCPR aggregator) ──────┐
│ Style bank:      {s_j}_j                  │
│ Class-proto bank: {p_c^j}_{c, j}          │
│                                            │
│ For each target client i:                  │
│   g_ij = cos(s_i, s_j)                    │
│   raw_j = exp(g_ij / τ_SCPR) · 𝟙[j ≠ i]   │
│   A_i^c = {l : l ≠ i, p_c^l exists}       │
│   w_{i→j}^c = raw_j / Σ_{l ∈ A_i^c}raw_l  │
│                if j ∈ A_i^c else 0         │
│                                            │
│ Dispatch {w_{i→j}^c, p_c^j} → client i    │
│                                            │
│ FedAvg: DFE / sem_head / classifier        │
│ FedBN:  BN not aggregated                  │
└────────────────────────────────────────────┘
```

### Core Mechanism — Self-Masked Style-Weighted Multi-Positive InfoNCE

对客户端 k 上样本 `(x, y=c)`,`z = sem_head(backbone(x))`:

**Self-masked style attention**:
```
g_kj = cos(s_k, s_j),     j = 1..K
raw_j = exp(g_kj / τ_SCPR) · 𝟙[j ≠ k]
```

**Per-class renormalize**(处理 missing class):
```
A_k^c = { j : j ≠ k, p_c^j exists }
w_{k→j}^c = raw_j / Σ_{l ∈ A_k^c} raw_l    if j ∈ A_k^c
            0                                otherwise
```

**SCPR loss**(SupCon multi-positive 的 style-weighted 版本):
```
L_SCPR(z, c) = - Σ_{j ∈ A_k^c} w_{k→j}^c · log [
    exp(sim(z, p_c^j) / τ_nce)
    / ( exp(sim(z, p_c^j) / τ_nce)
        + Σ_{c' ≠ c, l ∈ A_k^{c'}} exp(sim(z, p_{c'}^l) / τ_nce) )
]
```

所有 `p_c^j` 与 `s_j` 均 `.detach()`(软锚点,不从对比损失回传梯度)。

**总损失**:
```
L = L_CE + λ_orth · L_orth + λ_hsic · L_HSIC + λ_sem · L_SCPR
```
超参继承 Plan A:`(λ_orth, λ_hsic, λ_sem) = (1.0, 0.1, 1.0)`。

### 数学性质(唯一主 claim)

> **`τ_SCPR → ∞`**:`w_{k→j}^c = 1/|A_k^c|` → SCPR **严格退化为 M3 domain-aware multi-positive InfoNCE**(已验证 PACS +5.09% 下界)。

其余解读(非 claim):
- **`τ_SCPR → 0`**:`w` 塌缩为 one-hot,指向 `argmax_{j ≠ k} cos(s_k, s_j)` → nearest-style single positive
- **Self-mask**:`w_{k→k} ≡ 0`,Share 路径永不退化为 FedBN-like 本地化

### Formal Derivation via Entropy-Regularized Noise Minimization

> **注意范围**:以下推导在 **imperfect decoupling + 线性噪声近似** 这一 residual-noise 模型下成立,**不是所有 prototype weighting 的无条件定理**。目的是证明 SCPR 的 softmax-over-cosine 形式是**该模型下的唯一最优**,不是启发式选择。

**Setup**:分解原型为
```
p_c^j = p_c^* + eps_j
```
其中 `p_c^*` 是"理想风格无关类 c 原型",`eps_j` 是客户端 j 的风格残留,源自软正交约束 `cos²(z_sem, z_sty) → 0` 不能严格为 0(实测残余 cos ~ 0.05–0.1)。

客户端 k 上对样本 z 做 SupCon,每个 positive 的 pulling 可分解:
```
sim(z, p_c^j) = sim(z, p_c^*) + sim(z, eps_j)
                 ──────────      ──────────
                 语义信号         风格噪声拉力(有害)
```

定义 `l_j := E[sim(z, eps_j) | client k]`:客户端 j 原型对 k 的风格噪声投影期望。

**优化目标**:在可行 weight 分布上最小化噪声拉力 + 熵正则
```
min_{w ∈ Δ^{K-1}}  J(w) = Σ_j w_j · l_j + τ · Σ_j w_j log w_j
s.t. Σ_j w_j = 1,  w_j ≥ 0
```
(熵正则防止 w 塌缩到 one-hot,`τ > 0` 是正则强度)

**拉格朗日一阶条件**:
```
∂J/∂w_j = l_j + τ (log w_j + 1) + λ = 0
⇒ w_j^* = exp(-l_j / τ) / Z,   Z = Σ_l exp(-l_l / τ)
```

**线性近似**(under imperfect decouple,风格越远噪声越大):
```
l_j ≈ c · (1 - cos(s_k, s_j)) = c · style_dist(k, j),  c > 0
```

**代入得**:
```
w_j^* ∝ exp(-c · (1 - cos(s_k, s_j)) / τ)
       ∝ exp(c · cos(s_k, s_j) / τ)
       = softmax_j(cos(s_k, s_j) / τ_SCPR),   τ_SCPR := τ / c
```

**结论**:**SCPR 的 softmax-over-cosine 是该 residual-noise 模型下 entropy-regularized noise 目标的 unique Boltzmann minimizer**,不是设计选择。

**两极限**(自然过渡):
- **Decouple 完美**(`eps_j ≡ 0`,`l_j ≡ 0`):`J(w)` 只剩熵项,极小化给出 uniform w → **SCPR = M3**(无缝 fallback,无切换成本)
- **Decouple 不完美**(实际情形):style weighting 是最优噪声抑制


 succeeded in 843ms:

> FINAL_PROPOSAL.md:1:# Research Proposal:Style-Conditioned Prototype Retrieval ([7mSCPR[0m)[0m
[7m[0m  FINAL_PROPOSAL.md:2:[0m
> FINAL_PROPOSAL.md:3:## [7mSelf-Mask[0med Style-Weighted Multi-Positive InfoNCE for Cross-Domain Federated Learning[0m
[7m[0m  FINAL_PROPOSAL.md:4:[0m
[7m[0m  FINAL_PROPOSAL.md:5:> 基础框架:FedDSA(Decouple-Share-Align)[0m
[7m[0m  FINAL_PROPOSAL.md:6:> 目标会议:CVPR / ICCV / NeurIPS[0m
[7m[0m  FINAL_PROPOSAL.md:7:> Refine 最终评分:**9.1/10 READY**(5 轮 GPT-5.4 xhigh 审核)[0m
[7m[0m  FINAL_PROPOSAL.md:8:[0m
[7m[0m  FINAL_PROPOSAL.md:9:---[0m
  FINAL_PROPOSAL.md:33:  - 保留 InfoNCE 对齐(**只改 target 权重**,不改损失形式)
  FINAL_PROPOSAL.md:34:
  FINAL_PROPOSAL.md:35:- **Success condition**:
> FINAL_PROPOSAL.md:36:  1. PACS 3-seed mean AVG Best ≥ **81.5%**(vs orth_only 80.41%,严格优于 [7mM3[0m 孤立值 81.91%)[0m
[7m[0m  FINAL_PROPOSAL.md:37:  2. Office 3-seed mean AVG Best ≥ **90.5%**(vs SAS 89.82%,逼近 FDSE 90.58%)[0m
[7m[0m  FINAL_PROPOSAL.md:38:  3. R200 drop(Best − Last)≤ 2%[0m
[7m[0m  FINAL_PROPOSAL.md:39:  4. **新增代码 < 100 行、0 新 trainable 组件**[0m
[7m[0m  FINAL_PROPOSAL.md:40:  5. FedDSA Share 章节首次有正向实证[0m
[7m[0m  FINAL_PROPOSAL.md:41:[0m
[7m[0m  FINAL_PROPOSAL.md:42:---[0m
  FINAL_PROPOSAL.md:57:|   | 风格不共享 | 风格共享 |
  FINAL_PROPOSAL.md:58:|---|-----------|---------|
  FINAL_PROPOSAL.md:59:| 不解耦 | FedBN / FedAvg / FedProto | FISC / PARDON / StyleDDG / FedCCRL |
> FINAL_PROPOSAL.md:60:| 解耦 | FedSTAR / FedSeProto / FDSE | **[7mSCPR[0m(首次占据)** |[0m
[7m[0m  FINAL_PROPOSAL.md:61:[0m
[7m[0m  FINAL_PROPOSAL.md:62:**最小充分干预**:只改 InfoNCE alignment target 的**权重构造**,其余(特征、sem_head、classifier、B[0m
[7m[0mN、正交解耦、FedAvg/FedBN 规则)全部冻结。[0m
[7m[0m  FINAL_PROPOSAL.md:63:[0m
[7m[0m  FINAL_PROPOSAL.md:64:---[0m
[7m[0m  FINAL_PROPOSAL.md:65:[0m
[7m[0m  FINAL_PROPOSAL.md:66:## Method Thesis[0m
  FINAL_PROPOSAL.md:67:
> FINAL_PROPOSAL.md:68:> **唯一 thesis**:把 [7mM3[0m domain-aware multi-positive InfoNCE 的"等权 positives"替换为"**按客户端[0m
[7m[0m风格相似度加权的 positives**",并**自掩码**(自己不参与 attention)。[0m
[7m[0m  FINAL_PROPOSAL.md:69:>[0m
[7m[0m  FINAL_PROPOSAL.md:70:> **机制动机**:在 imperfect decouple 假设和线性噪声近似下,从 entropy-regularized noise-minimizat[0m
[7m[0mion 目标函数**推导**出来的 **unique Boltzmann 最优解**(见 Formal Derivation)。softmax-over-cosine 是**推导结果**,不是设[0m
[7m[0m计选择。[0m
[7m[0m  FINAL_PROPOSAL.md:71:[0m
> FINAL_PROPOSAL.md:72:- **最小充分干预**:修改 [7mM3[0m 的 positive 权重计算(~30 行),无新组件
> FINAL_PROPOSAL.md:73:- **数学自洽**:uniform weight 严格退化为 [7mM3[0m(已验证 +5.09% PACS)
> FINAL_PROPOSAL.md:74:- **安全性**:[7mself-mask[0m 保证 Share 永不退化为 FedBN-like;最差情况 = M3[0m
[7m[0m  FINAL_PROPOSAL.md:75:[0m
[7m[0m  FINAL_PROPOSAL.md:76:---[0m
[7m[0m  FINAL_PROPOSAL.md:77:[0m
[7m[0m  FINAL_PROPOSAL.md:78:## Contribution Focus[0m
[7m[0m  FINAL_PROPOSAL.md:79:[0m
> FINAL_PROPOSAL.md:80:- **唯一主贡献**:**[7mSelf-Mask[0med Style-Weighted Multi-Positive InfoNCE(SCPR)**
> FINAL_PROPOSAL.md:81:  > 在 FedDSA 解耦架构上,首次把"客户端风格"作为跨客户端 attention key,对 [7mM3[0m 的等权多正例进行风格[0m
[7m[0m加权;自掩码阻断 local-only 退化;softmax-over-cosine 的**确切形式**是从 imperfect-decouple 下 entropy-regularized noise [0m
[7m[0mminimization 的**唯一 Boltzmann 解**推导而得。[0m
[7m[0m  FINAL_PROPOSAL.md:82:[0m
> FINAL_PROPOSAL.md:83:- **附录 composability**(非主 claim):[7mSCPR[0m 与 SAS 的叠加正交性[0m
[7m[0m  FINAL_PROPOSAL.md:84:[0m
[7m[0m  FINAL_PROPOSAL.md:85:- **非贡献**:不做风格作训练数据、不做参数路由、不做新损失项、不加新可训练网络[0m
[7m[0m  FINAL_PROPOSAL.md:86:[0m
[7m[0m  FINAL_PROPOSAL.md:87:---[0m
[7m[0m  FINAL_PROPOSAL.md:88:[0m
[7m[0m  FINAL_PROPOSAL.md:89:## Proposed Method[0m
  FINAL_PROPOSAL.md:98:| `s_k := normalize(E_{x ∈ D_k}[z_sty(x)])` | 客户端 k 的 **L2-normalized 风格锚点** |
  FINAL_PROPOSAL.md:99:| `p_c^j := E_{(x, y=c) ∈ D_j}[z_sem(x)]` | 客户端 j 类 c 的**原型** |
  FINAL_PROPOSAL.md:100:| `τ_nce = 0.3` | InfoNCE 温度(继承 Plan A) |
> FINAL_PROPOSAL.md:101:| `τ_[7mSCPR[0m = 0.3` | SCPR attention 温度(继承 SAS) |[0m
[7m[0m  FINAL_PROPOSAL.md:102:[0m
[7m[0m  FINAL_PROPOSAL.md:103:### Complexity Budget[0m
[7m[0m  FINAL_PROPOSAL.md:104:[0m
[7m[0m  FINAL_PROPOSAL.md:105:| 组件 | 状态 |[0m
[7m[0m  FINAL_PROPOSAL.md:106:|------|------|[0m
[7m[0m  FINAL_PROPOSAL.md:107:| 骨干 / sem_head / style_head / classifier / BN | **冻结复用**(FedAvg/FedBN 规则不变) |[0m
  FINAL_PROPOSAL.md:108:| 正交解耦(cos² + HSIC) | **复用 Plan A** |
  FINAL_PROPOSAL.md:109:| Style bank `{s_j}_j` | **复用 SAS/EXP-084 实现** |
> FINAL_PROPOSAL.md:110:| Class-proto bank `{p_c^j}_{c,j}` | **复用 [7mM3[0m/EXP-072 实现** |[0m
> FINAL_PROPOSAL.md:111:| **[7mSCPR[0m attention + self-mask + renorm(~30 行)** | **新增,无参数** |[0m
[7m[0m  FINAL_PROPOSAL.md:112:[0m
[7m[0m  FINAL_PROPOSAL.md:113:- **0** 新 trainable 组件[0m
[7m[0m  FINAL_PROPOSAL.md:114:- **0** 新 loss 项[0m
> FINAL_PROPOSAL.md:115:- **1** 新超参(`τ_[7mSCPR[0m`)[0m
[7m[0m  FINAL_PROPOSAL.md:116:- **~30** 行新代码,远低于 100 LOC 预算[0m
[7m[0m  FINAL_PROPOSAL.md:117:[0m
[7m[0m  FINAL_PROPOSAL.md:118:### System Overview[0m
[7m[0m  FINAL_PROPOSAL.md:119:[0m
[7m[0m  FINAL_PROPOSAL.md:120:```[0m
[7m[0m  FINAL_PROPOSAL.md:121:┌────────────── 客户端 k ─────────────────┐[0m
  FINAL_PROPOSAL.md:123:│   ├→ style_head → z_sty → s_k (上传)     │
  FINAL_PROPOSAL.md:124:│   ├→ sem_head  → z_sem                    │
  FINAL_PROPOSAL.md:125:│   │   ├→ classifier → ŷ → L_CE            │
> FINAL_PROPOSAL.md:126:│   │   └→ L_[7mSCPR[0m(z_sem, {p_c^j, w_{k→j}^c})│[0m
[7m[0m  FINAL_PROPOSAL.md:127:│   └→ per-class z_sem mean → p_c^k (上传)  │[0m
[7m[0m  FINAL_PROPOSAL.md:128:└───────────────────────────────────────────┘[0m
[7m[0m  FINAL_PROPOSAL.md:129:                        │[0m
[7m[0m  FINAL_PROPOSAL.md:130:                        ▼[0m
> FINAL_PROPOSAL.md:131:┌────────── Server ([7mSCPR[0m aggregator) ──────┐[0m
[7m[0m  FINAL_PROPOSAL.md:132:│ Style bank:      {s_j}_j                  │[0m
[7m[0m  FINAL_PROPOSAL.md:133:│ Class-proto bank: {p_c^j}_{c, j}          │[0m
[7m[0m  FINAL_PROPOSAL.md:134:│                                            │[0m
[7m[0m  FINAL_PROPOSAL.md:135:│ For each target client i:                  │[0m
[7m[0m  FINAL_PROPOSAL.md:136:│   g_ij = cos(s_i, s_j)                    │[0m
> FINAL_PROPOSAL.md:137:│   raw_j = exp(g_ij / τ_[7mSCPR[0m) · 𝟙[j ≠ i]   │[0m
[7m[0m  FINAL_PROPOSAL.md:138:│   A_i^c = {l : l ≠ i, p_c^l exists}       │[0m
[7m[0m  FINAL_PROPOSAL.md:139:│   w_{i→j}^c = raw_j / Σ_{l ∈ A_i^c}raw_l  │[0m
[7m[0m  FINAL_PROPOSAL.md:140:│                if j ∈ A_i^c else 0         │[0m
[7m[0m  FINAL_PROPOSAL.md:141:│                                            │[0m
[7m[0m  FINAL_PROPOSAL.md:142:│ Dispatch {w_{i→j}^c, p_c^j} → client i    │[0m
[7m[0m  FINAL_PROPOSAL.md:143:│                                            │[0m
  FINAL_PROPOSAL.md:146:└────────────────────────────────────────────┘
  FINAL_PROPOSAL.md:147:```
  FINAL_PROPOSAL.md:148:
> FINAL_PROPOSAL.md:149:### Core Mechanism — [7mSelf-Mask[0med Style-Weighted Multi-Positive InfoNCE[0m
[7m[0m  FINAL_PROPOSAL.md:150:[0m
[7m[0m  FINAL_PROPOSAL.md:151:对客户端 k 上样本 `(x, y=c)`,`z = sem_head(backbone(x))`:[0m
[7m[0m  FINAL_PROPOSAL.md:152:[0m
> FINAL_PROPOSAL.md:153:**[7mSelf-mask[0med style attention**:[0m
[7m[0m  FINAL_PROPOSAL.md:154:```[0m
[7m[0m  FINAL_PROPOSAL.md:155:g_kj = cos(s_k, s_j),     j = 1..K[0m
> FINAL_PROPOSAL.md:156:raw_j = exp(g_kj / τ_[7mSCPR[0m) · 𝟙[j ≠ k][0m
[7m[0m  FINAL_PROPOSAL.md:157:```[0m
[7m[0m  FINAL_PROPOSAL.md:158:[0m
> FINAL_PROPOSAL.md:159:**Per-class [7mrenormal[0mize**(处理 missing class):[0m
[7m[0m  FINAL_PROPOSAL.md:160:```[0m
[7m[0m  FINAL_PROPOSAL.md:161:A_k^c = { j : j ≠ k, p_c^j exists }[0m
[7m[0m  FINAL_PROPOSAL.md:162:w_{k→j}^c = raw_j / Σ_{l ∈ A_k^c} raw_l    if j ∈ A_k^c[0m
[7m[0m  FINAL_PROPOSAL.md:163:            0                                otherwise[0m
[7m[0m  FINAL_PROPOSAL.md:164:```[0m
[7m[0m  FINAL_PROPOSAL.md:165:[0m
> FINAL_PROPOSAL.md:166:**[7mSCPR[0m loss**(SupCon multi-positive 的 style-weighted 版本):[0m
[7m[0m  FINAL_PROPOSAL.md:167:```[0m
> FINAL_PROPOSAL.md:168:L_[7mSCPR[0m(z, c) = - Σ_{j ∈ A_k^c} w_{k→j}^c · log [[0m
[7m[0m  FINAL_PROPOSAL.md:169:    exp(sim(z, p_c^j) / τ_nce)[0m
[7m[0m  FINAL_PROPOSAL.md:170:    / ( exp(sim(z, p_c^j) / τ_nce)[0m
[7m[0m  FINAL_PROPOSAL.md:171:        + Σ_{c' ≠ c, l ∈ A_k^{c'}} exp(sim(z, p_{c'}^l) / τ_nce) )[0m
[7m[0m  FINAL_PROPOSAL.md:172:][0m
[7m[0m  FINAL_PROPOSAL.md:173:```[0m
[7m[0m  FINAL_PROPOSAL.md:174:[0m
  FINAL_PROPOSAL.md:176:
  FINAL_PROPOSAL.md:177:**总损失**:
  FINAL_PROPOSAL.md:178:```
> FINAL_PROPOSAL.md:179:L = L_CE + λ_orth · L_orth + λ_hsic · L_HSIC + λ_sem · L_[7mSCPR[0m
[7m[0m  FINAL_PROPOSAL.md:180:```[0m
[7m[0m  FINAL_PROPOSAL.md:181:超参继承 Plan A:`(λ_orth, λ_hsic, λ_sem) = (1.0, 0.1, 1.0)`。[0m
[7m[0m  FINAL_PROPOSAL.md:182:[0m
[7m[0m  FINAL_PROPOSAL.md:183:### 数学性质(唯一主 claim)[0m
[7m[0m  FINAL_PROPOSAL.md:184:[0m
> FINAL_PROPOSAL.md:185:> **`τ_[7mSCPR[0m → ∞`**:`w_{k→j}^c = 1/|A_k^c|` → SCPR **严格退化为 M3 domain-aware multi-positive I[0m
[7m[0mnfoNCE**(已验证 PACS +5.09% 下界)。[0m
[7m[0m  FINAL_PROPOSAL.md:186:[0m
[7m[0m  FINAL_PROPOSAL.md:187:其余解读(非 claim):[0m
> FINAL_PROPOSAL.md:188:- **`τ_[7mSCPR[0m → 0`**:`w` 塌缩为 one-hot,指向 `argmax_{j ≠ k} cos(s_k, s_j)` → nearest-style singl[0m
[7m[0me positive[0m
> FINAL_PROPOSAL.md:189:- **[7mSelf-mask[0m**:`w_{k→k} ≡ 0`,Share 路径永不退化为 FedBN-like 本地化[0m
[7m[0m  FINAL_PROPOSAL.md:190:[0m
[7m[0m  FINAL_PROPOSAL.md:191:### Formal Derivation via Entropy-Regularized Noise Minimization[0m
[7m[0m  FINAL_PROPOSAL.md:192:[0m
> FINAL_PROPOSAL.md:193:> **注意范围**:以下推导在 **imperfect decoupling + 线性噪声近似** 这一 residual-noise 模型下成
立,**不是所有 prototype weighting 的无条件定理**。目的是证明 [7mSCPR[0m 的 softmax-over-cosine 形式是**该模型下的唯一最优**,[0m
[7m[0m不是启发式选择。[0m
[7m[0m  FINAL_PROPOSAL.md:194:[0m
[7m[0m  FINAL_PROPOSAL.md:195:**Setup**:分解原型为[0m
[7m[0m  FINAL_PROPOSAL.md:196:```[0m
[7m[0m  FINAL_PROPOSAL.md:197:p_c^j = p_c^* + eps_j[0m
[7m[0m  FINAL_PROPOSAL.md:198:```[0m
[7m[0m  FINAL_PROPOSAL.md:199:其中 `p_c^*` 是"理想风格无关类 c 原型",`eps_j` 是客户端 j 的风格残留,源自软正交约束 `cos²(z_sem[0m
[7m[0m, z_sty) → 0` 不能严格为 0(实测残余 cos ~ 0.05–0.1)。[0m
  FINAL_PROPOSAL.md:229:```
  FINAL_PROPOSAL.md:230:w_j^* ∝ exp(-c · (1 - cos(s_k, s_j)) / τ)
  FINAL_PROPOSAL.md:231:       ∝ exp(c · cos(s_k, s_j) / τ)
> FINAL_PROPOSAL.md:232:       = softmax_j(cos(s_k, s_j) / τ_[7mSCPR[0m),   τ_SCPR := τ / c[0m
[7m[0m  FINAL_PROPOSAL.md:233:```[0m
[7m[0m  FINAL_PROPOSAL.md:234:[0m
> FINAL_PROPOSAL.md:235:**结论**:**[7mSCPR[0m 的 softmax-over-cosine 是该 residual-noise 模型下 entropy-regularized noise 目[0m
[7m[0m标的 unique Boltzmann minimizer**,不是设计选择。[0m
[7m[0m  FINAL_PROPOSAL.md:236:[0m
[7m[0m  FINAL_PROPOSAL.md:237:**两极限**(自然过渡):[0m
> FINAL_PROPOSAL.md:238:- **Decouple 完美**(`eps_j ≡ 0`,`l_j ≡ 0`):`J(w)` 只剩熵项,极小化给出 uniform w → **[7mSCPR[0m = M3**[0m
[7m[0m(无缝 fallback,无切换成本)[0m
[7m[0m  FINAL_PROPOSAL.md:239:- **Decouple 不完美**(实际情形):style weighting 是最优噪声抑制[0m
[7m[0m  FINAL_PROPOSAL.md:240:[0m
[7m[0m  FINAL_PROPOSAL.md:241:### Conceptual Position(论文 Related Work 导出)[0m
[7m[0m  FINAL_PROPOSAL.md:242:[0m
> FINAL_PROPOSAL.md:243:[7mSCPR[0m 概念上位于 M3 与 SAS 之间:
> FINAL_PROPOSAL.md:244:- **[7mM3[0m**:无风格信息,所有 positive 等权 → PACS 良好,**没 exploit 风格邻域先验**[0m
[7m[0m  FINAL_PROPOSAL.md:245:- **SAS**:参数空间风格路由(修改 sem_head 权重)→ Office 单 outlier 胜,**PACS 全 outlier 退化为本[0m
[7m[0m地**(EXP-086)[0m
> FINAL_PROPOSAL.md:246:- **[7mSCPR[0m**:对齐目标风格加权 + **参数保持共享** → 两种 regime **均适用**[0m
[7m[0m  FINAL_PROPOSAL.md:247:[0m
[7m[0m  FINAL_PROPOSAL.md:248:这一中间位置的机制论证:[0m
[7m[0m  FINAL_PROPOSAL.md:249:- SAS 失败根因(PACS):所有 client 参数都退化到本地路径,破坏了跨域共识[0m
> FINAL_PROPOSAL.md:250:- [7mSCPR[0m 隔离设计:参数层 FedAvg 不变 → 保留跨域共识;仅对齐目标按风格加权 → 捕获风格邻域优势[0m
[7m[0m  FINAL_PROPOSAL.md:251:[0m
[7m[0m  FINAL_PROPOSAL.md:252:### Failure Modes and Diagnostics[0m
[7m[0m  FINAL_PROPOSAL.md:253:[0m
[7m[0m  FINAL_PROPOSAL.md:254:| Failure | Signal | Fallback |[0m
[7m[0m  FINAL_PROPOSAL.md:255:|---------|--------|----------|[0m
> FINAL_PROPOSAL.md:256:| Attention uniform 塌缩 | `H(w_k) > 0.95·log(K-1)` 持续 > 20 轮 | 降维 `s_k` 或调低 `τ_[7mSCPR[0m` |
> FINAL_PROPOSAL.md:257:| 数值 NaN | loss NaN / Inf | `τ_[7mSCPR[0m: 0.3 → 1.0` 软化 |
> FINAL_PROPOSAL.md:258:| PACS < [7mM3[0m | 3-seed AVG Best < 81.91% | 检查 self-mask 启用 / entropy |[0m
[7m[0m  FINAL_PROPOSAL.md:259:| **Outlier-ness ρ ≈ 0** | 机制未激活 | 方法证伪,方案 downgrade |[0m
[7m[0m  FINAL_PROPOSAL.md:260:[0m
> FINAL_PROPOSAL.md:261:**内置安全网**:uniform w 下 [7mSCPR[0m = M3,下界 = 81.91%(已验证)。[0m
[7m[0m  FINAL_PROPOSAL.md:262:[0m
[7m[0m  FINAL_PROPOSAL.md:263:### Novelty and Elegance Argument[0m
[7m[0m  FINAL_PROPOSAL.md:264:[0m
[7m[0m  FINAL_PROPOSAL.md:265:- **最小机制**:~30 行代码、0 新参数、1 新超参[0m
> FINAL_PROPOSAL.md:266:- **数学自洽**:单一公式,uniform 严格退化为 [7mM3[0m
[7m[0m  FINAL_PROPOSAL.md:267:- **理论根基**:entropy-regularized MaxEnt 推导(不是启发式选择)[0m
[7m[0m  FINAL_PROPOSAL.md:268:- **2×2 首次占格**:(Decouple, Style-share)[0m
[7m[0m  FINAL_PROPOSAL.md:269:- **叙事闭环**:首次补齐 FedDSA Share 章节缺口[0m
[7m[0m  FINAL_PROPOSAL.md:270:[0m
[7m[0m  FINAL_PROPOSAL.md:271:**Closest prior works**:[0m
> FINAL_PROPOSAL.md:272:1. **FedProto (AAAI 2022)**:single global-mean proto + 等权对齐 → [7mSCPR[0m 用域索引 multi-proto + s[0m
[7m[0mtyle weighting[0m
> FINAL_PROPOSAL.md:273:2. **FPL (CVPR 2023) / FedPLVM (NeurIPS 2024)**:cluster-based multi-proto,聚类 key 是原型自身几
何 → [7mSCPR[0m 的 key 是**客户端风格**(语义完全不同)[0m
> FINAL_PROPOSAL.md:274:3. **FedSTAR (2025)**:Transformer aggregator + FiLM 风格分离,但风格**严格本地** → [7mSCPR[0m 首次把风[0m
[7m[0m格作为**跨客户端 attention key**[0m
> FINAL_PROPOSAL.md:275:4. **FISC / PARDON (ICDCS 2025)**:共享风格但在**图像/混合特征空间 AdaIN**(EXP-059 已证伪)→ [7mSCPR[0m
[7m[0m 在**解耦表征空间** + 只改对齐 target[0m
> FINAL_PROPOSAL.md:276:5. **SAS(我们,未发表)**:参数空间路由,PACS 失败(EXP-086)→ [7mSCPR[0m 把路由下放到**原型层**,保留参数共[0m
[7m[0m享[0m
[7m[0m  FINAL_PROPOSAL.md:277:[0m
[7m[0m  FINAL_PROPOSAL.md:278:---[0m
[7m[0m  FINAL_PROPOSAL.md:279:[0m
[7m[0m  FINAL_PROPOSAL.md:280:## Claim-Driven Validation Sketch[0m
[7m[0m  FINAL_PROPOSAL.md:281:[0m
> FINAL_PROPOSAL.md:282:### Claim A(主):PACS 全 outlier,[7mSCPR[0m > M3 uniform[0m
[7m[0m  FINAL_PROPOSAL.md:283:[0m
[7m[0m  FINAL_PROPOSAL.md:284:- **数据集**:PACS(4 domains, 7 classes)[0m
[7m[0m  FINAL_PROPOSAL.md:285:- **对比**:[0m
[7m[0m  FINAL_PROPOSAL.md:286:  - A.1 FedDSA orth_only(Plan A baseline,80.41%)[0m
> FINAL_PROPOSAL.md:287:  - A.2 + [7mM3[0m domain-aware multi-pos(= SCPR uniform attention;对应 τ_SCPR → ∞,~81.91%)
> FINAL_PROPOSAL.md:288:  - A.3 **+ [7mSCPR[0m(τ_SCPR = 0.3, self-mask)**[0m
[7m[0m  FINAL_PROPOSAL.md:289:- **指标**:3-seed {2, 15, 333} mean AVG Best / Last / per-domain(重点看 Art、Sketch)[0m
[7m[0m  FINAL_PROPOSAL.md:290:- **预期**:A.3 ≥ A.2 + 0.5%,A.3 ≥ 81.5%[0m
[7m[0m  FINAL_PROPOSAL.md:291:- **决定性**:若 A.3 < A.2,style weighting 不如 uniform,方法证伪[0m
[7m[0m  FINAL_PROPOSAL.md:292:[0m
> FINAL_PROPOSAL.md:293:### Claim B(主):Office 单 outlier,[7mSCPR[0m ≥ SAS(不做参数个性化)[0m
[7m[0m  FINAL_PROPOSAL.md:294:[0m
[7m[0m  FINAL_PROPOSAL.md:295:- **数据集**:Office-Caltech10(4 domains, 10 classes;Caltech 是 outlier)[0m
[7m[0m  FINAL_PROPOSAL.md:296:- **对比**:[0m
[7m[0m  FINAL_PROPOSAL.md:297:  - B.1 FedDSA orth_only(baseline)[0m
[7m[0m  FINAL_PROPOSAL.md:298:  - B.2 + SAS(参数空间路由,已验证 89.82%)[0m
> FINAL_PROPOSAL.md:299:  - B.3 **+ [7mSCPR[0m(原型层路由,τ_SCPR = 0.3)**[0m
[7m[0m  FINAL_PROPOSAL.md:300:- **指标**:3-seed mean AVG Best / Last / Caltech-specific[0m
[7m[0m  FINAL_PROPOSAL.md:301:- **预期**:B.3 ≥ B.2[0m
> FINAL_PROPOSAL.md:302:- **决定性**:证明**原型层 ≥ 参数层**,且 [7mSCPR[0m **普适**两种 regime(SAS 只赢 Office)[0m
[7m[0m  FINAL_PROPOSAL.md:303:[0m
> FINAL_PROPOSAL.md:304:### Claim C(机制):Outlier-ness 相关性 + τ_[7mSCPR[0m 敏感性[0m
[7m[0m  FINAL_PROPOSAL.md:305:[0m
[7m[0m  FINAL_PROPOSAL.md:306:**主诊断(非 tautological)**:[0m
[7m[0m  FINAL_PROPOSAL.md:307:- 定义 `iso_k := 1 - mean_{j ≠ k} cos(s_k, s_j)`:客户端 k 的风格孤立度[0m
> FINAL_PROPOSAL.md:308:- 定义 `gain_k := acc_k^{[7mSCPR[0m} - acc_k^{M3}`:SCPR 相对 M3 的 per-client accuracy 改善[0m
[7m[0m  FINAL_PROPOSAL.md:309:- 报告 **Spearman ρ(iso_k, gain_k)**,预期 **ρ > 0**(outlier 客户端受益更多)[0m
> FINAL_PROPOSAL.md:310:- **Non-tautological**:`gain_k` 是 downstream accuracy,不是 cos 的函数;[7mM3[0m baseline 与 style 无[0m
[7m[0m关;此预测**可证伪**[0m
[7m[0m  FINAL_PROPOSAL.md:311:- 若 ρ ≈ 0,机制未激活 → 方法证伪[0m
[7m[0m  FINAL_PROPOSAL.md:312:[0m
[7m[0m  FINAL_PROPOSAL.md:313:**辅助**:[0m
> FINAL_PROPOSAL.md:314:- `τ_[7mSCPR[0m ∈ {0.1, 0.3, 1.0, 3.0}` 扫描,记录 H(w_k) 曲线与 attention heatmap[0m
[7m[0m  FINAL_PROPOSAL.md:315:- 预期最优 `τ ∈ [0.3, 1.0]`,H(w) 不长期靠边界[0m
[7m[0m  FINAL_PROPOSAL.md:316:[0m
[7m[0m  FINAL_PROPOSAL.md:317:**GPU 预算**:0 额外(per-client accuracy 与 style bank 均已在现有日志中)[0m
[7m[0m  FINAL_PROPOSAL.md:318:[0m
[7m[0m  FINAL_PROPOSAL.md:319:### 附录 composability check(非主 claim)[0m
[7m[0m  FINAL_PROPOSAL.md:320:[0m
> FINAL_PROPOSAL.md:321:- Office 上 [7mSCPR[0m + SAS 叠加[0m
[7m[0m  FINAL_PROPOSAL.md:322:- 1 config × 3 seeds[0m
[7m[0m  FINAL_PROPOSAL.md:323:- 只回答"两层路由是否正交",不进 headline[0m
[7m[0m  FINAL_PROPOSAL.md:324:[0m
[7m[0m  FINAL_PROPOSAL.md:325:---[0m
[7m[0m  FINAL_PROPOSAL.md:326:[0m
[7m[0m  FINAL_PROPOSAL.md:327:## Experiment Handoff Inputs(交给 /experiment-plan)[0m
  FINAL_PROPOSAL.md:328:
  FINAL_PROPOSAL.md:329:- **Must-prove claims**:A, B, C(含 outlier-ness ρ)
  FINAL_PROPOSAL.md:330:- **Must-run ablations**:
> FINAL_PROPOSAL.md:331:  - uniform attention(= [7mM3[0m 下界)[0m
> FINAL_PROPOSAL.md:332:  - τ_[7mSCPR[0m 扫描
> FINAL_PROPOSAL.md:333:  - [7mself-mask[0m 必须性(单测层面)
> FINAL_PROPOSAL.md:334:  - per-class [7mrenormal[0mize 正确性(单测)[0m
[7m[0m  FINAL_PROPOSAL.md:335:- **Critical datasets / metrics**:[0m
[7m[0m  FINAL_PROPOSAL.md:336:  - PACS AVG Best/Last + per-domain(Art, Sketch)[0m
[7m[0m  FINAL_PROPOSAL.md:337:  - Office AVG Best/Last + Caltech[0m
[7m[0m  FINAL_PROPOSAL.md:338:  - **ρ(iso_k, gain_k)**[0m
[7m[0m  FINAL_PROPOSAL.md:339:- **Highest-risk assumptions**:[0m
[7m[0m  FINAL_PROPOSAL.md:340:  1. `s_k` 区分度充足(若 H(w) 长期 ≈ log(K-1),降维 fallback)[0m
> FINAL_PROPOSAL.md:341:  2. [7mSCPR[0m ≥ M3(若否,方法证伪)[0m
[7m[0m  FINAL_PROPOSAL.md:342:  3. Burn-in 期(bank 空时的 uniform fallback)不破坏训练[0m
[7m[0m  FINAL_PROPOSAL.md:343:- **Unit tests**:[0m
[7m[0m  FINAL_PROPOSAL.md:344:  - K=4 时 `w_{k→k} = 0` 数值验证[0m
> FINAL_PROPOSAL.md:345:  - `τ_[7mSCPR[0m → ∞` 时 `w` 收敛到 uniform 1/3
> FINAL_PROPOSAL.md:346:  - `τ_[7mSCPR[0m → 0` 时 `w` 收敛到 one-hot[0m
[7m[0m  FINAL_PROPOSAL.md:347:[0m
[7m[0m  FINAL_PROPOSAL.md:348:---[0m
[7m[0m  FINAL_PROPOSAL.md:349:[0m
[7m[0m  FINAL_PROPOSAL.md:350:## Compute & Timeline Estimate[0m
[7m[0m  FINAL_PROPOSAL.md:351:[0m
[7m[0m  FINAL_PROPOSAL.md:352:- **GPU·h**:~60(3 天单卡 24GB)[0m
  FINAL_PROPOSAL.md:353:  - PACS 主:3 configs × 3 seeds ≈ 18 h
  FINAL_PROPOSAL.md:354:  - Office 主:3 configs × 3 seeds ≈ 18 h
  FINAL_PROPOSAL.md:355:  - τ 扫描:4 configs × 3 seeds ≈ 24 h
> FINAL_PROPOSAL.md:356:  - 附录([7mSCPR[0m + SAS):1 config × 3 seeds ≈ 6 h[0m
[7m[0m  FINAL_PROPOSAL.md:357:- **数据 / 标注**:0(PACS + Office-Caltech10 已就位)[0m
[7m[0m  FINAL_PROPOSAL.md:358:- **人力 timeline**:[0m
[7m[0m  FINAL_PROPOSAL.md:359:  - Day 1:实现 + 单测 + codex review[0m
[7m[0m  FINAL_PROPOSAL.md:360:  - Day 2–4:跑实验[0m
[7m[0m  FINAL_PROPOSAL.md:361:  - Day 5:回填 NOTE.md + 消融决策 + 附录实验[0m
[7m[0m  FINAL_PROPOSAL.md:362:  - **共 5 天 end-to-end**[0m
  FINAL_PROPOSAL.md:365:
  FINAL_PROPOSAL.md:366:## Implementation Note(附录,从主方法剥离)
  FINAL_PROPOSAL.md:367:
> FINAL_PROPOSAL.md:368:1. **首轮 bank 空**:客户端首轮 pack() 时 bank 无可用 `s_j`;此时 `w` 自动回退到 uniform 1/(K-1)(
= [7mM3[0m burn-in,1-2 round),**无需显式 warmup 超参**[0m
> FINAL_PROPOSAL.md:369:2. **PACS / Office 上 `A_k^c`**:每客户端持有所有类样本,`A_k^c = {j : j ≠ k}`,[7mrenormal[0mize 恒为 1[0m
[7m[0m/(K-1)[0m
> FINAL_PROPOSAL.md:370:3. **数值稳定**:使用 log-sum-exp softmax;`τ_[7mSCPR[0m` 最小 0.1 以避免数值爆炸[0m
[7m[0m  FINAL_PROPOSAL.md:371:4. **单测**:[0m
> FINAL_PROPOSAL.md:372:   - `w_{k→k} ≡ 0`([7mself-mask[0m 验证)
> FINAL_PROPOSAL.md:373:   - `τ_[7mSCPR[0m → ∞` 时 `w → uniform`
> FINAL_PROPOSAL.md:374:   - `τ_[7mSCPR[0m → 0` 时 `w → one-hot`[0m
[7m[0m  FINAL_PROPOSAL.md:375:5. **Outlier-ness 诊断 log**:每轮记录 `(iso_k, acc_k)` 到 `attention_log.jsonl`,离线计算相关性[0m
[7m[0m  FINAL_PROPOSAL.md:376:[0m
[7m[0m  FINAL_PROPOSAL.md:377:---[0m
[7m[0m  FINAL_PROPOSAL.md:378:[0m
[7m[0m  FINAL_PROPOSAL.md:379:**文档版本**:Round 4 refinement,GPT-5.4 R5 verdict **READY 9.1/10**[0m
> FINAL_PROPOSAL.md:380:**Refine session**:`2026-04-18_[7mSCPR[0m_v1`(5 轮审核,日期 2026-04-18 → 2026-04-19)[0m
[7m[0m  FINAL_PROPOSAL.md:381:**建议下一步**:`/experiment-plan` 细化执行路线,然后实现并跑 Claim A / B / C[0m


codex
The proposal is useful here because it states the intended class-conditional weights explicitly. I’m checking one more thing before concluding: whether the current implementation’s denominator and scheduling semantics actually match the “strictly reduce to M3” claim, or whether it accidentally leaves the old Plan A loss active on top.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "git -C 'D:\\桌面文件\\联邦学习\\FDSE_CVPR25' diff -- 'algorithm/feddsa_scheduled.py'" in D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-18_SCPR_v1
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-ChildItem -Path 'D:\\桌面文件\\联邦学习\\FDSE_CVPR25' -Recurse -File | Select-String -Pattern 'multi-positive|M3|scpr|_infonce' | Select-Object Path, LineNumber, Line | Format-Table -AutoSize" in D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-18_SCPR_v1
2026-04-18T12:15:43.314938Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 0.8 seconds
Output:
warning: Not a git repository. Use --no-index to compare two paths outside a working tree
usage: git diff --no-index [<options>] <path> <path>

Diff output format options
    -p, --patch           generate patch
    -s, --no-patch        suppress diff output
    -u                    generate patch
    -U, --unified[=<n>]   generate diffs with <n> lines context
    -W, --function-context
                          generate diffs with <n> lines context
    --raw                 generate the diff in raw format
    --patch-with-raw      synonym for '-p --raw'
    --patch-with-stat     synonym for '-p --stat'
    --numstat             machine friendly --stat
    --shortstat           output only the last line of --stat
    -X, --dirstat[=<param1,param2>...]
                          output the distribution of relative amount of changes for each sub-directory
    --cumulative          synonym for --dirstat=cumulative
    --dirstat-by-file[=<param1,param2>...]
                          synonym for --dirstat=files,param1,param2...
    --check               warn if changes introduce conflict markers or whitespace errors
    --summary             condensed summary such as creations, renames and mode changes
    --name-only           show only names of changed files
    --name-status         show only names and status of changed files
    --stat[=<width>[,<name-width>[,<count>]]]
                          generate diffstat
    --stat-width <width>  generate diffstat with a given width
    --stat-name-width <width>
                          generate diffstat with a given name width
    --stat-graph-width <width>
                          generate diffstat with a given graph width
    --stat-count <count>  generate diffstat with limited lines
    --compact-summary     generate compact summary in diffstat
    --binary              output a binary diff that can be applied
    --full-index          show full pre- and post-image object names on the "index" lines
    --color[=<when>]      show colored diff
    --ws-error-highlight <kind>
                          highlight whitespace errors in the 'context', 'old' or 'new' lines in the diff
    -z                    do not munge pathnames and use NULs as output field terminators in --raw or --numstat
    --abbrev[=<n>]        use <n> digits to display object names
    --src-prefix <prefix>
                          show the given source prefix instead of "a/"
    --dst-prefix <prefix>
                          show the given destination prefix instead of "b/"
    --line-prefix <prefix>
                          prepend an additional prefix to every line of output
    --no-prefix           do not show any source or destination prefix
    --inter-hunk-context <n>
                          show context between diff hunks up to the specified number of lines
    --output-indicator-new <char>
                          specify the character to indicate a new line instead of '+'
    --output-indicator-old <char>
                          specify the character to indicate an old line instead of '-'
    --output-indicator-context <char>
                          specify the character to indicate a context instead of ' '

Diff rename options
    -B, --break-rewrites[=<n>[/<m>]]
                          break complete rewrite changes into pairs of delete and create
    -M, --find-renames[=<n>]
                          detect renames
    -D, --irreversible-delete
                          omit the preimage for deletes
    -C, --find-copies[=<n>]
                          detect copies
    --find-copies-harder  use unmodified files as source to find copies
    --no-renames          disable rename detection
    --rename-empty        use empty blobs as rename source
    --follow              continue listing the history of a file beyond renames
    -l <n>                prevent rename/copy detection if the number of rename/copy targets exceeds given limit

Diff algorithm options
    --minimal             produce the smallest possible diff
    -w, --ignore-all-space
                          ignore whitespace when comparing lines
    -b, --ignore-space-change
                          ignore changes in amount of whitespace
    --ignore-space-at-eol
                          ignore changes in whitespace at EOL
    --ignore-cr-at-eol    ignore carrier-return at the end of line
    --ignore-blank-lines  ignore changes whose lines are all blank
    -I, --ignore-matching-lines <regex>
                          ignore changes whose all lines match <regex>
    --indent-heuristic    heuristic to shift diff hunk boundaries for easy reading
    --patience            generate diff using the "patience diff" algorithm
    --histogram           generate diff using the "histogram diff" algorithm
    --diff-algorithm <algorithm>
                          choose a diff algorithm
    --anchored <text>     generate diff using the "anchored diff" algorithm
    --word-diff[=<mode>]  show word diff, using <mode> to delimit changed words
    --word-diff-regex <regex>
                          use <regex> to decide what a word is
    --color-words[=<regex>]
                          equivalent to --word-diff=color --word-diff-regex=<regex>
    --color-moved[=<mode>]
                          moved lines of code are colored differently
    --color-moved-ws <mode>
                          how white spaces are ignored in --color-moved

Other diff options
    --relative[=<prefix>]
                          when run from subdir, exclude changes outside and show relative paths
    -a, --text            treat all files as text
    -R                    swap two inputs, reverse the diff
    --exit-code           exit with 1 if there were differences, 0 otherwise
    --quiet               disable all output of the program
    --ext-diff            allow an external diff helper to be executed
    --textconv            run external text conversion filters when comparing binary files
    --ignore-submodules[=<when>]
                          ignore changes to submodules in the diff generation
    --submodule[=<format>]
                          specify how differences in submodules are shown
    --ita-invisible-in-index
                          hide 'git add -N' entries from the index
    --ita-visible-in-index
                          treat 'git add -N' entries as real in the index
    -S <string>           look for differences that change the number of occurrences of the specified string
    -G <regex>            look for differences that change the number of occurrences of the specified regex
    --pickaxe-all         show all changes in the changeset with -S or -G
    --pickaxe-regex       treat <string> in -S as extended POSIX regular expression
    -O <file>             control the order in which files appear in the output
    --rotate-to <path>    show the change in the specified path first
    --skip-to <path>      skip the output to the specified path
    --find-object <object-id>
                          look for differences that change the number of occurrences of the specified object
    --diff-filter [(A|C|D|M|R|T|U|X|B)...[*]]
                          select files by diff type
    --output <file>       output to a specific file


 exited 1 in 779ms:
warning: Not a git repository. Use --no-index to compare two paths outside a working tree
usage: git diff --no-index [<options>] <path> <path>

Diff output format options
    -p, --patch           generate patch
    -s, --no-patch        suppress diff output
    -u                    generate patch
    -U, --unified[=<n>]   generate diffs with <n> lines context
    -W, --function-context
                          generate diffs with <n> lines context
    --raw                 generate the diff in raw format
    --patch-with-raw      synonym for '-p --raw'
    --patch-with-stat     synonym for '-p --stat'
    --numstat             machine friendly --stat
    --shortstat           output only the last line of --stat
    -X, --dirstat[=<param1,param2>...]
                          output the distribution of relative amount of changes for each sub-directory
    --cumulative          synonym for --dirstat=cumulative
    --dirstat-by-file[=<param1,param2>...]
                          synonym for --dirstat=files,param1,param2...
    --check               warn if changes introduce conflict markers or whitespace errors
    --summary             condensed summary such as creations, renames and mode changes
    --name-only           show only names of changed files
    --name-status         show only names and status of changed files
    --stat[=<width>[,<name-width>[,<count>]]]
                          generate diffstat
    --stat-width <width>  generate diffstat with a given width
    --stat-name-width <width>
                          generate diffstat with a given name width
    --stat-graph-width <width>
                          generate diffstat with a given graph width
    --stat-count <count>  generate diffstat with limited lines
    --compact-summary     generate compact summary in diffstat
    --binary              output a binary diff that can be applied
    --full-index          show full pre- and post-image object names on the "index" lines
    --color[=<when>]      show colored diff
    --ws-error-highlight <kind>
                          highlight whitespace errors in the 'context', 'old' or 'new' lines in the diff
    -z                    do not munge pathnames and use NULs as output field terminators in --raw or --numstat
    --abbrev[=<n>]        use <n> digits to display object names
    --src-prefix <prefix>
                          show the given source prefix instead of "a/"
    --dst-prefix <prefix>
                          show the given destination prefix instead of "b/"
    --line-prefix <prefix>
                          prepend an additional prefix to every line of output
    --no-prefix           do not show any source or destination prefix
    --inter-hunk-context <n>
                          show context between diff hunks up to the specified number of lines
    --output-indicator-new <char>
                          specify the character to indicate a new line instead of '+'
    --output-indicator-old <char>
                          specify the character to indicate an old line instead of '-'
    --output-indicator-context <char>
                          specify the character to indicate a context instead of ' '

Diff rename options
    -B, --break-rewrites[=<n>[/<m>]]
                          break complete rewrite changes into pairs of delete and create
    -M, --find-renames[=<n>]
                          detect renames
    -D, --irreversible-delete
                          omit the preimage for deletes
    -C, --find-copies[=<n>]
                          detect copies
    --find-copies-harder  use unmodified files as source to find copies
    --no-renames          disable rename detection
    --rename-empty        use empty blobs as rename source
    --follow              continue listing the history of a file beyond renames
    -l <n>                prevent rename/copy detection if the number of rename/copy targets exceeds given limit

Diff algorithm options
    --minimal             produce the smallest possible diff
    -w, --ignore-all-space
                          ignore whitespace when comparing lines
    -b, --ignore-space-change
                          ignore changes in amount of whitespace
    --ignore-space-at-eol
                          ignore changes in whitespace at EOL
    --ignore-cr-at-eol    ignore carrier-return at the end of line
    --ignore-blank-lines  ignore changes whose lines are all blank
    -I, --ignore-matching-lines <regex>
                          ignore changes whose all lines match <regex>
    --indent-heuristic    heuristic to shift diff hunk boundaries for easy reading
    --patience            generate diff using the "patience diff" algorithm
    --histogram           generate diff using the "histogram diff" algorithm
    --diff-algorithm <algorithm>
                          choose a diff algorithm
    --anchored <text>     generate diff using the "anchored diff" algorithm
    --word-diff[=<mode>]  show word diff, using <mode> to delimit changed words
    --word-diff-regex <regex>
                          use <regex> to decide what a word is
    --color-words[=<regex>]
                          equivalent to --word-diff=color --word-diff-regex=<regex>
    --color-moved[=<mode>]
                          moved lines of code are colored differently
    --color-moved-ws <mode>
                          how white spaces are ignored in --color-moved

Other diff options
    --relative[=<prefix>]
                          when run from subdir, exclude changes outside and show relative paths
    -a, --text            treat all files as text
    -R                    swap two inputs, reverse the diff
    --exit-code           exit with 1 if there were differences, 0 otherwise
    --quiet               disable all output of the program
    --ext-diff            allow an external diff helper to be executed
    --textconv            run external text conversion filters when comparing binary files
    --ignore-submodules[=<when>]
                          ignore changes to submodules in the diff generation
    --submodule[=<format>]
                          specify how differences in submodules are shown
    --ita-invisible-in-index
                          hide 'git add -N' entries from the index
    --ita-visible-in-index
                          treat 'git add -N' entries as real in the index
    -S <string>           look for differences that change the number of occurrences of the specified string
    -G <regex>            look for differences that change the number of occurrences of the specified regex
    --pickaxe-all         show all changes in the changeset with -S or -G
    --pickaxe-regex       treat <string> in -S as extended POSIX regular expression
    -O <file>             control the order in which files appear in the output
    --rotate-to <path>    show the change in the specified path first
    --skip-to <path>      skip the output to the specified path
    --find-object <object-id>
                          look for differences that change the number of occurrences of the specified object
    --diff-filter [(A|C|D|M|R|T|U|X|B)...[*]]
                          select files by diff type
    --output <file>       output to a specific file


 succeeded in 2668ms:

[32;1mPath                                                                                  [0m[32;1m LineNumber[0m[32;1m Line[0m
[32;1m----                                                                                  [0m [32;1m----------[0m [32;1m----[0m
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_adaptive.py                                   3 Merges feddsa.py (ba…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_adaptive.py                                   7   M3: Domain-aware p…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_adaptive.py                                  12   adaptive_mode: 0=f…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_adaptive.py                                 140             'md': 1,…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_adaptive.py                                 171         self.domain_…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_adaptive.py                                 286         # 4. Store d…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_adaptive.py                                 512                 loss…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_adaptive.py                                 514                 loss…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_adaptive.py                                 516                 loss…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_adaptive.py                                 661             # M3-onl…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_adaptive.py                                 828     # ---- Domain-Aw…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_adaptive.py                                 830     def _infonce_dom…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_adaptive.py                                 831         """Multi-pos…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_adaptive.py                                 841             return s…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_adaptive.py                                 871     def _infonce_glo…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_augdown.py                                  117                 loss…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_augschedule.py                              119                 loss…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_auto.py                                     229                 loss…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_auto.py                                     310     def _infonce_los…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_consensus_kl.py                             149                 loss…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_domain_aware.py                               3 Key change: Replace …
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_domain_aware.py                               8 - Domain-aware: keep…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_domain_aware.py                             223 # Client — Domain-Aw…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_domain_aware.py                             320                 loss…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_domain_aware.py                             322                 loss…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_domain_aware.py                             372     def _infonce_dom…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_domain_aware.py                             374         Multi-positi…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_domain_aware.py                             390             return s…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_domain_aware.py                             399         # Multi-posi…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_domain_aware.py                             423     def _infonce_glo…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_gradual.py                                  505                 loss…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_gradual.py                                  507                 loss…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_gradual.py                                  714     # Domain-Aware S…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_gradual.py                                  717     def _infonce_dom…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_gradual.py                                  718         """Multi-pos…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_gradual.py                                  732             return s…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_gradual.py                                  763     def _infonce_glo…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_multilayer.py                               331                 loss…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_noaug_late.py                                96                 loss…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_pcgrad.py                                   138                 loss…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_plus.py                                     264                 loss…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_plus.py                                     365     def _infonce_los…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                134             # ------…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                136             # (obsid…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                137             'scpr': …
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                138                     …
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                139                     …
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                140                     …
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                143             'scpr_ta…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                162         # SCPR alias…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                163         self.scpr_mo…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                164         self.scpr_ta…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                176         # SCPR: per-…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                205             # SCPR p…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                206             c.scpr_m…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                207             c.scpr_t…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                293         # SCPR: comp…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                294         # Returns No…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                295         scpr_payload…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                296         if self.scpr…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                297             scpr_pay…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                304             'scpr_pa…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                387     def _compute_scp…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                388         """Compute S…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                391         scpr_mode=1 …
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                393         scpr_mode=2 …
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                394             w_{k->j}…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                432         if self.scpr…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                433             # Unifor…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                436         elif self.sc…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                437             # Style-…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                443             sims_t =…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                483         # SCPR: upda…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                484         if self.scpr…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                651         # SCPR state…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                652         self.scpr_mo…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                653         self.scpr_ta…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                654         self.scpr_pa…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                661         # SCPR: extr…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                662         self.scpr_pa…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                813                     …
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                815                     …
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                822             # L_aug …
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                832             # SCPR l…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                834             # scpr_m…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                835             if self.…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                836                 loss…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                837                 loss…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                                974     def _infonce_los…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                               1002     def _infonce_alp…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                               1050     # SCPR: Self-Mas…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                               1053     def _scpr_loss(s…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                               1054         """Self-mask…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                               1066         Under scpr_m…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                               1067         multi-positi…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                               1068         scpr_mode=2 …
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                               1072         if self.scpr…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                               1074         weights = se…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_scheduled.py                               1075         domain_proto…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_stable.py                                   248                 loss…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_stable.py                                   340     def _infonce_los…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_stylehead_bank.py                            79                 loss…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_triplet.py                                    9 This only changes _i…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_triplet.py                                   53     def _infonce_los…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa_vae.py                                      190                 loss…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa.py                                          317                 loss…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\feddsa.py                                          418     def _infonce_los…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\__pycache__\feddsa_adaptive.cpython-38.pyc          13 Merges feddsa.py (ba…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\__pycache__\feddsa_adaptive.cpython-38.pyc          17   M3: Domain-aware p…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\__pycache__\feddsa_adaptive.cpython-38.pyc          21   adaptive_mode: 0=f…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\__pycache__\feddsa_adaptive.cpython-38.pyc         190 learning_rater�   r�…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\__pycache__\feddsa_adaptive.cpython-38.pyc         254 | S )	z9Multi-posi…[0m
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\__pycache__\feddsa_adaptive.cpython-38.pyc         260 zClient._infonce…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\__pycache__\feddsa_adaptive.cpython-38.pyc         272 Nc               …
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\__pycache__\feddsa_adaptive.cpython-38.pyc         274   Zvalid_tZ	target…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\__pycache__\feddsa_gradual.cpython-38.pyc          194 batch_sizer�   r�   …
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\__pycache__\feddsa_gradual.cpython-38.pyc          264 | S )	z�Multi-posi…[0m
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\__pycache__\feddsa_gradual.cpython-38.pyc          268         r   r   c…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\__pycache__\feddsa_gradual.cpython-38.pyc          274 zClient._infonce…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\__pycache__\feddsa_gradual.cpython-38.pyc          286 z?InfoNCE: pull towa…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\__pycache__\feddsa_gradual.cpython-38.pyc          287 cross_entropy)r9   …
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\__pycache__\feddsa_scheduled.cpython-38.pyc        258 batch_size�rangeZg…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\__pycache__\feddsa_scheduled.cpython-38.pyc        259 _infonce_loss�_mse_…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\__pycache__\feddsa_scheduled.cpython-38.pyc        315 z?InfoNCE: pull towa…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\__pycache__\feddsa_scheduled.cpython-38.pyc        317 cross_entropy)r&   …
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\__pycache__\feddsa_scheduled.cpython-38.pyc        331         Requires cos…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\__pycache__\feddsa_scheduled.cpython-38.pyc        332   rd  rn  r�  rM…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\__pycache__\feddsa.cpython-311.pyc                 157 _infonce_lossrg   rh…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\__pycache__\feddsa.cpython-311.pyc                 209 S rO   r{   )r}   r…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\__pycache__\feddsa.cpython-311.pyc                 213 r=   r   r�   r�   …
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\__pycache__\feddsa.cpython-311.pyc                 214 cross_entropy)r:   …
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\__pycache__\feddsa.cpython-38.pyc                  122 _infonce_lossr\   r]…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\__pycache__\feddsa.cpython-38.pyc                  160 z?InfoNCE: pull towa…
D:\桌面文件\联邦学习\FDSE_CVPR25\algorithm\__pycache__\feddsa.cpython-38.pyc                  161 cross_entropy)r8   …
D:\桌面文件\联邦学习\FDSE_CVPR25\config\office\feddsa_074_m2_tau02.yml                          1 # EXP-074 Office: ta…
D:\桌面文件\联邦学习\FDSE_CVPR25\config\office\feddsa_074_m3_tau02.yml                          1 # EXP-074 Office: ta…
D:\桌面文件\联邦学习\FDSE_CVPR25\config\pacs\feddsa_072d.yml                                    1 # EXP-072d: M3-only …
D:\桌面文件\联邦学习\FDSE_CVPR25\config\pacs\feddsa_072d.yml                                    2 # adaptive_mode=2 (M…
D:\桌面文件\联邦学习\FDSE_CVPR25\config\pacs\feddsa_072d.yml                                   15 - 2      # adaptive_…
D:\桌面文件\联邦学习\FDSE_CVPR25\config\pacs\feddsa_073.yml                                     1 # EXP-073: M1+M3 Ful…
D:\桌面文件\联邦学习\FDSE_CVPR25\config\pacs\feddsa_073.yml                                     2 # adaptive_mode=3 (M…
D:\桌面文件\联邦学习\FDSE_CVPR25\config\pacs\feddsa_073.yml                                    15 - 3      # adaptive_…
D:\桌面文件\联邦学习\FDSE_CVPR25\config\pacs\feddsa_074_m2_tau02.yml                            1 # EXP-074: tau=0.2 i…
D:\桌面文件\联邦学习\FDSE_CVPR25\config\pacs\feddsa_074_m2_tau02.yml                            2 # Goal: M3 with stab…
D:\桌面文件\联邦学习\FDSE_CVPR25\config\pacs\feddsa_074_m2_tau02.yml                            3 # adaptive_mode=2 (M…
D:\桌面文件\联邦学习\FDSE_CVPR25\config\pacs\feddsa_074_m2_tau02.yml                           16 - 2      # adaptive_…
D:\桌面文件\联邦学习\FDSE_CVPR25\config\pacs\feddsa_074_m3_tau02.yml                            1 # EXP-074: tau=0.2 i…
D:\桌面文件\联邦学习\FDSE_CVPR25\config\pacs\feddsa_074_m3_tau02.yml                            3 # adaptive_mode=3 (M…
D:\桌面文件\联邦学习\FDSE_CVPR25\config\pacs\feddsa_074_m3_tau02.yml                           16 - 3      # adaptive_…
D:\桌面文件\联邦学习\FDSE_CVPR25\config\pacs\feddsa_gradual_deep.yml                            1 # EXP-075: Gradual t…
D:\桌面文件\联邦学习\FDSE_CVPR25\config\pacs\feddsa_gradual_noaug.yml                           1 # EXP-075: Gradual t…
D:\桌面文件\联邦学习\FDSE_CVPR25\config\pacs\feddsa_gradual_shallow.yml                         1 # EXP-075: Gradual t…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\domainnet_c6\res.png                                     82 ���Y�⤓� ��{������d…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\domainnet_c6\res.png                                    182 ���K����`Ϟ=L�4����l…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\domainnet_c6\res.png                                    790 ��M��w��wGI���<<�,…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\domainnet_c6\res.png                                    902 <l݂tШ�_���X���…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\domainnet_c6\res.png                                   1143 wfv�|�@E���9~r����r…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\domainnet_c6\res.png                                   1967 �=�)�!8F;T=���-��…[0m
D:\桌面文件\联邦学习\FDSE_CVPR25\task\domainnet_c6\res.png                                   2196 ��`7�,�b��b߾}N�Yծq�…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\domainnet_c6\res.png                                   2511 �$Q�T��b���ӅB��J�J…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\domainnet_c6\res.png                                   2703 �(��;w��3�<�d2Iss…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\domainnet_c6\res.png                                   2802 F4mZgZ��)j��êJ���h…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\domainnet_c6\res.png                                   2878 Ap#;Ǽ,�d�Yt]�X,��…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\domainnet_c6\res.png                                   2979 ��Mttt0�w>�\�kt�…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\domainnet_c6\res.png                                   3205 J�:C� b�	�x� 䀪i�8…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\domainnet_c6\res.png                                   3217 �T�=�侀��oT�?����…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\domainnet_c6\res.png                                   3297 �{,�?��"SRZLX���h�…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\domainnet_c6\res.png                                   3351 و���\��|9����lw��0M…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\domainnet_c6\res.png                                   3376 SQQN�&�J��u��q�͛…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\domainnet_c6\res.png                                   3406 c�24Nhfd KG�nL�b``�`…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\domainnet_c6\res.png                                   3532 0���L&E �~\������4…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                             179 ^̆��5�]Q+���6�k�PYQ…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                             340 �я~��n���n:��Ȉ��7��…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                             447 �^~���ݜ=7����BI�� <…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                             479 �k������}|���.���h…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                             823 }���Op�aIQP�W)Q�D2Y…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                             840 ��|�d1n��y�?�<�=�…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                             864 Tx�V&�b����5T�(�X�|…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                            1077 �R�"��Xm�sP}�����…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                            1093 �T?�m3�����ip
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                            1110 l�A3t,��v�T(4]�/dq…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                            1154 È�p�B6l���~�+`p�3��m…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                            1212 ���a�\��^t���W�x���…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                            1401 ��𨬞"0N�m�k�������l…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                            1823 A�=�S�-h��>�ZdY3�V…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                            2133 J�4�C��»dW*'�4h�<�…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                            2146 h]���#lӐ����6iSw…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                            2518 ���ۻ����ݏ1C͠���C��…[0m
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                            2674 RF��!>&D*[�@e$i\t…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                            3023 [o��P7ͤ�)����.>Y�…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                            3034 E�y"8m3	��l`G�9%KC…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                            3114 󔉱Cա�5�A�C��^I�L�…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                            3156 �gC�C��n?3�Bv�)�Yl…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                            3312 �+r�D����x��/rrr���…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                            3497 ��b�ꚜ�m3�DCibr�T�…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                            3616 :�ޝ1M����V+��?�|��…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                            3861 �{/��G���#��x��]ތ…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                            3867 O�ٵ+�<�b��Rq}}e)��…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                            3871 �v���x��?f}2��|e�…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\office_caltech10_c4\res.png                            3921 ���aSU�RP�����-�l…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\PACS_c4\res.png                                          44 ���2mڴ�Ҷ�:������o…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\PACS_c4\res.png                                         165 H$**5^lC�!ק�za�<�…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\PACS_c4\res.png                                         359 Y��'M3��B�9|?��R�…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\PACS_c4\res.png                                         630 b��֘����h��CHI���IM3…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\PACS_c4\res.png                                         786 �I�e�nh�Xl� ɨT5Y1��…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\PACS_c4\res.png                                         823 �'�qDg�W�za�������…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\PACS_c4\res.png                                         849 g������y������$9…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\PACS_c4\res.png                                         911 [���s���+�	�{��{��…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\PACS_c4\res.png                                        1113 (pB9_wp�A<�BsNc\ …
D:\桌面文件\联邦学习\FDSE_CVPR25\task\PACS_c4\res.png                                        1125 �w������b�<zǝ"��ϋ��…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\PACS_c4\res.png                                        1313 ��>j��2��ޠ�9/z){(s…[0m
D:\桌面文件\联邦学习\FDSE_CVPR25\task\PACS_c4\res.png                                        1428 �Q��f�4��2��I�N��…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\PACS_c4\res.png                                        1544 twv��>���Q,���~�%…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\PACS_c4\res.png                                        1698 �ɰ���M33��^o�4��@+�w…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\PACS_c4\res.png                                        1877 :ݡ���}� ��Qx"�C(C…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\PACS_c4\res.png                                        2193 d��#PJ�̓B��{�#�G��/…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\PACS_c4\res.png                                        2365 �P��CE�tIz-l�`��N…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\PACS_c4\res.png                                        2412 /����C�:�5<��.��g…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\PACS_c4\res.png                                        2451 ��n��e�F�Po4�lN�[ꍔ,…
D:\桌面文件\联邦学习\FDSE_CVPR25\task\PACS_c4\res.png                                        2459 ҆�V���b|��e��bӜ���@…


codex
I found the existing domain-aware multi-positive implementation in sibling algorithms. I’m comparing SCPR against that reference now, because the `scpr=1` path is only correct if it collapses to the same loss, not just a superficially similar weighting.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$i=1; Get-Content -Path '"'D:\\桌面文件\\联邦学习\\FDSE_CVPR25\\algorithm\\feddsa_adaptive.py' | ForEach-Object { '{0,5}: {1}' -f "'$i, $_; $i++ } | Select-Object -Skip 820 -First 80' in D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-18_SCPR_v1
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$i=1; Get-Content -Path '"'D:\\桌面文件\\联邦学习\\FDSE_CVPR25\\algorithm\\feddsa_domain_aware.py' | ForEach-Object { '{0,5}: {1}' -f "'$i, $_; $i++ } | Select-Object -Skip 360 -First 80' in D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-18_SCPR_v1
 succeeded in 800ms:
  821: 
  822:             # Residual FiLM: z_sem + gate * (gamma * z_sem + beta)
  823:             z_sem_film[i] = z_sem[i] + gate * (gamma * z_sem[i] + beta)
  824:             augmented = True
  825: 
  826:         return z_sem_film if augmented else None
  827: 
  828:     # ---- Domain-Aware InfoNCE (M3) ----
  829: 
  830:     def _infonce_domain_aware(self, z_sem, y):
  831:         """Multi-positive SupCon InfoNCE with per-domain prototypes."""
  832:         entries = []
  833:         entry_classes = []
  834:         for key in sorted(self.domain_protos.keys()):
  835:             cls = key[0]
  836:             proto = self.domain_protos[key]
  837:             entries.append(proto)
  838:             entry_classes.append(cls)
  839: 
  840:         if len(entries) < 2:
  841:             return self._infonce_global(z_sem, y)
  842: 
  843:         proto_matrix = torch.stack([p.to(z_sem.device) for p in entries])
  844:         proto_labels = torch.tensor(entry_classes, device=z_sem.device)
  845: 
  846:         z_n = F.normalize(z_sem, dim=1)
  847:         p_n = F.normalize(proto_matrix, dim=1)
  848:         logits = z_n @ p_n.T / self.tau
  849: 
  850:         loss = torch.tensor(0.0, device=z_sem.device)
  851:         count = 0
  852: 
  853:         for i in range(y.size(0)):
  854:             label = y[i].item()
  855:             pos_mask = (proto_labels == label)
  856:             n_pos = pos_mask.sum().item()
  857:             if n_pos == 0:
  858:                 continue
  859: 
  860:             log_denom = torch.logsumexp(logits[i], dim=0)
  861:             pos_logits = logits[i][pos_mask]
  862:             loss += (-pos_logits + log_denom).sum() / n_pos
  863:             count += 1
  864: 
  865:         if count == 0:
  866:             return torch.tensor(0.0, device=z_sem.device)
  867:         return loss / count
  868: 
  869:     # ---- Global InfoNCE (fallback) ----
  870: 
  871:     def _infonce_global(self, z_sem, y):
  872:         available = sorted([c for c, p in self.global_protos.items() if p is not None])
  873:         if len(available) < 2:
  874:             return torch.tensor(0.0, device=z_sem.device)
  875: 
  876:         proto_matrix = torch.stack([self.global_protos[c].to(z_sem.device) for c in available])
  877:         class_to_idx = {c: i for i, c in enumerate(available)}
  878: 
  879:         z_n = F.normalize(z_sem, dim=1)
  880:         p_n = F.normalize(proto_matrix, dim=1)
  881:         logits = (z_n @ p_n.T) / self.tau
  882: 
  883:         targets = []
  884:         valid = []
  885:         for i in range(y.size(0)):
  886:             label = y[i].item()
  887:             if label in class_to_idx:
  888:                 targets.append(class_to_idx[label])
  889:                 valid.append(i)
  890: 
  891:         if len(valid) == 0:
  892:             return torch.tensor(0.0, device=z_sem.device)
  893: 
  894:         valid_t = torch.tensor(valid, device=z_sem.device)
  895:         targets_t = torch.tensor(targets, device=z_sem.device, dtype=torch.long)
  896:         return F.cross_entropy(logits[valid_t], targets_t)
  897: 
  898:     # ---- Decoupling losses ----
  899: 
  900:     def _decouple_loss(self, z_sem, z_sty):

 succeeded in 756ms:
  361:         self._local_proto_counts = proto_count
  362: 
  363:         if style_n > 1:
  364:             mu = style_sum / style_n
  365:             var = style_sq_sum / style_n - mu ** 2
  366:             self._local_style_stats = (mu, var.clamp(min=1e-6).sqrt())
  367:         else:
  368:             self._local_style_stats = None
  369: 
  370:     # ---- Domain-Aware InfoNCE (KEY INNOVATION) ----
  371: 
  372:     def _infonce_domain_aware(self, z_sem, y):
  373:         """
  374:         Multi-positive InfoNCE with per-domain prototypes.
  375:         For sample with label c:
  376:           Positives: all domain-specific prototypes of class c
  377:           Negatives: all prototypes of classes != c
  378:         This avoids "semantic dilution" from global averaging.
  379:         """
  380:         # Build proto matrix from domain_protos: (class, client_id) -> proto
  381:         entries = []
  382:         entry_classes = []
  383:         for key in sorted(self.domain_protos.keys()):
  384:             cls = key[0]
  385:             proto = self.domain_protos[key]
  386:             entries.append(proto)
  387:             entry_classes.append(cls)
  388: 
  389:         if len(entries) < 2:
  390:             return self._infonce_global(z_sem, y)
  391: 
  392:         proto_matrix = torch.stack([p.to(z_sem.device) for p in entries])  # [K, d]
  393:         proto_labels = torch.tensor(entry_classes, device=z_sem.device)  # [K]
  394: 
  395:         z_n = F.normalize(z_sem, dim=1)       # [B, d]
  396:         p_n = F.normalize(proto_matrix, dim=1) # [K, d]
  397:         logits = z_n @ p_n.T / self.tau        # [B, K]
  398: 
  399:         # Multi-positive SupCon-style loss
  400:         loss = torch.tensor(0.0, device=z_sem.device)
  401:         count = 0
  402: 
  403:         for i in range(y.size(0)):
  404:             label = y[i].item()
  405:             pos_mask = (proto_labels == label)
  406:             n_pos = pos_mask.sum().item()
  407: 
  408:             if n_pos == 0:
  409:                 continue
  410: 
  411:             # log-sum-exp over ALL prototypes (denominator)
  412:             log_denom = torch.logsumexp(logits[i], dim=0)
  413: 
  414:             # Average over all positive prototypes
  415:             pos_logits = logits[i][pos_mask]
  416:             loss += (-pos_logits + log_denom).sum() / n_pos
  417:             count += 1
  418: 
  419:         if count == 0:
  420:             return torch.tensor(0.0, device=z_sem.device)
  421:         return loss / count
  422: 
  423:     def _infonce_global(self, z_sem, y):
  424:         """Fallback: original single-prototype InfoNCE."""
  425:         available = sorted([c for c, p in self.global_protos.items() if p is not None])
  426:         if len(available) < 2:
  427:             return torch.tensor(0.0, device=z_sem.device)
  428: 
  429:         proto_matrix = torch.stack([self.global_protos[c].to(z_sem.device) for c in available])
  430:         class_to_idx = {c: i for i, c in enumerate(available)}
  431: 
  432:         z_n = F.normalize(z_sem, dim=1)
  433:         p_n = F.normalize(proto_matrix, dim=1)
  434:         logits = (z_n @ p_n.T) / self.tau
  435: 
  436:         targets = []
  437:         valid = []
  438:         for i in range(y.size(0)):
  439:             label = y[i].item()
  440:             if label in class_to_idx:

codex
The reference M3 implementation confirms something important: its denominator is the standard SupCon one over all prototypes. I’m now checking whether the existing domain-aware path includes self as a positive, because that determines whether SCPR’s `scpr=1` branch is a true reduction or only a modified/self-masked variant.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$i=1; Get-Content -Path '"'D:\\桌面文件\\联邦学习\\FDSE_CVPR25\\algorithm\\feddsa_adaptive.py' | ForEach-Object { '{0,5}: {1}' -f "'$i, $_; $i++ } | Select-Object -First 340' in D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-18_SCPR_v1
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$i=1; Get-Content -Path '"'D:\\桌面文件\\联邦学习\\FDSE_CVPR25\\algorithm\\feddsa_domain_aware.py' | ForEach-Object { '{0,5}: {1}' -f "'$i, $_; $i++ } | Select-Object -First 240' in D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-18_SCPR_v1
 succeeded in 879ms:
    1: """
    2: FedDSA-Adaptive: Adaptive Augmentation + Domain-Aware Prototypes
    3: Merges feddsa.py (base) + feddsa_domain_aware.py (M3) + new adaptive aug (M1).
    4: 
    5: Key changes vs base FedDSA:
    6:   M1: Adaptive augmentation strength — gap from z_sty stats → per-client alpha
    7:   M3: Domain-aware prototype alignment — per-(class, client) protos + SupCon InfoNCE
    8:   M4: Dual alignment — L_intra (own-domain cosine) + L_cross (cross-domain InfoNCE)
    9:   M5: M4 + style utilization — L_sty_contrast (domain-contrastive on z_sty)
   10: 
   11: Modes controlled by algo_para:
   12:   adaptive_mode: 0=fixed_alpha, 1=adaptive (M1), 2=M3-only, 3=M1+M3 full, 4=M4 dual, 5=M4+style
   13:   fixed_alpha_value: used when adaptive_mode=0
   14: """
   15: import os
   16: import copy
   17: import collections
   18: import numpy as np
   19: import torch
   20: import torch.nn as nn
   21: import torch.nn.functional as F
   22: import flgo.algorithm.fedbase
   23: import flgo.utils.fmodule as fuf
   24: from collections import OrderedDict
   25: 
   26: 
   27: # ============================================================
   28: # Model: AlexNet backbone + dual-head (same as base FedDSA)
   29: # ============================================================
   30: 
   31: class AlexNetEncoder(nn.Module):
   32:     def __init__(self):
   33:         super().__init__()
   34:         self.features = nn.Sequential(OrderedDict([
   35:             ('conv1', nn.Conv2d(3, 64, kernel_size=11, stride=4, padding=2)),
   36:             ('bn1', nn.BatchNorm2d(64)),
   37:             ('relu1', nn.ReLU(inplace=True)),
   38:             ('maxpool1', nn.MaxPool2d(kernel_size=3, stride=2)),
   39:             ('conv2', nn.Conv2d(64, 192, kernel_size=5, padding=2)),
   40:             ('bn2', nn.BatchNorm2d(192)),
   41:             ('relu2', nn.ReLU(inplace=True)),
   42:             ('maxpool2', nn.MaxPool2d(kernel_size=3, stride=2)),
   43:             ('conv3', nn.Conv2d(192, 384, kernel_size=3, padding=1)),
   44:             ('bn3', nn.BatchNorm2d(384)),
   45:             ('relu3', nn.ReLU(inplace=True)),
   46:             ('conv4', nn.Conv2d(384, 256, kernel_size=3, padding=1)),
   47:             ('bn4', nn.BatchNorm2d(256)),
   48:             ('relu4', nn.ReLU(inplace=True)),
   49:             ('conv5', nn.Conv2d(256, 256, kernel_size=3, padding=1)),
   50:             ('bn5', nn.BatchNorm2d(256)),
   51:             ('relu5', nn.ReLU(inplace=True)),
   52:             ('maxpool5', nn.MaxPool2d(kernel_size=3, stride=2)),
   53:         ]))
   54:         self.avgpool = nn.AdaptiveAvgPool2d((6, 6))
   55:         self.fc1 = nn.Linear(256 * 6 * 6, 1024)
   56:         self.bn6 = nn.BatchNorm1d(1024)
   57:         self.relu = nn.ReLU()
   58:         self.fc2 = nn.Linear(1024, 1024)
   59:         self.bn7 = nn.BatchNorm1d(1024)
   60: 
   61:     def forward(self, x):
   62:         x = self.features(x)
   63:         x = self.avgpool(x)
   64:         x = torch.flatten(x, 1)
   65:         x = self.bn6(self.fc1(x))
   66:         x = self.relu(x)
   67:         x = self.bn7(self.fc2(x))
   68:         x = self.relu(x)
   69:         return x  # [B, 1024]
   70: 
   71: 
   72: class StyleModulator(nn.Module):
   73:     """M6: Delta-FiLM — maps style difference to modulation parameters."""
   74:     def __init__(self, dim=128):
   75:         super().__init__()
   76:         self.film_net = nn.Sequential(
   77:             nn.Linear(dim, dim), nn.ReLU(), nn.Linear(dim, dim * 2)
   78:         )
   79:         self.gate_linear = nn.Linear(dim, 1)
   80: 
   81:     def forward(self, delta_sty):
   82:         """delta_sty: [B, dim] or [1, dim] normalized style difference."""
   83:         params = self.film_net(delta_sty)
   84:         gamma, beta = params.chunk(2, dim=-1)
   85:         gamma = torch.tanh(gamma)  # constrain to [-1, 1] for stability
   86:         gate = torch.sigmoid(self.gate_linear(delta_sty))  # [B, 1]
   87:         return gamma, beta, gate
   88: 
   89: 
   90: class FedDSAModel(fuf.FModule):
   91:     def __init__(self, num_classes=7, feat_dim=1024, proj_dim=128):
   92:         super().__init__()
   93:         self.encoder = AlexNetEncoder()
   94:         self.semantic_head = nn.Sequential(
   95:             nn.Linear(feat_dim, proj_dim), nn.ReLU(), nn.Linear(proj_dim, proj_dim)
   96:         )
   97:         self.style_head = nn.Sequential(
   98:             nn.Linear(feat_dim, proj_dim), nn.ReLU(), nn.Linear(proj_dim, proj_dim)
   99:         )
  100:         self.head = nn.Linear(proj_dim, num_classes)
  101:         self.style_modulator = StyleModulator(proj_dim)  # M6: shared via FedAvg
  102: 
  103:     def forward(self, x):
  104:         h = self.encoder(x)
  105:         z_sem = self.semantic_head(h)
  106:         return self.head(z_sem)
  107: 
  108:     def encode(self, x):
  109:         return self.encoder(x)
  110: 
  111:     def get_semantic(self, h):
  112:         return self.semantic_head(h)
  113: 
  114:     def get_style(self, h):
  115:         return self.style_head(h)
  116: 
  117: 
  118: # ============================================================
  119: # Server — Adaptive Gap Computation + Domain-Aware Protos
  120: # ============================================================
  121: 
  122: class Server(flgo.algorithm.fedbase.BasicServer):
  123:     def initialize(self):
  124:         # Short keys used to keep record filename under 255 chars (Linux limit).
  125:         # lo=lambda_orth, lh=lambda_hsic, ls=lambda_sem, wr=warmup_rounds,
  126:         # sdn=style_dispatch_num, pd=proj_dim, am=aug_min, ax=aug_max,
  127:         # ns=noise_std, ed=ema_decay, md=adaptive_mode, fa=fixed_alpha_value
  128:         self.init_algo_para({
  129:             'lo': 1.0,   # lambda_orth
  130:             'lh': 0.0,   # lambda_hsic
  131:             'ls': 1.0,   # lambda_sem
  132:             'tau': 0.1,
  133:             'wr': 50,    # warmup_rounds
  134:             'sdn': 5,    # style_dispatch_num
  135:             'pd': 128,   # proj_dim
  136:             'am': 0.05,  # aug_min
  137:             'ax': 0.8,   # aug_max
  138:             'ns': 0.05,  # noise_std
  139:             'ed': 0.9,   # ema_decay
  140:             'md': 1,     # adaptive_mode: 0=fixed_alpha,1=M1,2=M3,3=M1+M3,4=M4-dual
  141:             'fa': 0.5,   # fixed_alpha_value
  142:             'li': 1.0,   # lambda_intra (M4 dual alignment)
  143:             'lc': 0.5,   # lambda_cross (M4 dual alignment)
  144:             'lsc': 0.5,  # lambda_sty_contrast (M5 style contrastive)
  145:         })
  146:         # Readable aliases so all downstream code is unchanged
  147:         self.lambda_orth = float(self.lo)
  148:         self.lambda_hsic = float(self.lh)
  149:         self.lambda_sem = float(self.ls)
  150:         self.warmup_rounds = int(self.wr)
  151:         self.style_dispatch_num = int(self.sdn)
  152:         self.proj_dim = int(self.pd)
  153:         self.aug_min = float(self.am)
  154:         self.aug_max = float(self.ax)
  155:         self.noise_std = float(self.ns)
  156:         self.ema_decay = float(self.ed)
  157:         self.adaptive_mode = int(self.md)
  158:         self.fixed_alpha_value = float(self.fa)
  159:         self.lambda_intra = float(self.li)
  160:         self.lambda_cross = float(self.lc)
  161:         self.lambda_sty_contrast = float(self.lsc)
  162:         self.sample_option = 'full'
  163: 
  164:         # Style bank: h-space stats for AdaIN augmentation
  165:         self.style_bank = {}  # client_id -> (mu_h, sigma_h)
  166:         # Style gap bank: z_sty-space stats for gap measurement (dual bank, R2 review)
  167:         self.style_gap_bank = {}  # client_id -> (mu_zsty, sigma_zsty)
  168: 
  169:         # Prototypes
  170:         self.global_semantic_protos = {}  # class -> avg proto
  171:         self.domain_protos = {}  # (class, client_id) -> z_sem proto (M3/M4)
  172:         self.style_domain_protos = {}  # (class, client_id) -> z_sty proto (M5)
  173: 
  174:         # Gap metrics (EMA smoothed)
  175:         self.client_gaps = {}  # client_id -> normalized gap [0,1]
  176:         self._ema_gap_mean = None
  177:         self._ema_gap_std = None
  178: 
  179:         self._init_agg_keys()
  180: 
  181:         # Pass config to clients
  182:         for c in self.clients:
  183:             c.lambda_orth = self.lambda_orth
  184:             c.lambda_hsic = self.lambda_hsic
  185:             c.lambda_sem = self.lambda_sem
  186:             c.tau = self.tau
  187:             c.warmup_rounds = self.warmup_rounds
  188:             c.proj_dim = self.proj_dim
  189:             c.aug_min = self.aug_min
  190:             c.aug_max = self.aug_max
  191:             c.noise_std = self.noise_std
  192:             c.adaptive_mode = int(self.adaptive_mode)
  193:             c.fixed_alpha_value = float(self.fixed_alpha_value)
  194:             c.lambda_intra = float(self.lambda_intra)
  195:             c.lambda_cross = float(self.lambda_cross)
  196:             c.lambda_sty_contrast = float(self.lambda_sty_contrast)
  197: 
  198:     def _init_agg_keys(self):
  199:         all_keys = list(self.model.state_dict().keys())
  200:         self.private_keys = set()
  201:         for k in all_keys:
  202:             if 'style_head' in k and self.adaptive_mode not in (5, 6):
  203:                 # style_head private by default; M5/M6 skip this (style_head shared)
  204:                 self.private_keys.add(k)
  205:             elif 'bn' in k.lower() and ('running_' in k or 'num_batches_tracked' in k):
  206:                 self.private_keys.add(k)
  207:         self.shared_keys = [k for k in all_keys if k not in self.private_keys]
  208: 
  209:     def pack(self, client_id, mtype=0):
  210:         # Dispatch style bank (h-space, for AdaIN augmentation)
  211:         dispatched_styles = None
  212:         if len(self.style_bank) > 0 and self.current_round >= self.warmup_rounds:
  213:             available = {cid: s for cid, s in self.style_bank.items() if cid != client_id}
  214:             if len(available) == 0:
  215:                 available = self.style_bank
  216:             n = min(self.style_dispatch_num, len(available))
  217:             keys = list(available.keys())
  218:             chosen = np.random.choice(keys, n, replace=False)
  219:             dispatched_styles = [available[k] for k in chosen]
  220: 
  221:         # Gap for this client (M1)
  222:         gap_normalized = self.client_gaps.get(client_id, 0.5)
  223: 
  224:         # Choose which protos to send based on mode
  225:         use_domain = self.adaptive_mode in (2, 3, 4, 5, 6)
  226: 
  227:         # M4/M5: split domain_protos into intra (own-domain) + cross (other-domain)
  228:         intra_protos = {}
  229:         cross_protos = {}
  230:         if self.adaptive_mode in (4, 5, 6) and self.domain_protos:
  231:             for (cls, cid), proto in self.domain_protos.items():
  232:                 if cid == client_id:
  233:                     intra_protos[cls] = proto
  234:                 else:
  235:                     cross_protos[(cls, cid)] = proto
  236: 
  237:         # M5: split style_domain_protos the same way
  238:         sty_intra_protos = {}
  239:         sty_cross_protos = {}
  240:         if self.adaptive_mode in (5, 6) and self.style_domain_protos:
  241:             for (cls, cid), proto in self.style_domain_protos.items():
  242:                 if cid == client_id:
  243:                     sty_intra_protos[cls] = proto
  244:                 else:
  245:                     sty_cross_protos[(cls, cid)] = proto
  246: 
  247:         return {
  248:             'model': copy.deepcopy(self.model),
  249:             'global_protos': copy.deepcopy(self.global_semantic_protos),
  250:             'domain_protos': copy.deepcopy(self.domain_protos) if use_domain else {},
  251:             'intra_protos': copy.deepcopy(intra_protos),
  252:             'cross_protos': copy.deepcopy(cross_protos),
  253:             'sty_intra_protos': copy.deepcopy(sty_intra_protos),
  254:             'sty_cross_protos': copy.deepcopy(sty_cross_protos),
  255:             'style_bank': dispatched_styles,
  256:             'current_round': self.current_round,
  257:             'gap_normalized': gap_normalized,
  258:         }
  259: 
  260:     def iterate(self):
  261:         self.selected_clients = self.sample()
  262:         res = self.communicate(self.selected_clients)
  263: 
  264:         models = res['model']
  265:         protos_list = res['protos']
  266:         proto_counts_list = res['proto_counts']
  267:         style_stats_list = res['style_stats']
  268:         style_gap_stats_list = res['style_gap_stats']
  269: 
  270:         # 1. Aggregate shared parameters (FedAvg)
  271:         self._aggregate_shared(models)
  272: 
  273:         # 2. Collect dual style banks
  274:         for cid, h_style, zsty_style in zip(
  275:             self.received_clients, style_stats_list, style_gap_stats_list
  276:         ):
  277:             if h_style is not None:
  278:                 self.style_bank[cid] = h_style
  279:             if zsty_style is not None:
  280:                 self.style_gap_bank[cid] = zsty_style
  281: 
  282:         # 3. Compute gap metrics from z_sty bank (M1)
  283:         if self.adaptive_mode in (1, 3):
  284:             self._compute_gap_metrics()
  285: 
  286:         # 4. Store domain protos (M3 / M4 / M5 / M6)
  287:         if self.adaptive_mode in (2, 3, 4, 5, 6):
  288:             self._store_domain_protos(protos_list)
  289: 
  290:         # 5. Store style domain protos (M5 / M6)
  291:         if self.adaptive_mode in (5, 6):
  292:             sty_protos_list = res.get('sty_protos', [None] * len(self.received_clients))
  293:             for cid, sty_protos in zip(self.received_clients, sty_protos_list):
  294:                 if sty_protos is None:
  295:                     continue
  296:                 for c, proto in sty_protos.items():
  297:                     self.style_domain_protos[(c, cid)] = proto
  298: 
  299:         # 5. Aggregate global protos (always, as fallback)
  300:         self._aggregate_protos(protos_list, proto_counts_list)
  301: 
  302:     def _compute_gap_metrics(self):
  303:         """Compute per-client domain gap using z_sty statistics (pure domain signal)."""
  304:         if len(self.style_gap_bank) < 2:
  305:             return
  306: 
  307:         all_mu = torch.stack([s[0] for s in self.style_gap_bank.values()])
  308:         all_sigma = torch.stack([s[1] for s in self.style_gap_bank.values()])
  309: 
  310:         mu_center = all_mu.mean(dim=0)
  311:         sigma_center = all_sigma.mean(dim=0)
  312: 
  313:         raw_gaps = {}
  314:         for cid, (mu, sigma) in self.style_gap_bank.items():
  315:             gap = ((mu - mu_center) ** 2).sum() + ((sigma - sigma_center) ** 2).sum()
  316:             raw_gaps[cid] = gap.item()
  317: 
  318:         # EMA z-score normalization
  319:         gap_vals = list(raw_gaps.values())
  320:         batch_mean = np.mean(gap_vals)
  321:         batch_std = np.std(gap_vals) + 1e-8
  322: 
  323:         if self._ema_gap_mean is None:
  324:             self._ema_gap_mean = batch_mean
  325:             self._ema_gap_std = batch_std
  326:         else:
  327:             self._ema_gap_mean = self.ema_decay * self._ema_gap_mean + (1 - self.ema_decay) * batch_mean
  328:             self._ema_gap_std = self.ema_decay * self._ema_gap_std + (1 - self.ema_decay) * batch_std
  329: 
  330:         for cid, g in raw_gaps.items():
  331:             z = (g - self._ema_gap_mean) / (self._ema_gap_std + 1e-8)
  332:             self.client_gaps[cid] = float(np.clip(z * 0.5 + 0.5, 0.0, 1.0))
  333: 
  334:     def _aggregate_shared(self, models):
  335:         if len(models) == 0:
  336:             return
  337:         weights = np.array(
  338:             [len(self.clients[cid].train_data) for cid in self.received_clients], dtype=float
  339:         )
  340:         weights /= weights.sum()

 succeeded in 866ms:
    1: """
    2: FedDSA-DA: Domain-Aware Prototype Alignment variant.
    3: Key change: Replace global-avg prototypes with per-domain prototypes + multi-positive InfoNCE.
    4: 
    5: Motivation:
    6: - Current FedDSA averages prototypes across all domains → "semantic dilution"
    7: - Photo-dog and Sketch-dog forced to same centroid → domain conflict
    8: - Domain-aware: keep per-(class, domain) prototypes, use SupCon-style multi-positive contrastive
    9: 
   10: Based on: FedDAP (CVPR 2026) domain-aware prototype concept.
   11: """
   12: import os
   13: import copy
   14: import collections
   15: import numpy as np
   16: import torch
   17: import torch.nn as nn
   18: import torch.nn.functional as F
   19: import flgo.algorithm.fedbase
   20: import flgo.utils.fmodule as fuf
   21: from collections import OrderedDict
   22: 
   23: 
   24: # ============================================================
   25: # Model: Same as base FedDSA
   26: # ============================================================
   27: 
   28: class AlexNetEncoder(nn.Module):
   29:     def __init__(self):
   30:         super().__init__()
   31:         self.features = nn.Sequential(OrderedDict([
   32:             ('conv1', nn.Conv2d(3, 64, kernel_size=11, stride=4, padding=2)),
   33:             ('bn1', nn.BatchNorm2d(64)),
   34:             ('relu1', nn.ReLU(inplace=True)),
   35:             ('maxpool1', nn.MaxPool2d(kernel_size=3, stride=2)),
   36:             ('conv2', nn.Conv2d(64, 192, kernel_size=5, padding=2)),
   37:             ('bn2', nn.BatchNorm2d(192)),
   38:             ('relu2', nn.ReLU(inplace=True)),
   39:             ('maxpool2', nn.MaxPool2d(kernel_size=3, stride=2)),
   40:             ('conv3', nn.Conv2d(192, 384, kernel_size=3, padding=1)),
   41:             ('bn3', nn.BatchNorm2d(384)),
   42:             ('relu3', nn.ReLU(inplace=True)),
   43:             ('conv4', nn.Conv2d(384, 256, kernel_size=3, padding=1)),
   44:             ('bn4', nn.BatchNorm2d(256)),
   45:             ('relu4', nn.ReLU(inplace=True)),
   46:             ('conv5', nn.Conv2d(256, 256, kernel_size=3, padding=1)),
   47:             ('bn5', nn.BatchNorm2d(256)),
   48:             ('relu5', nn.ReLU(inplace=True)),
   49:             ('maxpool5', nn.MaxPool2d(kernel_size=3, stride=2)),
   50:         ]))
   51:         self.avgpool = nn.AdaptiveAvgPool2d((6, 6))
   52:         self.fc1 = nn.Linear(256 * 6 * 6, 1024)
   53:         self.bn6 = nn.BatchNorm1d(1024)
   54:         self.relu = nn.ReLU()
   55:         self.fc2 = nn.Linear(1024, 1024)
   56:         self.bn7 = nn.BatchNorm1d(1024)
   57: 
   58:     def forward(self, x):
   59:         x = self.features(x)
   60:         x = self.avgpool(x)
   61:         x = torch.flatten(x, 1)
   62:         x = self.bn6(self.fc1(x))
   63:         x = self.relu(x)
   64:         x = self.bn7(self.fc2(x))
   65:         x = self.relu(x)
   66:         return x
   67: 
   68: 
   69: class FedDSAModel(fuf.FModule):
   70:     def __init__(self, num_classes=7, feat_dim=1024, proj_dim=128):
   71:         super().__init__()
   72:         self.encoder = AlexNetEncoder()
   73:         self.semantic_head = nn.Sequential(
   74:             nn.Linear(feat_dim, proj_dim), nn.ReLU(), nn.Linear(proj_dim, proj_dim)
   75:         )
   76:         self.style_head = nn.Sequential(
   77:             nn.Linear(feat_dim, proj_dim), nn.ReLU(), nn.Linear(proj_dim, proj_dim)
   78:         )
   79:         self.head = nn.Linear(proj_dim, num_classes)
   80: 
   81:     def forward(self, x):
   82:         h = self.encoder(x)
   83:         z_sem = self.semantic_head(h)
   84:         return self.head(z_sem)
   85: 
   86:     def encode(self, x):
   87:         return self.encoder(x)
   88: 
   89:     def get_semantic(self, h):
   90:         return self.semantic_head(h)
   91: 
   92:     def get_style(self, h):
   93:         return self.style_head(h)
   94: 
   95: 
   96: # ============================================================
   97: # Server — Domain-Aware Prototype Management
   98: # ============================================================
   99: 
  100: class Server(flgo.algorithm.fedbase.BasicServer):
  101:     def initialize(self):
  102:         self.init_algo_para({
  103:             'lambda_orth': 1.0,
  104:             'lambda_hsic': 0.1,
  105:             'lambda_sem': 1.0,
  106:             'tau': 0.1,
  107:             'warmup_rounds': 10,
  108:             'style_dispatch_num': 5,
  109:             'proj_dim': 128,
  110:         })
  111:         self.sample_option = 'full'
  112: 
  113:         self.style_bank = {}
  114:         self.global_semantic_protos = {}  # class -> avg proto (fallback)
  115:         self.domain_protos = {}  # (class, client_id) -> proto (domain-aware)
  116: 
  117:         self._init_agg_keys()
  118: 
  119:         for c in self.clients:
  120:             c.lambda_orth = self.lambda_orth
  121:             c.lambda_hsic = self.lambda_hsic
  122:             c.lambda_sem = self.lambda_sem
  123:             c.tau = self.tau
  124:             c.warmup_rounds = self.warmup_rounds
  125:             c.proj_dim = self.proj_dim
  126: 
  127:     def _init_agg_keys(self):
  128:         all_keys = list(self.model.state_dict().keys())
  129:         self.private_keys = set()
  130:         for k in all_keys:
  131:             if 'style_head' in k:
  132:                 self.private_keys.add(k)
  133:             elif 'bn' in k.lower() and ('running_' in k or 'num_batches_tracked' in k):
  134:                 self.private_keys.add(k)
  135:         self.shared_keys = [k for k in all_keys if k not in self.private_keys]
  136: 
  137:     def pack(self, client_id, mtype=0):
  138:         dispatched_styles = None
  139:         if len(self.style_bank) > 0 and self.current_round >= self.warmup_rounds:
  140:             available = {cid: s for cid, s in self.style_bank.items() if cid != client_id}
  141:             if len(available) == 0:
  142:                 available = self.style_bank
  143:             n = min(self.style_dispatch_num, len(available))
  144:             keys = list(available.keys())
  145:             chosen = np.random.choice(keys, n, replace=False)
  146:             dispatched_styles = [available[k] for k in chosen]
  147: 
  148:         return {
  149:             'model': copy.deepcopy(self.model),
  150:             'global_protos': copy.deepcopy(self.global_semantic_protos),
  151:             'domain_protos': copy.deepcopy(self.domain_protos),
  152:             'style_bank': dispatched_styles,
  153:             'current_round': self.current_round,
  154:         }
  155: 
  156:     def iterate(self):
  157:         self.selected_clients = self.sample()
  158:         res = self.communicate(self.selected_clients)
  159: 
  160:         models = res['model']
  161:         protos_list = res['protos']
  162:         proto_counts_list = res['proto_counts']
  163:         style_stats_list = res['style_stats']
  164: 
  165:         # 1. Aggregate shared parameters (FedAvg)
  166:         self._aggregate_shared(models)
  167: 
  168:         # 2. Collect style bank
  169:         for cid, style in zip(self.received_clients, style_stats_list):
  170:             if style is not None:
  171:                 self.style_bank[cid] = style
  172: 
  173:         # 3. Store per-domain prototypes (NEW)
  174:         self._store_domain_protos(protos_list, proto_counts_list)
  175: 
  176:         # 4. Also compute global avg protos (fallback for early rounds)
  177:         self._aggregate_protos(protos_list, proto_counts_list)
  178: 
  179:     def _aggregate_shared(self, models):
  180:         if len(models) == 0:
  181:             return
  182:         weights = np.array([len(self.clients[cid].train_data) for cid in self.received_clients], dtype=float)
  183:         weights /= weights.sum()
  184: 
  185:         global_dict = self.model.state_dict()
  186:         model_dicts = [m.state_dict() for m in models]
  187: 
  188:         for k in self.shared_keys:
  189:             if 'num_batches_tracked' in k:
  190:                 continue
  191:             global_dict[k] = sum(w * md[k] for w, md in zip(weights, model_dicts))
  192: 
  193:         self.model.load_state_dict(global_dict, strict=False)
  194: 
  195:     def _store_domain_protos(self, protos_list, counts_list):
  196:         """Store per-(class, client_id) prototypes — domain-aware."""
  197:         for cid, protos in zip(self.received_clients, protos_list):
  198:             if protos is None:
  199:                 continue
  200:             for c, proto in protos.items():
  201:                 self.domain_protos[(c, cid)] = proto
  202: 
  203:     def _aggregate_protos(self, protos_list, counts_list):
  204:         """Global average (fallback for warmup)."""
  205:         agg = {}
  206:         for protos, counts in zip(protos_list, counts_list):
  207:             if protos is None:
  208:                 continue
  209:             for c, proto in protos.items():
  210:                 cnt = counts.get(c, 1)
  211:                 if c not in agg:
  212:                     agg[c] = (proto * cnt, cnt)
  213:                 else:
  214:                     prev_sum, prev_cnt = agg[c]
  215:                     agg[c] = (prev_sum + proto * cnt, prev_cnt + cnt)
  216: 
  217:         self.global_semantic_protos = {}
  218:         for c, (s, n) in agg.items():
  219:             self.global_semantic_protos[c] = s / n
  220: 
  221: 
  222: # ============================================================
  223: # Client — Domain-Aware Multi-Positive InfoNCE
  224: # ============================================================
  225: 
  226: class Client(flgo.algorithm.fedbase.BasicClient):
  227:     def initialize(self):
  228:         self.model = copy.deepcopy(self.server.model).to('cpu')
  229:         self.loss_fn = nn.CrossEntropyLoss()
  230:         self.current_round = 0
  231:         self.local_style_bank = None
  232:         self.global_protos = None
  233:         self.domain_protos = None
  234: 
  235:     def reply(self, svr_pkg):
  236:         model, global_protos, domain_protos, style_bank, current_round = self.unpack(svr_pkg)
  237:         self.current_round = current_round
  238:         self.global_protos = global_protos
  239:         self.domain_protos = domain_protos
  240:         self.local_style_bank = style_bank

codex
The math path is mostly clean. The remaining question is severity: whether the current `scpr` branch has any true correctness bugs, or whether the main problem is that it’s still composable with the legacy scheduled alignment and therefore no longer represents the stated SCPR objective unless configs are constrained. I’m pulling exact line numbers for that block now.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$i=1; Get-Content -Path '"'D:\\桌面文件\\联邦学习\\FDSE_CVPR25\\algorithm\\feddsa_scheduled.py' | ForEach-Object { '{0,5}: {1}' -f "'$i, $_; $i++ } | Select-Object -Skip 780 -First 70' in D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-18_SCPR_v1
 succeeded in 805ms:
  781:             loss_aug = torch.tensor(0.0, device=x.device)
  782:             z_sem_aug = None
  783:             has_aug = (w_aux > 1e-4
  784:                        and self.local_style_bank is not None
  785:                        and len(self.local_style_bank) > 0)
  786:             if has_aug:
  787:                 h_aug = self._style_augment(h)
  788:                 z_sem_aug = model.get_semantic(h_aug)
  789:                 if self.schedule_mode != 7:
  790:                     # Normal modes: augmented features go through CE
  791:                     output_aug = model.head(z_sem_aug)
  792:                     loss_aug = self.loss_fn(output_aug, y)
  793:                 # Mode 7: z_sem_aug computed but NOT sent through CE
  794: 
  795:             # Loss 3: Decoupling — ALWAYS full weight from R0
  796:             loss_orth, loss_hsic = self._decouple_loss(z_sem, z_sty)
  797: 
  798:             # Loss 4: Alignment (controlled by w_aux, type depends on mode)
  799:             loss_sem = torch.tensor(0.0, device=x.device)
  800:             loss_mse_anchor = torch.tensor(0.0, device=x.device)
  801:             use_alpha = self.schedule_mode in (5, 6, 7)
  802:             use_mse = self.schedule_mode in (4, 6, 7)
  803:             if w_aux > 1e-4 and self.global_protos and len(self.global_protos) >= 2:
  804:                 # Choose InfoNCE variant
  805:                 # For mode 7: use z_sem + z_sem_aug concatenated for richer contrastive
  806:                 z_for_contrast = z_sem
  807:                 y_for_contrast = y
  808:                 if self.schedule_mode == 7 and z_sem_aug is not None:
  809:                     z_for_contrast = torch.cat([z_sem, z_sem_aug], dim=0)
  810:                     y_for_contrast = torch.cat([y, y], dim=0)
  811: 
  812:                 if use_alpha:
  813:                     loss_sem = self._infonce_alpha_loss(z_for_contrast, y_for_contrast)
  814:                 else:
  815:                     loss_sem = self._infonce_loss(z_for_contrast, y_for_contrast)
  816: 
  817:                 if use_mse:
  818:                     loss_mse_anchor = self._mse_anchor_loss(z_sem, y)
  819: 
  820:             # Total loss:
  821:             # L_orth: ALWAYS full weight (key change from original)
  822:             # L_aug + L_InfoNCE + L_MSE: controlled by w_aux schedule
  823:             loss = (
  824:                 loss_task
  825:                 + w_aux * loss_aug
  826:                 + self.lambda_orth * loss_orth
  827:                 + self.lambda_hsic * loss_hsic
  828:                 + w_aux * self.lambda_sem * loss_sem
  829:                 + w_aux * self.lambda_mse * loss_mse_anchor
  830:             )
  831: 
  832:             # SCPR loss: self-masked (style-)weighted multi-positive InfoNCE.
  833:             # Independent from sm/w_aux schedule (works even when Plan A sm=0).
  834:             # scpr_mode=0 → skip entirely; =1 → uniform multi-pos (M3 bound); =2 → style-weighted.
  835:             if self.scpr_mode > 0 and self.scpr_payload is not None:
  836:                 loss_scpr = self._scpr_loss(z_sem, y)
  837:                 loss = loss + self.lambda_sem * loss_scpr
  838: 
  839:             # Gradient conflict logging (last batch of last epoch)
  840:             is_last_batch = (step == num_steps - 1)
  841:             if should_log_grad and is_last_batch and loss_sem.item() > 0:
  842:                 self._log_grad_conflict(
  843:                     model, loss_task, w_aux * self.lambda_sem * loss_sem
  844:                 )
  845: 
  846:             loss.backward()
  847:             if self.clip_grad > 0:
  848:                 torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=self.clip_grad)
  849:             optimizer.step()
  850: 

ERROR: Reconnecting... 1/5
codex
**Verdict**

`MUST_FIX`

Self-mask, per-class positive renormalization, detach behavior, and `logsumexp` stability are implemented correctly. `scpr_payload` integration is backward-compatible, `sas=1` can coexist without a key/state collision, and the empty-bank / `has_pos=0` / 2-client / `K=1` edge cases fail safe rather than crashing. Rebuilding `proto_matrix` each batch is acceptable at current PACS/Office scale and does not leak gradients.

**Findings**

1. `IMPORTANT` `scpr=1` is still incorrectly dependent on style availability in [`feddsa_scheduled.py#L403`](D:/桌面文件/联邦学习/FDSE_CVPR25/algorithm/feddsa_scheduled.py#L403).  
Mode 1 is supposed to be the style-free M3 lower bound, but `_compute_scpr_payload()` immediately requires `target_style`, and later filters sources on `src_style` / shape match. That means uniform mode can silently disable SCPR or drop valid prototype sources even when the prototype bank is usable.

Fix: at [`feddsa_scheduled.py#L403`](D:/桌面文件/联邦学习/FDSE_CVPR25/algorithm/feddsa_scheduled.py#L403), only read `target_style` / `target_vec` and `src_style` / `src_vec` inside the `self.scpr_mode == 2` path. For `self.scpr_mode == 1`, collect sources using only `cid != target_cid` and `protos is not None and len(protos) > 0`, then assign uniform weights over that set.

2. `IMPORTANT` SCPR currently stacks with the legacy scheduled alignment path in [`feddsa_scheduled.py#L803`](D:/桌面文件/联邦学习/FDSE_CVPR25/algorithm/feddsa_scheduled.py#L803) and [`feddsa_scheduled.py#L835`](D:/桌面文件/联邦学习/FDSE_CVPR25/algorithm/feddsa_scheduled.py#L835).  
With any `sm != 0`, `scpr=1/2` is no longer the advertised M3/SCPR objective; it becomes old global-prototype InfoNCE (+ optional alpha/MSE path) plus SCPR. That breaks the “`scpr=1` strictly reduces to M3” contract unless configs are manually constrained.

Fix: if SCPR is intended to be the alignment objective, add a hard config guard right after alias setup at [`feddsa_scheduled.py#L163`](D:/桌面文件/联邦学习/FDSE_CVPR25/algorithm/feddsa_scheduled.py#L163): reject `self.scpr_mode > 0 and self.schedule_mode != 0`. If coexistence is intended instead, then the legacy block at [`feddsa_scheduled.py#L803`](D:/桌面文件/联邦学习/FDSE_CVPR25/algorithm/feddsa_scheduled.py#L803) must be explicitly redesigned/gated so the semantics are documented, because it is not “pure SCPR” today.

3. `MINOR` The `_scpr_loss` docstring around [`feddsa_scheduled.py#L1053`](D:/桌面文件/联邦学习/FDSE_CVPR25/algorithm/feddsa_scheduled.py#L1053) does not match the implemented denominator.  
The code uses `logsumexp` over all entries, which is standard multi-positive SupCon and is the part that matches the `scpr=1 -> M3` claim. The written formula excludes the other positives from the denominator, so the comment/spec should be updated.
tokens used
136,355
**Verdict**

`MUST_FIX`

Self-mask, per-class positive renormalization, detach behavior, and `logsumexp` stability are implemented correctly. `scpr_payload` integration is backward-compatible, `sas=1` can coexist without a key/state collision, and the empty-bank / `has_pos=0` / 2-client / `K=1` edge cases fail safe rather than crashing. Rebuilding `proto_matrix` each batch is acceptable at current PACS/Office scale and does not leak gradients.

**Findings**

1. `IMPORTANT` `scpr=1` is still incorrectly dependent on style availability in [`feddsa_scheduled.py#L403`](D:/桌面文件/联邦学习/FDSE_CVPR25/algorithm/feddsa_scheduled.py#L403).  
Mode 1 is supposed to be the style-free M3 lower bound, but `_compute_scpr_payload()` immediately requires `target_style`, and later filters sources on `src_style` / shape match. That means uniform mode can silently disable SCPR or drop valid prototype sources even when the prototype bank is usable.

Fix: at [`feddsa_scheduled.py#L403`](D:/桌面文件/联邦学习/FDSE_CVPR25/algorithm/feddsa_scheduled.py#L403), only read `target_style` / `target_vec` and `src_style` / `src_vec` inside the `self.scpr_mode == 2` path. For `self.scpr_mode == 1`, collect sources using only `cid != target_cid` and `protos is not None and len(protos) > 0`, then assign uniform weights over that set.

2. `IMPORTANT` SCPR currently stacks with the legacy scheduled alignment path in [`feddsa_scheduled.py#L803`](D:/桌面文件/联邦学习/FDSE_CVPR25/algorithm/feddsa_scheduled.py#L803) and [`feddsa_scheduled.py#L835`](D:/桌面文件/联邦学习/FDSE_CVPR25/algorithm/feddsa_scheduled.py#L835).  
With any `sm != 0`, `scpr=1/2` is no longer the advertised M3/SCPR objective; it becomes old global-prototype InfoNCE (+ optional alpha/MSE path) plus SCPR. That breaks the “`scpr=1` strictly reduces to M3” contract unless configs are manually constrained.

Fix: if SCPR is intended to be the alignment objective, add a hard config guard right after alias setup at [`feddsa_scheduled.py#L163`](D:/桌面文件/联邦学习/FDSE_CVPR25/algorithm/feddsa_scheduled.py#L163): reject `self.scpr_mode > 0 and self.schedule_mode != 0`. If coexistence is intended instead, then the legacy block at [`feddsa_scheduled.py#L803`](D:/桌面文件/联邦学习/FDSE_CVPR25/algorithm/feddsa_scheduled.py#L803) must be explicitly redesigned/gated so the semantics are documented, because it is not “pure SCPR” today.

3. `MINOR` The `_scpr_loss` docstring around [`feddsa_scheduled.py#L1053`](D:/桌面文件/联邦学习/FDSE_CVPR25/algorithm/feddsa_scheduled.py#L1053) does not match the implemented denominator.  
The code uses `logsumexp` over all entries, which is standard multi-positive SupCon and is the part that matches the `scpr=1 -> M3` claim. The written formula excludes the other positives from the denominator, so the comment/spec should be updated.
