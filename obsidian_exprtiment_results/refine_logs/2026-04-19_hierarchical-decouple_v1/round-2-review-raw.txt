OpenAI Codex v0.121.0 (research preview)
--------
workdir: D:\桌面文件\联邦学习\obsidian_exprtiment_results\refine_logs\2026-04-19_hierarchical-decouple_v1
model: gpt-5.4
provider: fox
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR, C:\Users\31279\.codex\memories]
reasoning effort: xhigh
reasoning summaries: none
session id: 019da503-c79b-7101-8d64-0e040de63fce
--------
user
[Round 2 re-evaluation]

I revised the proposal based on your Round 1 feedback. Please re-score.

Key changes:

1. [CRITICAL math fix] Replaced GAP-based cos² (degenerate under IN affine=False) with spatial cross-correlation:
   xin = F.normalize(out_in.flatten(2), dim=2)
   xbn = F.normalize(out_bn.flatten(2), dim=2)
   corr = bmm(xin, xbn.T) / H*W
   L_layer = corr.square().mean()
   This preserves spatial structure and does not vanish under IN.

2. [CRITICAL claim narrowing] Narrowed thesis to: "Parameter-free early-split decorrelation for FedBN that supports the existing end-layer Plan A decouple." No more claim of "hierarchical style/semantic persistent decomposition."

3. [CRITICAL ablation] Added essential IBN-only ablation (A.2): IBN replaces BNs but no L_orth_layer loss. Now 3 configs: Plan A / IBN-only / HIBD. A.3 > A.2 proves decorrelation loss independent contribution.

4. [IMPORTANT venue] Single-sentence headline claim: "Replacing early FedBN layers with split IN/BN plus local spatial decorrelation improves cross-domain FL without adding trainable parameters."

5. [Clarification] HSIC λ=0.0 in Plan A is inherited from EXP-017 optimal config (HSIC harmful, from 85+ experiments). We do NOT change it in this proposal.

6. [Simplification] Removed forward hook. IBN module caches out_in/out_bn directly, client.train() iterates ibn_layers to compute loss.

=== REVISED PROPOSAL ===

# Research Proposal Round 1: Hierarchical IBN-Decorrelated FedBN (HIBD-r1)

## Problem Anchor (preserved)

- Under K=4 FedBN cross-domain FL, current FedDSA Plan A decouples ONLY at the final projection. Backbone mid-layers polluted. 85+ downstream experiments all fail at Plan A ceiling.
- Must-solve: shallow decouple → no direct backbone signal → all prototype/alignment methods (including just-failed SCPR v1/v2) polluted by mid-layer drift.
- Non-goals: prototype routing, new trainables >10%, backbone resize, VLM/CLIP, K>4.
- Constraints: AlexNet/ResNet-18, FedBN local, cos²+HSIC Plan A stays, 3-seed {2,15,333}, must beat PACS 82.31% by +0.5%, <150 LOC, 0 new trainable.
- Success: PACS ≥ 82.81%; R=20 sanity shows per-layer L_orth_layer drops; IBN-only vs HIBD shows decorrelation loss has independent contribution.

## Technical Gap

Plan A's `cos²(z_sem, z_sty)` only at final projection → backbone conv/BN receive only weak indirect gradient. FDSE (CVPR 2025) uses per-layer DFE+DSE with new params (DSE≈DFE/94). **HIBD: 0 new params via IBN normalization scheme + spatial decorrelation.**

## Method Thesis (narrowed)

Parameter-free early-split regularization: replace 2 early BNs with IBN (IN+BN parallel on split channels); enforce spatial cross-correlation decorrelation between IN-channel and BN-channel outputs. This supports Plan A's end-layer decouple; it is NOT a claim of persistent style/semantic decomposition.

## Contribution Focus

- SINGLE main contribution: parameter-free early-split decorrelation for FedBN.
- NO optional support.
- Non-contributions: prototype routing, alignment, style sharing.

## Proposed Method

### Complexity Budget
- Frozen: backbone conv/linear, sem_head/style_head/classifier, Plan A L_orth_end(λ=1.0), L_HSIC(λ=0.0 inherited from EXP-017)
- Replaced: bn1 & bn2 → IBN instances (IN affine=False, BN on half channels; 0 new trainable)
- New: L_orth_layer (spatial cross-correlation, 0 params)
- 0 new trainable, 1 new loss, 2 new hyperparameters

### IBN Module (with cache, no forward hook)

```python
class IBN(nn.Module):
    def __init__(self, channels, in_ratio=0.5):
        super().__init__()
        self.in_ch = int(channels * in_ratio)
        self.bn_ch = channels - self.in_ch
        self.instance_norm = nn.InstanceNorm2d(self.in_ch, affine=False)
        self.batch_norm = nn.BatchNorm2d(self.bn_ch)
        self._cache_in = None
        self._cache_bn = None
    def forward(self, x):
        xin, xbn = torch.split(x, [self.in_ch, self.bn_ch], dim=1)
        out_in = self.instance_norm(xin)
        out_bn = self.batch_norm(xbn)
        self._cache_in = out_in  # [B, Cin, H, W]
        self._cache_bn = out_bn  # [B, Cbn, H, W]
        return torch.cat([out_in, out_bn], dim=1)
```

### Core Mechanism — Spatial Cross-Correlation Decorrelation

```python
def layer_decorr_loss(ibn):
    xin = F.normalize(ibn._cache_in.flatten(2), dim=2)   # [B, Cin, HW], per-channel unit norm
    xbn = F.normalize(ibn._cache_bn.flatten(2), dim=2)   # [B, Cbn, HW]
    corr = torch.bmm(xin, xbn.transpose(1, 2)) / xin.size(-1)   # [B, Cin, Cbn]
    return corr.square().mean()

L_orth_layer = (1 / N_IBN) * sum_l layer_decorr_loss(ibn_l)
```

Under IN(affine=False), normalized flatten along HW dimension gives non-zero unit-length spatial sequences; correlation matrix is meaningful.

### Total Loss
L = L_CE + 1.0 * L_orth_end (Plan A) + 0.0 * L_HSIC (EXP-017 default) + 0.5 * L_orth_layer (NEW)

### Integration
- Edit FDSE_CVPR25/algorithm/feddsa_scheduled.py:
  - AlexNetEncoder: self.bn1 = IBN(64), self.bn2 = IBN(192)
  - Client.train(): after forward, sum L_orth_layer from self.ibn_layers caches
- No forward hooks; cache inside IBN module
- ~80 LOC total

### FedBN
- IN: parameter-free, per-sample, no FL sync
- IBN's BN on half channels: same as Plan A FedBN (local running stats)

### Failure Modes
| Failure | Signal | Fallback |
|---------|--------|----------|
| L_orth_layer stuck | corr.square.mean > 0.1 for 20+ rounds | check normalize dim; raise λ to 1.0 |
| PACS < Plan A | 3-seed < 82.31% | num_ibn 2→1 |
| IN batch-1 instability | loss NaN | switch IN → GroupNorm(affine=False) |
| L drops but no acc gain | corr↓ but AVG stuck | evidence Plan A is ceiling → accept negative result |

## Claim-Driven Validation

### Claim A (main): HIBD > Plan A PACS AVG Best by ≥ +0.5% AND decorrelation loss has independent contribution

- Configs (3):
  A.1 Plan A orth_only
  A.2 IBN-only (IBN replaces BN, NO L_orth_layer)  ← isolation control
  A.3 HIBD (IBN + L_orth_layer, λ=0.5)
- PACS 3 seeds {2, 15, 333}, R=200
- Decisive:
  - A.3 ≥ 82.81% (main)
  - A.3 > A.2 (decorrelation loss adds value beyond IBN structure)
  - A.2 vs A.1 (IBN structural DG effect under K=4 FL)

### Claim B (mechanism, diagnostic): HIBD's end-layer cos²(z_sem, z_sty) is lower than Plan A's

- Zero GPU overhead: track end-layer cos² across training in both A.1 and A.3
- Not tautological (we don't directly optimize end-layer cos² with L_orth_layer)
- Evidence that early decorrelation propagates through backbone

## Budget: ~40 runs, ~50 GPU-hours (parallel 4 → 12h wall)

=== END PROPOSAL ===

Please re-score 7 dimensions + overall. Check:
1. Is L_orth_layer now mathematically valid?
2. Is the claim appropriately narrow?
3. Is the IBN-only ablation sufficient isolation?
4. Any remaining blocker issues?

Output in English.

**Re-evaluating the proposal**

I need to answer the user, who is asking for a conceptual review of the provided text. No web browsing is necessary since the user has provided specifics for me to summarize. Even though there's a competitor in 2025, it's not crucial to check that. However, I might ponder browsing for additional context just in case. I need to focus on evaluating the proposal, prioritizing findings, risks, and overall quality for a concise yet substantial response in English.
**Assessing mathematical validity**

I need to analyze the mathematical validity of the L_orth_layer carefully. My task involves re-scoring seven dimensions and looking at the overall proposal. I should check claims about narrowness, IBN-only ablation sufficiency, and any remaining blocker issues. From a senior ML review perspective, I’ll dive deeply into the method. The proposal suggests a final layer decoupling, and I need to confirm if the final calculations are mathematically sound given the specific conditions of the setup.
**Inspecting correlation calculations**

I need to flatten and L2 normalize each channel over the height and width dimensions, which leads to nonzero results unless it's a constant zero map. The correlation between normalized input and batch-normalized channels, using the formula `corr = xin x xbn^T / HW`, raises some concerns. If I normalize xin to a unit norm and then divide by HW, it scales the correlation down by 1/HW. This means the result approaches 1/HW, which might not be what I intended. I'll need to reflect on this further.
**Evaluating paper claims**

I'm considering whether the core claim is sufficient for a minimal paper. It seems likely, but if the intent is to assert that it supports the existing end-layer decouple, there might need to be an additional comparison: HIBD without the end-layer orth. The main aim is to support Plan A's decouple rather than replace it. Showing HIBD's effect on the end-layer could add depth, but it might not be mandatory since the method enhances Plan A without introducing new parameters.
**Addressing potential blockers**

The user wants to know if there are any remaining blocker issues. I think it's not necessarily a complete blocker, but it would help strengthen the interaction claim. I need to recalculate scores. For Fidelity, I believe it might have improved to a 9, while Method Specificity is around 8 due to some residual scaling issues. Contribution Quality could be a 7 or 8, and Frontier Leverage might be an 8 or 9. There is still a methodological blocker regarding loss scaling and mixing, so that might still be a concern.
**Examining empirical risks**

I’m considering if it's a central empirical risk, even if it’s not fatal. It's important to be thorough as the user requested. I need to look into some subtler issues. One potential concern is with IBN in AlexNet after the first two convolutions and in the early layers of ResNet-18. The FedBN is using local running stats only on the BN layers. If I replace half the channels with IN, does that slightly change the architecture, while keeping the trainable parameters the same? I need to compute this.
**Evaluating loss implications**

I need to provide a nuanced answer to Q1. The loss is non-degenerate now, but the formula needs adjustment; specifically, I should drop `/ HW` or remove `F.normalize` to avoid double scaling. This could definitely be a blocker if we leave it as is since a tiny loss magnitude might lead to confusing conclusions when scaling lambda. If users can tune lambda properly, it might not be fatal, but it’s important and likely a method-level issue I should label as IMPORTANT or even CRITICAL.
**Assessing mathematical evaluation**

I’m considering the evaluation process for a proposal, especially regarding scaling and methods. If there's a division by HW, that’s still valid mathematically, but it may not effectively vanish under IN. For categories of evaluation, I think Problem Fidelity is about a 9 since it avoids complexity. Method Specificity seems to drop to an 8 with defined core losses and integration issues. Contribution Quality might be around 7 or 8, given its focused elegance but still incremental contributions. It’s important to assess these properly!
**Evaluating proposal metrics**

I'm assessing various aspects of a proposal, focusing on the smallest adequate mechanisms. So, for Contribution Quality (CQ), I might rate it an 8, while Venue Readiness (VR) could be around a 7—it's better but not quite ready due to narrative concerns. For Frontier Leverage, I think it deserves a 9 given its modern restraint. Feasibility seems strong at about 8 or 9. The overall score appears to sit around 8.25, leading to a likely recommendation to revise since it’s below 9. Definitely noting weaknesses for anything under 7!
**Considering evaluation thresholds**

I’m thinking that if everything is rated 7 or above, then technically there wouldn't be anything below 7, right? But the user wants detailed feedback and any unresolved issues. Since Venue Readiness might be a 7, we could say none are below 7 overall. Still, I need to ensure I address all potential blocker issues to provide the user with a complete picture. It's important to clarify and validate these ratings thoroughly!
**Reviewing proposal requirements**

To satisfy the request, I need to clarify that no dimension is currently below 7, but perhaps we might consider having at least one under that threshold. If the proposal isn't ready yet, then metrics might score at least 7. The assessment must include a brief verdict summary, four direct answers to specific checks, a concise scores table with reasons, and a few bullet points on remaining blockers. Also, I should touch on simplification opportunities and potentially warn about drift before concluding the verdict.
**Clarifying hierarchical claims**

If the goal is to make a stronger case that local decorrelation improves end-layer decoupling, you could add A.4 = HIBD without the end-layer orthogonalization or report the final-layer cos² and classification, though that's optional. I need to point out that using "Hierarchical" in the name might overstate the claim, as there are only two early layers and no persistent multi-stage structure. A name change could help, maybe to "Early IBN-Decorrelated FedBN" or "Split-Norm Decorrelation for FedBN." This affects the perceived contribution quality and readiness for publication, as it appears to drift from what’s truly hierarchical.
**Addressing parameter count issues**

This is an important point! A remaining blocker is the fact that the IBN replacement slightly reduces the trainable parameter count. I might say "no added trainables" instead of "0 new trainables" to sidestep any nitpicks. The `BatchNorm2d(self.bn_ch)` on half the channels has its own gamma and beta, which replace the original full BN parameters. So, the parameter count is reduced. For example, the original bn1 with 64 channels had 128 params, while the new one with 32 channels only has 64 params. I should definitely mention this!
**Evaluating normalization methods**

I'm examining a formula involving HW and its impact on scaling. With values like H*W equating to 3025, the resulting mean square is tiny, making it impossible for thresholds over 0.1. This points out a significant issue: dividing by HW disrupts diagnostic validity. Without that division, thresholds could meet the required values. I need to ensure that the implementation reflects these mathematical conclusions to avoid inconsistencies and ensure the correct formula is being applied.
**Examining formulation options**

I'm considering how this might influence the MS score, perhaps bringing it down from 8 to 7. I need to find the right formulation for my calculations. One approach is A: using a cosine-style map decorrelation, where I flatten each channel map and subtract the mean over HW. Then, I would apply `F.normalize(..., dim=2)` followed by calculating the correlation using `bmm(xin, xbn.transpose(1, 2))` without dividing by HW. That would lead to the average squared cosine between channel maps with `L_layer = corr.square().mean()`.
**Evaluating model adjustments**

I think the degeneracy issue is fixed; it no longer collapses to zero due to IN. However, I still need to clean up the exact equation. I should only use either `F.normalize(..., dim=2)` or `/HW` for normalization, not both. My claims are narrow enough, and the IBN-only ablation is enough for the core claim. But there are still a few blockers to address, such as double scaling and overstated terminology. I'll need to specify the backbone and ensure the claims focus on improvement over Plan A.
**Evaluating scores**

I’m considering potential scores for a model. I think I’d give PF a score of 9, which seems pretty solid. For MS, I’m leaning toward a score of 7 because there’s a scaling/formula issue and some discrepancies with backbone mapping. It feels like the backbone mapping isn’t aligning as well as it should, which might be something to address to ensure a more accurate overall score.
**Evaluating scoring criteria**

The user asked me to re-score, and I think it’s okay to have one score under 7. I’m considering that the method specificity might not quite match up perfectly since the formula is inconsistent. I’m contemplating setting Method Specificity at 7 instead of 8 due to this issue. The overall score seems to come out to about 7.95, which sounds pretty good! I also want to state that this proposal has moved from being speculative to credible.
**Considering cross-correlation methods**

I’m thinking about how to achieve "cross-correlation" using centered + /HW. It's critical to be specific here. That's a good point, and I should mention that choosing consistent nomenclature with the formula is essential. Reviewers will definitely notice any inconsistencies, so I need to stay on top of that. Let’s make sure everything aligns correctly! It’s details like these that can really make a difference.
**Evaluating venue readiness criteria**

I’m thinking about the venue readiness and considering a threshold of 7 for specific methods, but maybe keeping VR at 6 is okay. I want to detail this further. One weakness I see is that a mismatch between the formula and presentation could leave space for reviewers to critique. Also, the "hierarchical" naming might overstate things. I’m curious how to balance all these factors effectively.
**Revising the output**

I'm focusing on the phrase "no added trainables." Let’s look at modernization opportunities, and it seems there are none; it looks like the FM pivot is unnecessary. As for drift warnings, there's no material drift, just some naming issues. I think the verdict here is clear: I need to revise it. I’ll make sure the final answer isn’t too long, but the user asked for a detailed review, so I'll craft something that's both concise and informative.
codex
This is materially better. The proposal is now much closer to a real mechanism paper than “throw IBN at it and hope.” I still land at **REVISE**, not READY.

**Direct Checks**

1. **Is `L_orth_layer` now mathematically valid?**  
Yes in the important sense: the original IN+GAP degeneracy is fixed. The loss no longer collapses just because `InstanceNorm2d(affine=False)` makes spatial means zero.

But the written formula still has one inconsistency:
- `F.normalize(..., dim=2)` already turns each channel map into a unit-norm vector over `HW`.
- After that, `bmm(xin, xbn.T)` is already a cosine-style similarity matrix in `[-1, 1]`.
- Dividing again by `HW` makes the loss artificially tiny.

So the mechanism is now **non-degenerate**, but the exact formula should be one of these, not both:
- **Cosine-style decorrelation:** keep `F.normalize`, **remove `/ HW`**
- **Cross-correlation style:** center/standardize maps, **keep `/ HW`**, do not L2-normalize to unit norm first

As written, your diagnostics are inconsistent with the formula. With early AlexNet feature maps, `/ HW` after unit-norm scaling makes values so small that thresholds like `> 0.1` are not interpretable.

2. **Is the claim appropriately narrow?**  
Yes. This is now the right claim:
- parameter-free early-split regularization
- supports existing Plan A end-layer decouple
- no claim of persistent style/semantic decomposition

That is much safer and much more defensible.

3. **Is the IBN-only ablation sufficient isolation?**  
Yes for the core paper claim.  
`Plan A / IBN-only / HIBD` is the minimal sufficient trio:
- `A.2 vs A.1` isolates the structural IBN effect
- `A.3 vs A.2` isolates the value of the new decorrelation loss
- `A.3 vs A.1` supports the main practical claim

That is enough. I would not ask for more unless results are very close/noisy.

4. **Any remaining blocker issues?**  
Yes, but fewer:
- fix the loss scaling mismatch (`normalize` plus `/HW`)
- stop calling it “hierarchical” unless you really want to defend that wording
- keep the paper language away from “style/semantic branches” for the IBN halves
- report `final` as well as `best`; `AVG Best` alone is too soft for a mechanism paper

**Scores**

| Dimension | Score | Reason |
|---|---:|---|
| 1. Problem Fidelity | 9 | Still tightly anchored to the actual failure diagnosis; no prototype drift. |
| 2. Method Specificity | 7 | Much better, but the written layer-loss formula still needs one cleanup to be fully coherent. |
| 3. Contribution Quality | 8 | Focused, elegant, and appropriately small. This is the right kind of minimal contribution. |
| 4. Frontier Leverage | 9 | Appropriate restraint. No FM pivot is needed here. |
| 5. Feasibility | 8 | Easy to implement and cheap to test. Scientific risk remains, engineering risk is low. |
| 6. Validation Focus | 8 | The new `IBN-only` control is the key missing piece and now makes the validation credible. |
| 7. Venue Readiness | 6 | Better, but the paper can still be attacked on objective-definition precision and slightly inflated framing. |

**Overall: 8.0/10**

**Below 7**

**Venue Readiness — 6/10**  
Weakness: the proposal is now conceptually coherent, but the written objective and framing still leave avoidable reviewer openings.

Concrete fix:
- choose one consistent loss definition:
  - either “pairwise spatial cosine decorrelation” with `F.normalize` and no `/HW`
  - or true “cross-correlation decorrelation” with centered/standardized maps and `/HW`
- rename if possible; “hierarchical” is stronger than what is actually implemented
- say “no added trainable parameters” rather than “0 new trainable” to avoid BN-parameter-count nitpicks
- make `final` performance part of the main table, not just `best`

Priority: **CRITICAL**

**Simplification Opportunities**

- Drop “hierarchical” from the title unless you really use multiple depth stages in a way you want to defend.
- Fix `in_ratio=0.5` and `num_ibn=2` as default, not as a broad search space.
- Use one backbone as the main result path first. If AlexNet is the active battleground, make that the center of gravity.

**Modernization Opportunities**

No foundation-model pivot is warranted. For this anchored problem, adding CLIP/VLM/LLM machinery would be drift.

**Drift Warning**

No major drift now. The only mild drift risk is rhetorical: the method name still sounds larger than the actual mechanism.

**Verdict**

**REVISE**

Round 1 was “interesting but mathematically broken.”  
Round 2 is “credible, focused, and worth running,” but not yet fully venue-ready on paper because the layer objective still needs one precise cleanup and the framing should get even tighter. If you fix the loss definition and the method then clears `A.3 > A.2 > A.1` on PACS with stable `final` numbers, this becomes a defensible minimal paper.
tokens used
24,392
This is materially better. The proposal is now much closer to a real mechanism paper than “throw IBN at it and hope.” I still land at **REVISE**, not READY.

**Direct Checks**

1. **Is `L_orth_layer` now mathematically valid?**  
Yes in the important sense: the original IN+GAP degeneracy is fixed. The loss no longer collapses just because `InstanceNorm2d(affine=False)` makes spatial means zero.

But the written formula still has one inconsistency:
- `F.normalize(..., dim=2)` already turns each channel map into a unit-norm vector over `HW`.
- After that, `bmm(xin, xbn.T)` is already a cosine-style similarity matrix in `[-1, 1]`.
- Dividing again by `HW` makes the loss artificially tiny.

So the mechanism is now **non-degenerate**, but the exact formula should be one of these, not both:
- **Cosine-style decorrelation:** keep `F.normalize`, **remove `/ HW`**
- **Cross-correlation style:** center/standardize maps, **keep `/ HW`**, do not L2-normalize to unit norm first

As written, your diagnostics are inconsistent with the formula. With early AlexNet feature maps, `/ HW` after unit-norm scaling makes values so small that thresholds like `> 0.1` are not interpretable.

2. **Is the claim appropriately narrow?**  
Yes. This is now the right claim:
- parameter-free early-split regularization
- supports existing Plan A end-layer decouple
- no claim of persistent style/semantic decomposition

That is much safer and much more defensible.

3. **Is the IBN-only ablation sufficient isolation?**  
Yes for the core paper claim.  
`Plan A / IBN-only / HIBD` is the minimal sufficient trio:
- `A.2 vs A.1` isolates the structural IBN effect
- `A.3 vs A.2` isolates the value of the new decorrelation loss
- `A.3 vs A.1` supports the main practical claim

That is enough. I would not ask for more unless results are very close/noisy.

4. **Any remaining blocker issues?**  
Yes, but fewer:
- fix the loss scaling mismatch (`normalize` plus `/HW`)
- stop calling it “hierarchical” unless you really want to defend that wording
- keep the paper language away from “style/semantic branches” for the IBN halves
- report `final` as well as `best`; `AVG Best` alone is too soft for a mechanism paper

**Scores**

| Dimension | Score | Reason |
|---|---:|---|
| 1. Problem Fidelity | 9 | Still tightly anchored to the actual failure diagnosis; no prototype drift. |
| 2. Method Specificity | 7 | Much better, but the written layer-loss formula still needs one cleanup to be fully coherent. |
| 3. Contribution Quality | 8 | Focused, elegant, and appropriately small. This is the right kind of minimal contribution. |
| 4. Frontier Leverage | 9 | Appropriate restraint. No FM pivot is needed here. |
| 5. Feasibility | 8 | Easy to implement and cheap to test. Scientific risk remains, engineering risk is low. |
| 6. Validation Focus | 8 | The new `IBN-only` control is the key missing piece and now makes the validation credible. |
| 7. Venue Readiness | 6 | Better, but the paper can still be attacked on objective-definition precision and slightly inflated framing. |

**Overall: 8.0/10**

**Below 7**

**Venue Readiness — 6/10**  
Weakness: the proposal is now conceptually coherent, but the written objective and framing still leave avoidable reviewer openings.

Concrete fix:
- choose one consistent loss definition:
  - either “pairwise spatial cosine decorrelation” with `F.normalize` and no `/HW`
  - or true “cross-correlation decorrelation” with centered/standardized maps and `/HW`
- rename if possible; “hierarchical” is stronger than what is actually implemented
- say “no added trainable parameters” rather than “0 new trainable” to avoid BN-parameter-count nitpicks
- make `final` performance part of the main table, not just `best`

Priority: **CRITICAL**

**Simplification Opportunities**

- Drop “hierarchical” from the title unless you really use multiple depth stages in a way you want to defend.
- Fix `in_ratio=0.5` and `num_ibn=2` as default, not as a broad search space.
- Use one backbone as the main result path first. If AlexNet is the active battleground, make that the center of gravity.

**Modernization Opportunities**

No foundation-model pivot is warranted. For this anchored problem, adding CLIP/VLM/LLM machinery would be drift.

**Drift Warning**

No major drift now. The only mild drift risk is rhetorical: the method name still sounds larger than the actual mechanism.

**Verdict**

**REVISE**

Round 1 was “interesting but mathematically broken.”  
Round 2 is “credible, focused, and worth running,” but not yet fully venue-ready on paper because the layer objective still needs one precise cleanup and the framing should get even tighter. If you fix the loss definition and the method then clears `A.3 > A.2 > A.1` on PACS with stable `final` numbers, this becomes a defensible minimal paper.
